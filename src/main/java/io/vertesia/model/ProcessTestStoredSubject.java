/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260905.011013Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.Arrays;
import java.util.HashSet;
import java.util.Objects;

/**
 * ProcessTestStoredSubject
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class ProcessTestStoredSubject {
    /**
     * Gets or Sets kind
     */
    @JsonAdapter(KindEnum.Adapter.class)
    public enum KindEnum {
        STORED("stored"),

        UNKNOWN_DEFAULT_OPEN_API("unknown_default_open_api");

        private String value;

        KindEnum(String value) {
            this.value = value;
        }

        public String getValue() {
            return value;
        }

        @Override
        public String toString() {
            return String.valueOf(value);
        }

        public static KindEnum fromValue(String value) {
            for (KindEnum b : KindEnum.values()) {
                if (b.value.equals(value)) {
                    return b;
                }
            }
            return UNKNOWN_DEFAULT_OPEN_API;
        }

        public static class Adapter extends TypeAdapter<KindEnum> {
            @Override
            public void write(final JsonWriter jsonWriter, final KindEnum enumeration)
                    throws IOException {
                jsonWriter.value(enumeration.getValue());
            }

            @Override
            public KindEnum read(final JsonReader jsonReader) throws IOException {
                String value = jsonReader.nextString();
                return KindEnum.fromValue(value);
            }
        }

        public static void validateJsonElement(JsonElement jsonElement) throws IOException {
            String value = jsonElement.getAsString();
            KindEnum.fromValue(value);
        }
    }

    public static final String SERIALIZED_NAME_KIND = "kind";

    @SerializedName(SERIALIZED_NAME_KIND)
    @jakarta.annotation.Nonnull
    private KindEnum kind;

    public static final String SERIALIZED_NAME_PROCESS_ROOT_ID = "process_root_id";

    @SerializedName(SERIALIZED_NAME_PROCESS_ROOT_ID)
    @jakarta.annotation.Nonnull
    private String processRootId;

    public static final String SERIALIZED_NAME_PROCESS_REVISION_ID = "process_revision_id";

    @SerializedName(SERIALIZED_NAME_PROCESS_REVISION_ID)
    @jakarta.annotation.Nonnull
    private String processRevisionId;

    public ProcessTestStoredSubject() {}

    public ProcessTestStoredSubject kind(@jakarta.annotation.Nonnull KindEnum kind) {
        this.kind = kind;
        return this;
    }

    /**
     * Get kind
     * @return kind
     */
    @jakarta.annotation.Nonnull
    public KindEnum getKind() {
        return kind;
    }

    public void setKind(@jakarta.annotation.Nonnull KindEnum kind) {
        this.kind = kind;
    }

    public ProcessTestStoredSubject processRootId(
            @jakarta.annotation.Nonnull String processRootId) {
        this.processRootId = processRootId;
        return this;
    }

    /**
     * Get processRootId
     * @return processRootId
     */
    @jakarta.annotation.Nonnull
    public String getProcessRootId() {
        return processRootId;
    }

    public void setProcessRootId(@jakarta.annotation.Nonnull String processRootId) {
        this.processRootId = processRootId;
    }

    public ProcessTestStoredSubject processRevisionId(
            @jakarta.annotation.Nonnull String processRevisionId) {
        this.processRevisionId = processRevisionId;
        return this;
    }

    /**
     * Get processRevisionId
     * @return processRevisionId
     */
    @jakarta.annotation.Nonnull
    public String getProcessRevisionId() {
        return processRevisionId;
    }

    public void setProcessRevisionId(@jakarta.annotation.Nonnull String processRevisionId) {
        this.processRevisionId = processRevisionId;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ProcessTestStoredSubject processTestStoredSubject = (ProcessTestStoredSubject) o;
        return Objects.equals(this.kind, processTestStoredSubject.kind)
                && Objects.equals(this.processRootId, processTestStoredSubject.processRootId)
                && Objects.equals(
                        this.processRevisionId, processTestStoredSubject.processRevisionId);
    }

    @Override
    public int hashCode() {
        return Objects.hash(kind, processRootId, processRevisionId);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class ProcessTestStoredSubject {\n");
        sb.append("    kind: ").append(toIndentedString(kind)).append("\n");
        sb.append("    processRootId: ").append(toIndentedString(processRootId)).append("\n");
        sb.append("    processRevisionId: ")
                .append(toIndentedString(processRevisionId))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields =
                new HashSet<String>(
                        Arrays.asList("kind", "process_root_id", "process_revision_id"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields =
                new HashSet<String>(
                        Arrays.asList("kind", "process_root_id", "process_revision_id"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to ProcessTestStoredSubject
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!ProcessTestStoredSubject.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in ProcessTestStoredSubject is not found in the empty JSON string",
                                ProcessTestStoredSubject.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : ProcessTestStoredSubject.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (!jsonObj.get("kind").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `kind` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("kind").toString()));
        }
        // validate the required field `kind`
        KindEnum.validateJsonElement(jsonObj.get("kind"));
        if (!jsonObj.get("process_root_id").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `process_root_id` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("process_root_id").toString()));
        }
        if (!jsonObj.get("process_revision_id").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `process_revision_id` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("process_revision_id").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ProcessTestStoredSubject.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'ProcessTestStoredSubject' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ProcessTestStoredSubject> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(ProcessTestStoredSubject.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<ProcessTestStoredSubject>() {
                        @Override
                        public void write(JsonWriter out, ProcessTestStoredSubject value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public ProcessTestStoredSubject read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of ProcessTestStoredSubject given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of ProcessTestStoredSubject
     * @throws IOException if the JSON string is invalid with respect to ProcessTestStoredSubject
     */
    public static ProcessTestStoredSubject fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, ProcessTestStoredSubject.class);
    }

    /**
     * Convert an instance of ProcessTestStoredSubject to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
