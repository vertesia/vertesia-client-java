/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.6.0-dev
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.Arrays;
import java.util.HashSet;
import java.util.Objects;

/**
 * Client-side selection behavior for normalized View hits.
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class ViewSelectionConfiguration {
    public static final String SERIALIZED_NAME_MODE = "mode";

    @SerializedName(SERIALIZED_NAME_MODE)
    @jakarta.annotation.Nonnull
    private ViewSelectionMode mode;

    /**
     * Select-all applies to the currently rendered page.
     */
    @JsonAdapter(SelectAllEnum.Adapter.class)
    public enum SelectAllEnum {
        PAGE("page"),

        UNKNOWN_DEFAULT_OPEN_API("unknown_default_open_api");

        private String value;

        SelectAllEnum(String value) {
            this.value = value;
        }

        public String getValue() {
            return value;
        }

        @Override
        public String toString() {
            return String.valueOf(value);
        }

        public static SelectAllEnum fromValue(String value) {
            for (SelectAllEnum b : SelectAllEnum.values()) {
                if (b.value.equals(value)) {
                    return b;
                }
            }
            return UNKNOWN_DEFAULT_OPEN_API;
        }

        public static class Adapter extends TypeAdapter<SelectAllEnum> {
            @Override
            public void write(final JsonWriter jsonWriter, final SelectAllEnum enumeration)
                    throws IOException {
                jsonWriter.value(enumeration.getValue());
            }

            @Override
            public SelectAllEnum read(final JsonReader jsonReader) throws IOException {
                String value = jsonReader.nextString();
                return SelectAllEnum.fromValue(value);
            }
        }

        public static void validateJsonElement(JsonElement jsonElement) throws IOException {
            String value = jsonElement.getAsString();
            SelectAllEnum.fromValue(value);
        }
    }

    public static final String SERIALIZED_NAME_SELECT_ALL = "select_all";

    @SerializedName(SERIALIZED_NAME_SELECT_ALL)
    @jakarta.annotation.Nullable private SelectAllEnum selectAll;

    public ViewSelectionConfiguration() {}

    public ViewSelectionConfiguration mode(@jakarta.annotation.Nonnull ViewSelectionMode mode) {
        this.mode = mode;
        return this;
    }

    /**
     * Get mode
     * @return mode
     */
    @jakarta.annotation.Nonnull
    public ViewSelectionMode getMode() {
        return mode;
    }

    public void setMode(@jakarta.annotation.Nonnull ViewSelectionMode mode) {
        this.mode = mode;
    }

    public ViewSelectionConfiguration selectAll(
            @jakarta.annotation.Nullable SelectAllEnum selectAll) {
        this.selectAll = selectAll;
        return this;
    }

    /**
     * Select-all applies to the currently rendered page.
     * @return selectAll
     */
    @jakarta.annotation.Nullable public SelectAllEnum getSelectAll() {
        return selectAll;
    }

    public void setSelectAll(@jakarta.annotation.Nullable SelectAllEnum selectAll) {
        this.selectAll = selectAll;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ViewSelectionConfiguration viewSelectionConfiguration = (ViewSelectionConfiguration) o;
        return Objects.equals(this.mode, viewSelectionConfiguration.mode)
                && Objects.equals(this.selectAll, viewSelectionConfiguration.selectAll);
    }

    @Override
    public int hashCode() {
        return Objects.hash(mode, selectAll);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class ViewSelectionConfiguration {\n");
        sb.append("    mode: ").append(toIndentedString(mode)).append("\n");
        sb.append("    selectAll: ").append(toIndentedString(selectAll)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>(Arrays.asList("mode", "select_all"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(Arrays.asList("mode"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to ViewSelectionConfiguration
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!ViewSelectionConfiguration.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in ViewSelectionConfiguration is not found in the empty JSON string",
                                ViewSelectionConfiguration.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : ViewSelectionConfiguration.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // validate the required field `mode`
        ViewSelectionMode.validateJsonElement(jsonObj.get("mode"));
        if ((jsonObj.get("select_all") != null && !jsonObj.get("select_all").isJsonNull())
                && !jsonObj.get("select_all").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `select_all` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("select_all").toString()));
        }
        // validate the optional field `select_all`
        if (jsonObj.get("select_all") != null && !jsonObj.get("select_all").isJsonNull()) {
            SelectAllEnum.validateJsonElement(jsonObj.get("select_all"));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ViewSelectionConfiguration.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'ViewSelectionConfiguration' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ViewSelectionConfiguration> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(ViewSelectionConfiguration.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<ViewSelectionConfiguration>() {
                        @Override
                        public void write(JsonWriter out, ViewSelectionConfiguration value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public ViewSelectionConfiguration read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of ViewSelectionConfiguration given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of ViewSelectionConfiguration
     * @throws IOException if the JSON string is invalid with respect to ViewSelectionConfiguration
     */
    public static ViewSelectionConfiguration fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, ViewSelectionConfiguration.class);
    }

    /**
     * Convert an instance of ViewSelectionConfiguration to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
