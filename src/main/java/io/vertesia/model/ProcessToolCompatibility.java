/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260901.052906Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.Arrays;
import java.util.HashSet;
import java.util.Objects;

/**
 * ProcessToolCompatibility
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class ProcessToolCompatibility {
    public static final String SERIALIZED_NAME_COMPATIBLE = "compatible";

    @SerializedName(SERIALIZED_NAME_COMPATIBLE)
    @jakarta.annotation.Nonnull
    private Boolean compatible;

    public static final String SERIALIZED_NAME_REASON = "reason";

    @SerializedName(SERIALIZED_NAME_REASON)
    @jakarta.annotation.Nullable private ProcessToolCompatibilityReason reason;

    public ProcessToolCompatibility() {}

    public ProcessToolCompatibility compatible(@jakarta.annotation.Nonnull Boolean compatible) {
        this.compatible = compatible;
        return this;
    }

    /**
     * Get compatible
     * @return compatible
     */
    @jakarta.annotation.Nonnull
    public Boolean getCompatible() {
        return compatible;
    }

    public void setCompatible(@jakarta.annotation.Nonnull Boolean compatible) {
        this.compatible = compatible;
    }

    public ProcessToolCompatibility reason(
            @jakarta.annotation.Nullable ProcessToolCompatibilityReason reason) {
        this.reason = reason;
        return this;
    }

    /**
     * Get reason
     * @return reason
     */
    @jakarta.annotation.Nullable public ProcessToolCompatibilityReason getReason() {
        return reason;
    }

    public void setReason(@jakarta.annotation.Nullable ProcessToolCompatibilityReason reason) {
        this.reason = reason;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ProcessToolCompatibility processToolCompatibility = (ProcessToolCompatibility) o;
        return Objects.equals(this.compatible, processToolCompatibility.compatible)
                && Objects.equals(this.reason, processToolCompatibility.reason);
    }

    @Override
    public int hashCode() {
        return Objects.hash(compatible, reason);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class ProcessToolCompatibility {\n");
        sb.append("    compatible: ").append(toIndentedString(compatible)).append("\n");
        sb.append("    reason: ").append(toIndentedString(reason)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>(Arrays.asList("compatible", "reason"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(Arrays.asList("compatible"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to ProcessToolCompatibility
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!ProcessToolCompatibility.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in ProcessToolCompatibility is not found in the empty JSON string",
                                ProcessToolCompatibility.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : ProcessToolCompatibility.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // validate the optional field `reason`
        if (jsonObj.get("reason") != null && !jsonObj.get("reason").isJsonNull()) {
            ProcessToolCompatibilityReason.validateJsonElement(jsonObj.get("reason"));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ProcessToolCompatibility.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'ProcessToolCompatibility' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ProcessToolCompatibility> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(ProcessToolCompatibility.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<ProcessToolCompatibility>() {
                        @Override
                        public void write(JsonWriter out, ProcessToolCompatibility value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public ProcessToolCompatibility read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of ProcessToolCompatibility given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of ProcessToolCompatibility
     * @throws IOException if the JSON string is invalid with respect to ProcessToolCompatibility
     */
    public static ProcessToolCompatibility fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, ProcessToolCompatibility.class);
    }

    /**
     * Convert an instance of ProcessToolCompatibility to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
