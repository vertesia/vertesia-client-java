/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260804.124748Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.math.BigDecimal;
import java.util.Arrays;
import java.util.HashSet;
import java.util.Objects;

/**
 * How each extracted value was verified. Two kinds, both trustworthy: digitally verified (matched the document&#39;s text — digital layer or OCR) and AI verified (the reviewer confirmed it against the page image — the primary signal for scanned or handwritten content, which has no text layer to match).
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class GroundedVerificationBreakdown {
    public static final String SERIALIZED_NAME_TOTAL = "total";

    @SerializedName(SERIALIZED_NAME_TOTAL)
    @jakarta.annotation.Nonnull
    private BigDecimal total;

    public static final String SERIALIZED_NAME_DIGITALLY_VERIFIED = "digitally_verified";

    @SerializedName(SERIALIZED_NAME_DIGITALLY_VERIFIED)
    @jakarta.annotation.Nonnull
    private BigDecimal digitallyVerified;

    public static final String SERIALIZED_NAME_AI_VERIFIED = "ai_verified";

    @SerializedName(SERIALIZED_NAME_AI_VERIFIED)
    @jakarta.annotation.Nonnull
    private BigDecimal aiVerified;

    public static final String SERIALIZED_NAME_UNVERIFIED = "unverified";

    @SerializedName(SERIALIZED_NAME_UNVERIFIED)
    @jakarta.annotation.Nonnull
    private BigDecimal unverified;

    public GroundedVerificationBreakdown() {}

    public GroundedVerificationBreakdown total(@jakarta.annotation.Nonnull BigDecimal total) {
        this.total = total;
        return this;
    }

    /**
     * Total number of cited values.
     * @return total
     */
    @jakarta.annotation.Nonnull
    public BigDecimal getTotal() {
        return total;
    }

    public void setTotal(@jakarta.annotation.Nonnull BigDecimal total) {
        this.total = total;
    }

    public GroundedVerificationBreakdown digitallyVerified(
            @jakarta.annotation.Nonnull BigDecimal digitallyVerified) {
        this.digitallyVerified = digitallyVerified;
        return this;
    }

    /**
     * Values matched verbatim against the document&#39;s text (digital layer or OCR).
     * @return digitallyVerified
     */
    @jakarta.annotation.Nonnull
    public BigDecimal getDigitallyVerified() {
        return digitallyVerified;
    }

    public void setDigitallyVerified(@jakarta.annotation.Nonnull BigDecimal digitallyVerified) {
        this.digitallyVerified = digitallyVerified;
    }

    public GroundedVerificationBreakdown aiVerified(
            @jakarta.annotation.Nonnull BigDecimal aiVerified) {
        this.aiVerified = aiVerified;
        return this;
    }

    /**
     * Values the reviewer model confirmed against the page image.
     * @return aiVerified
     */
    @jakarta.annotation.Nonnull
    public BigDecimal getAiVerified() {
        return aiVerified;
    }

    public void setAiVerified(@jakarta.annotation.Nonnull BigDecimal aiVerified) {
        this.aiVerified = aiVerified;
    }

    public GroundedVerificationBreakdown unverified(
            @jakarta.annotation.Nonnull BigDecimal unverified) {
        this.unverified = unverified;
        return this;
    }

    /**
     * Values read from the image but neither text-matched nor reviewer-confirmed.
     * @return unverified
     */
    @jakarta.annotation.Nonnull
    public BigDecimal getUnverified() {
        return unverified;
    }

    public void setUnverified(@jakarta.annotation.Nonnull BigDecimal unverified) {
        this.unverified = unverified;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        GroundedVerificationBreakdown groundedVerificationBreakdown =
                (GroundedVerificationBreakdown) o;
        return Objects.equals(this.total, groundedVerificationBreakdown.total)
                && Objects.equals(
                        this.digitallyVerified, groundedVerificationBreakdown.digitallyVerified)
                && Objects.equals(this.aiVerified, groundedVerificationBreakdown.aiVerified)
                && Objects.equals(this.unverified, groundedVerificationBreakdown.unverified);
    }

    @Override
    public int hashCode() {
        return Objects.hash(total, digitallyVerified, aiVerified, unverified);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class GroundedVerificationBreakdown {\n");
        sb.append("    total: ").append(toIndentedString(total)).append("\n");
        sb.append("    digitallyVerified: ")
                .append(toIndentedString(digitallyVerified))
                .append("\n");
        sb.append("    aiVerified: ").append(toIndentedString(aiVerified)).append("\n");
        sb.append("    unverified: ").append(toIndentedString(unverified)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields =
                new HashSet<String>(
                        Arrays.asList("total", "digitally_verified", "ai_verified", "unverified"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields =
                new HashSet<String>(
                        Arrays.asList("total", "digitally_verified", "ai_verified", "unverified"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to GroundedVerificationBreakdown
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!GroundedVerificationBreakdown.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in GroundedVerificationBreakdown is not found in the empty JSON string",
                                GroundedVerificationBreakdown.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : GroundedVerificationBreakdown.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!GroundedVerificationBreakdown.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'GroundedVerificationBreakdown' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<GroundedVerificationBreakdown> thisAdapter =
                    gson.getDelegateAdapter(
                            this, TypeToken.get(GroundedVerificationBreakdown.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<GroundedVerificationBreakdown>() {
                        @Override
                        public void write(JsonWriter out, GroundedVerificationBreakdown value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public GroundedVerificationBreakdown read(JsonReader in)
                                throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of GroundedVerificationBreakdown given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of GroundedVerificationBreakdown
     * @throws IOException if the JSON string is invalid with respect to GroundedVerificationBreakdown
     */
    public static GroundedVerificationBreakdown fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, GroundedVerificationBreakdown.class);
    }

    /**
     * Convert an instance of GroundedVerificationBreakdown to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
