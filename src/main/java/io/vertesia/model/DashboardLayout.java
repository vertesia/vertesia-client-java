/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.3.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.math.BigDecimal;
import java.util.Arrays;
import java.util.HashSet;
import java.util.Objects;

/**
 * Dashboard layout configuration.
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class DashboardLayout {
    public static final String SERIALIZED_NAME_COLUMNS = "columns";

    @SerializedName(SERIALIZED_NAME_COLUMNS)
    @jakarta.annotation.Nonnull
    private BigDecimal columns;

    public static final String SERIALIZED_NAME_CELL_WIDTH = "cellWidth";

    @SerializedName(SERIALIZED_NAME_CELL_WIDTH)
    @jakarta.annotation.Nonnull
    private BigDecimal cellWidth;

    public static final String SERIALIZED_NAME_CELL_HEIGHT = "cellHeight";

    @SerializedName(SERIALIZED_NAME_CELL_HEIGHT)
    @jakarta.annotation.Nonnull
    private BigDecimal cellHeight;

    public static final String SERIALIZED_NAME_PADDING = "padding";

    @SerializedName(SERIALIZED_NAME_PADDING)
    @jakarta.annotation.Nonnull
    private BigDecimal padding;

    public DashboardLayout() {}

    public DashboardLayout columns(@jakarta.annotation.Nonnull BigDecimal columns) {
        this.columns = columns;
        return this;
    }

    /**
     * Number of columns in the grid (default: 2)
     * @return columns
     */
    @jakarta.annotation.Nonnull
    public BigDecimal getColumns() {
        return columns;
    }

    public void setColumns(@jakarta.annotation.Nonnull BigDecimal columns) {
        this.columns = columns;
    }

    public DashboardLayout cellWidth(@jakarta.annotation.Nonnull BigDecimal cellWidth) {
        this.cellWidth = cellWidth;
        return this;
    }

    /**
     * Width of each cell in pixels (default: 600)
     * @return cellWidth
     */
    @jakarta.annotation.Nonnull
    public BigDecimal getCellWidth() {
        return cellWidth;
    }

    public void setCellWidth(@jakarta.annotation.Nonnull BigDecimal cellWidth) {
        this.cellWidth = cellWidth;
    }

    public DashboardLayout cellHeight(@jakarta.annotation.Nonnull BigDecimal cellHeight) {
        this.cellHeight = cellHeight;
        return this;
    }

    /**
     * Height of each cell in pixels (default: 400)
     * @return cellHeight
     */
    @jakarta.annotation.Nonnull
    public BigDecimal getCellHeight() {
        return cellHeight;
    }

    public void setCellHeight(@jakarta.annotation.Nonnull BigDecimal cellHeight) {
        this.cellHeight = cellHeight;
    }

    public DashboardLayout padding(@jakarta.annotation.Nonnull BigDecimal padding) {
        this.padding = padding;
        return this;
    }

    /**
     * Padding between cells in pixels (default: 20)
     * @return padding
     */
    @jakarta.annotation.Nonnull
    public BigDecimal getPadding() {
        return padding;
    }

    public void setPadding(@jakarta.annotation.Nonnull BigDecimal padding) {
        this.padding = padding;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        DashboardLayout dashboardLayout = (DashboardLayout) o;
        return Objects.equals(this.columns, dashboardLayout.columns)
                && Objects.equals(this.cellWidth, dashboardLayout.cellWidth)
                && Objects.equals(this.cellHeight, dashboardLayout.cellHeight)
                && Objects.equals(this.padding, dashboardLayout.padding);
    }

    @Override
    public int hashCode() {
        return Objects.hash(columns, cellWidth, cellHeight, padding);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class DashboardLayout {\n");
        sb.append("    columns: ").append(toIndentedString(columns)).append("\n");
        sb.append("    cellWidth: ").append(toIndentedString(cellWidth)).append("\n");
        sb.append("    cellHeight: ").append(toIndentedString(cellHeight)).append("\n");
        sb.append("    padding: ").append(toIndentedString(padding)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields =
                new HashSet<String>(Arrays.asList("columns", "cellWidth", "cellHeight", "padding"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields =
                new HashSet<String>(Arrays.asList("columns", "cellWidth", "cellHeight", "padding"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to DashboardLayout
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!DashboardLayout.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in DashboardLayout is not found in the empty JSON string",
                                DashboardLayout.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : DashboardLayout.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!DashboardLayout.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'DashboardLayout' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<DashboardLayout> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(DashboardLayout.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<DashboardLayout>() {
                        @Override
                        public void write(JsonWriter out, DashboardLayout value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public DashboardLayout read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of DashboardLayout given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of DashboardLayout
     * @throws IOException if the JSON string is invalid with respect to DashboardLayout
     */
    public static DashboardLayout fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, DashboardLayout.class);
    }

    /**
     * Convert an instance of DashboardLayout to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
