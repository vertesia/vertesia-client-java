/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260804.124748Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.math.BigDecimal;
import java.util.Arrays;
import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;
import org.openapitools.jackson.nullable.JsonNullable;

/**
 * Completed grounded extraction result: the extracted data with its trust verdict and verification breakdown, plus a download URL for the full citations artifact.
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class GroundedExtractionResultResponse {
    public static final String SERIALIZED_NAME_OBJECT_ID = "object_id";

    @SerializedName(SERIALIZED_NAME_OBJECT_ID)
    @jakarta.annotation.Nonnull
    private String objectId;

    public static final String SERIALIZED_NAME_DATA = "data";

    @SerializedName(SERIALIZED_NAME_DATA)
    @jakarta.annotation.Nonnull
    private Map<String, Object> data = new HashMap<>();

    public static final String SERIALIZED_NAME_VERDICT = "verdict";

    @SerializedName(SERIALIZED_NAME_VERDICT)
    @jakarta.annotation.Nullable private GroundedExtractionVerdict verdict;

    public static final String SERIALIZED_NAME_VERDICT_REASON = "verdict_reason";

    @SerializedName(SERIALIZED_NAME_VERDICT_REASON)
    @jakarta.annotation.Nullable private String verdictReason;

    public static final String SERIALIZED_NAME_CONFIDENCE = "confidence";

    @SerializedName(SERIALIZED_NAME_CONFIDENCE)
    @jakarta.annotation.Nullable private BigDecimal confidence;

    public static final String SERIALIZED_NAME_VERIFICATION = "verification";

    @SerializedName(SERIALIZED_NAME_VERIFICATION)
    @jakarta.annotation.Nonnull
    private GroundedVerificationBreakdown verification;

    public static final String SERIALIZED_NAME_REVIEW = "review";

    @SerializedName(SERIALIZED_NAME_REVIEW)
    @jakarta.annotation.Nullable private GroundedExtractionResultResponseReview review;

    public static final String SERIALIZED_NAME_RESULT_URL = "result_url";

    @SerializedName(SERIALIZED_NAME_RESULT_URL)
    @jakarta.annotation.Nullable private String resultUrl;

    public GroundedExtractionResultResponse() {}

    public GroundedExtractionResultResponse objectId(@jakarta.annotation.Nonnull String objectId) {
        this.objectId = objectId;
        return this;
    }

    /**
     * Get objectId
     * @return objectId
     */
    @jakarta.annotation.Nonnull
    public String getObjectId() {
        return objectId;
    }

    public void setObjectId(@jakarta.annotation.Nonnull String objectId) {
        this.objectId = objectId;
    }

    public GroundedExtractionResultResponse data(
            @jakarta.annotation.Nonnull Map<String, Object> data) {
        this.data = data;
        return this;
    }

    public GroundedExtractionResultResponse putDataItem(String key, Object dataItem) {
        if (this.data == null) {
            this.data = new HashMap<>();
        }
        this.data.put(key, dataItem);
        return this;
    }

    /**
     * The extracted data, shaped by the requested schema.
     * @return data
     */
    @jakarta.annotation.Nonnull
    public Map<String, Object> getData() {
        return data;
    }

    public void setData(@jakarta.annotation.Nonnull Map<String, Object> data) {
        this.data = data;
    }

    public GroundedExtractionResultResponse verdict(
            @jakarta.annotation.Nullable GroundedExtractionVerdict verdict) {
        this.verdict = verdict;
        return this;
    }

    /**
     * Document-level trust verdict.
     * @return verdict
     */
    @jakarta.annotation.Nullable public GroundedExtractionVerdict getVerdict() {
        return verdict;
    }

    public void setVerdict(@jakarta.annotation.Nullable GroundedExtractionVerdict verdict) {
        this.verdict = verdict;
    }

    public GroundedExtractionResultResponse verdictReason(
            @jakarta.annotation.Nullable String verdictReason) {
        this.verdictReason = verdictReason;
        return this;
    }

    /**
     * One-sentence rationale for the verdict.
     * @return verdictReason
     */
    @jakarta.annotation.Nullable public String getVerdictReason() {
        return verdictReason;
    }

    public void setVerdictReason(@jakarta.annotation.Nullable String verdictReason) {
        this.verdictReason = verdictReason;
    }

    public GroundedExtractionResultResponse confidence(
            @jakarta.annotation.Nullable BigDecimal confidence) {
        this.confidence = confidence;
        return this;
    }

    /**
     * Mean citation confidence in [0,1].
     * @return confidence
     */
    @jakarta.annotation.Nullable public BigDecimal getConfidence() {
        return confidence;
    }

    public void setConfidence(@jakarta.annotation.Nullable BigDecimal confidence) {
        this.confidence = confidence;
    }

    public GroundedExtractionResultResponse verification(
            @jakarta.annotation.Nonnull GroundedVerificationBreakdown verification) {
        this.verification = verification;
        return this;
    }

    /**
     * Per-value verification breakdown.
     * @return verification
     */
    @jakarta.annotation.Nonnull
    public GroundedVerificationBreakdown getVerification() {
        return verification;
    }

    public void setVerification(
            @jakarta.annotation.Nonnull GroundedVerificationBreakdown verification) {
        this.verification = verification;
    }

    public GroundedExtractionResultResponse review(
            @jakarta.annotation.Nullable GroundedExtractionResultResponseReview review) {
        this.review = review;
        return this;
    }

    /**
     * Get review
     * @return review
     */
    @jakarta.annotation.Nullable public GroundedExtractionResultResponseReview getReview() {
        return review;
    }

    public void setReview(
            @jakarta.annotation.Nullable GroundedExtractionResultResponseReview review) {
        this.review = review;
    }

    public GroundedExtractionResultResponse resultUrl(
            @jakarta.annotation.Nullable String resultUrl) {
        this.resultUrl = resultUrl;
        return this;
    }

    /**
     * Signed download URL for the full grounded-extraction.json (data + citations + boxes).
     * @return resultUrl
     */
    @jakarta.annotation.Nullable public String getResultUrl() {
        return resultUrl;
    }

    public void setResultUrl(@jakarta.annotation.Nullable String resultUrl) {
        this.resultUrl = resultUrl;
    }

    /**
     * A container for additional, undeclared properties.
     * This is a holder for any undeclared properties as specified with
     * the 'additionalProperties' keyword in the OAS document.
     */
    private Map<String, Object> additionalProperties;

    /**
     * Set the additional (undeclared) property with the specified name and value.
     * If the property does not already exist, create it otherwise replace it.
     *
     * @param key name of the property
     * @param value value of the property
     * @return the GroundedExtractionResultResponse instance itself
     */
    public GroundedExtractionResultResponse putAdditionalProperty(String key, Object value) {
        if (this.additionalProperties == null) {
            this.additionalProperties = new HashMap<String, Object>();
        }
        this.additionalProperties.put(key, value);
        return this;
    }

    /**
     * Return the additional (undeclared) property.
     *
     * @return a map of objects
     */
    public Map<String, Object> getAdditionalProperties() {
        return additionalProperties;
    }

    /**
     * Return the additional (undeclared) property with the specified name.
     *
     * @param key name of the property
     * @return an object
     */
    public Object getAdditionalProperty(String key) {
        if (this.additionalProperties == null) {
            return null;
        }
        return this.additionalProperties.get(key);
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        GroundedExtractionResultResponse groundedExtractionResultResponse =
                (GroundedExtractionResultResponse) o;
        return Objects.equals(this.objectId, groundedExtractionResultResponse.objectId)
                && Objects.equals(this.data, groundedExtractionResultResponse.data)
                && Objects.equals(this.verdict, groundedExtractionResultResponse.verdict)
                && Objects.equals(
                        this.verdictReason, groundedExtractionResultResponse.verdictReason)
                && Objects.equals(this.confidence, groundedExtractionResultResponse.confidence)
                && Objects.equals(this.verification, groundedExtractionResultResponse.verification)
                && Objects.equals(this.review, groundedExtractionResultResponse.review)
                && Objects.equals(this.resultUrl, groundedExtractionResultResponse.resultUrl)
                && Objects.equals(
                        this.additionalProperties,
                        groundedExtractionResultResponse.additionalProperties);
    }

    private static <T> boolean equalsNullable(JsonNullable<T> a, JsonNullable<T> b) {
        return a == b
                || (a != null
                        && b != null
                        && a.isPresent()
                        && b.isPresent()
                        && Objects.deepEquals(a.get(), b.get()));
    }

    @Override
    public int hashCode() {
        return Objects.hash(
                objectId,
                data,
                verdict,
                verdictReason,
                confidence,
                verification,
                review,
                resultUrl,
                additionalProperties);
    }

    private static <T> int hashCodeNullable(JsonNullable<T> a) {
        if (a == null) {
            return 1;
        }
        return a.isPresent() ? Arrays.deepHashCode(new Object[] {a.get()}) : 31;
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class GroundedExtractionResultResponse {\n");
        sb.append("    objectId: ").append(toIndentedString(objectId)).append("\n");
        sb.append("    data: ").append(toIndentedString(data)).append("\n");
        sb.append("    verdict: ").append(toIndentedString(verdict)).append("\n");
        sb.append("    verdictReason: ").append(toIndentedString(verdictReason)).append("\n");
        sb.append("    confidence: ").append(toIndentedString(confidence)).append("\n");
        sb.append("    verification: ").append(toIndentedString(verification)).append("\n");
        sb.append("    review: ").append(toIndentedString(review)).append("\n");
        sb.append("    resultUrl: ").append(toIndentedString(resultUrl)).append("\n");
        sb.append("    additionalProperties: ")
                .append(toIndentedString(additionalProperties))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields =
                new HashSet<String>(
                        Arrays.asList(
                                "object_id",
                                "data",
                                "verdict",
                                "verdict_reason",
                                "confidence",
                                "verification",
                                "review",
                                "result_url"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields =
                new HashSet<String>(Arrays.asList("object_id", "data", "verification"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to GroundedExtractionResultResponse
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!GroundedExtractionResultResponse.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in GroundedExtractionResultResponse is not found in the empty JSON string",
                                GroundedExtractionResultResponse.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : GroundedExtractionResultResponse.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (!jsonObj.get("object_id").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `object_id` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("object_id").toString()));
        }
        // validate the optional field `verdict`
        if (jsonObj.get("verdict") != null && !jsonObj.get("verdict").isJsonNull()) {
            GroundedExtractionVerdict.validateJsonElement(jsonObj.get("verdict"));
        }
        if ((jsonObj.get("verdict_reason") != null && !jsonObj.get("verdict_reason").isJsonNull())
                && !jsonObj.get("verdict_reason").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `verdict_reason` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("verdict_reason").toString()));
        }
        // validate the required field `verification`
        GroundedVerificationBreakdown.validateJsonElement(jsonObj.get("verification"));
        // validate the optional field `review`
        if (jsonObj.get("review") != null && !jsonObj.get("review").isJsonNull()) {
            GroundedExtractionResultResponseReview.validateJsonElement(jsonObj.get("review"));
        }
        if ((jsonObj.get("result_url") != null && !jsonObj.get("result_url").isJsonNull())
                && !jsonObj.get("result_url").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `result_url` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("result_url").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!GroundedExtractionResultResponse.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'GroundedExtractionResultResponse' and
                // its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<GroundedExtractionResultResponse> thisAdapter =
                    gson.getDelegateAdapter(
                            this, TypeToken.get(GroundedExtractionResultResponse.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<GroundedExtractionResultResponse>() {
                        @Override
                        public void write(JsonWriter out, GroundedExtractionResultResponse value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            obj.remove("additionalProperties");
                            // serialize additional properties
                            if (value.getAdditionalProperties() != null) {
                                for (Map.Entry<String, Object> entry :
                                        value.getAdditionalProperties().entrySet()) {
                                    if (entry.getValue() instanceof String)
                                        obj.addProperty(entry.getKey(), (String) entry.getValue());
                                    else if (entry.getValue() instanceof Number)
                                        obj.addProperty(entry.getKey(), (Number) entry.getValue());
                                    else if (entry.getValue() instanceof Boolean)
                                        obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                                    else if (entry.getValue() instanceof Character)
                                        obj.addProperty(
                                                entry.getKey(), (Character) entry.getValue());
                                    else {
                                        JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                                        if (jsonElement.isJsonArray()) {
                                            obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                                        } else {
                                            obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                                        }
                                    }
                                }
                            }
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public GroundedExtractionResultResponse read(JsonReader in)
                                throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            JsonObject jsonObj = jsonElement.getAsJsonObject();
                            // store additional fields in the deserialized instance
                            GroundedExtractionResultResponse instance =
                                    thisAdapter.fromJsonTree(jsonObj);
                            for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
                                if (!openapiFields.contains(entry.getKey())) {
                                    if (entry.getValue().isJsonPrimitive()) { // primitive type
                                        if (entry.getValue().getAsJsonPrimitive().isString())
                                            instance.putAdditionalProperty(
                                                    entry.getKey(), entry.getValue().getAsString());
                                        else if (entry.getValue().getAsJsonPrimitive().isNumber())
                                            instance.putAdditionalProperty(
                                                    entry.getKey(), entry.getValue().getAsNumber());
                                        else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                                            instance.putAdditionalProperty(
                                                    entry.getKey(),
                                                    entry.getValue().getAsBoolean());
                                        else
                                            throw new IllegalArgumentException(
                                                    String.format(
                                                            java.util.Locale.ROOT,
                                                            "The field `%s` has unknown primitive type. Value: %s",
                                                            entry.getKey(),
                                                            entry.getValue().toString()));
                                    } else if (entry.getValue().isJsonArray()) {
                                        instance.putAdditionalProperty(
                                                entry.getKey(),
                                                gson.fromJson(entry.getValue(), List.class));
                                    } else { // JSON object
                                        instance.putAdditionalProperty(
                                                entry.getKey(),
                                                gson.fromJson(entry.getValue(), HashMap.class));
                                    }
                                }
                            }
                            return instance;
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of GroundedExtractionResultResponse given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of GroundedExtractionResultResponse
     * @throws IOException if the JSON string is invalid with respect to GroundedExtractionResultResponse
     */
    public static GroundedExtractionResultResponse fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, GroundedExtractionResultResponse.class);
    }

    /**
     * Convert an instance of GroundedExtractionResultResponse to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
