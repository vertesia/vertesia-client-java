/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260804.124748Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.Arrays;
import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;

/**
 * DSLChildWorkflowStep
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class DSLChildWorkflowStep {
    /**
     * The type fo the step. If not set defaults to \&quot;activity\&quot;
     */
    @JsonAdapter(TypeEnum.Adapter.class)
    public enum TypeEnum {
        WORKFLOW("workflow"),

        UNKNOWN_DEFAULT_OPEN_API("unknown_default_open_api");

        private String value;

        TypeEnum(String value) {
            this.value = value;
        }

        public String getValue() {
            return value;
        }

        @Override
        public String toString() {
            return String.valueOf(value);
        }

        public static TypeEnum fromValue(String value) {
            for (TypeEnum b : TypeEnum.values()) {
                if (b.value.equals(value)) {
                    return b;
                }
            }
            return UNKNOWN_DEFAULT_OPEN_API;
        }

        public static class Adapter extends TypeAdapter<TypeEnum> {
            @Override
            public void write(final JsonWriter jsonWriter, final TypeEnum enumeration)
                    throws IOException {
                jsonWriter.value(enumeration.getValue());
            }

            @Override
            public TypeEnum read(final JsonReader jsonReader) throws IOException {
                String value = jsonReader.nextString();
                return TypeEnum.fromValue(value);
            }
        }

        public static void validateJsonElement(JsonElement jsonElement) throws IOException {
            String value = jsonElement.getAsString();
            TypeEnum.fromValue(value);
        }
    }

    public static final String SERIALIZED_NAME_TYPE = "type";

    @SerializedName(SERIALIZED_NAME_TYPE)
    @jakarta.annotation.Nonnull
    private TypeEnum type;

    public static final String SERIALIZED_NAME_NAME = "name";

    @SerializedName(SERIALIZED_NAME_NAME)
    @jakarta.annotation.Nonnull
    private String name;

    public static final String SERIALIZED_NAME_VARS = "vars";

    @SerializedName(SERIALIZED_NAME_VARS)
    @jakarta.annotation.Nullable private Map<String, Object> vars = new HashMap<>();

    public static final String SERIALIZED_NAME_ASYNC = "async";

    @SerializedName(SERIALIZED_NAME_ASYNC)
    @jakarta.annotation.Nullable private Boolean async;

    public static final String SERIALIZED_NAME_OUTPUT = "output";

    @SerializedName(SERIALIZED_NAME_OUTPUT)
    @jakarta.annotation.Nullable private String output;

    public static final String SERIALIZED_NAME_CONDITION = "condition";

    @SerializedName(SERIALIZED_NAME_CONDITION)
    @jakarta.annotation.Nullable private Map<String, Object> condition = new HashMap<>();

    public static final String SERIALIZED_NAME_SPEC = "spec";

    @SerializedName(SERIALIZED_NAME_SPEC)
    @jakarta.annotation.Nullable private DSLWorkflowSpec spec;

    public static final String SERIALIZED_NAME_OPTIONS = "options";

    @SerializedName(SERIALIZED_NAME_OPTIONS)
    @jakarta.annotation.Nullable private DSLChildWorkflowStepOptions options;

    public DSLChildWorkflowStep() {}

    public DSLChildWorkflowStep type(@jakarta.annotation.Nonnull TypeEnum type) {
        this.type = type;
        return this;
    }

    /**
     * The type fo the step. If not set defaults to \&quot;activity\&quot;
     * @return type
     */
    @jakarta.annotation.Nonnull
    public TypeEnum getType() {
        return type;
    }

    public void setType(@jakarta.annotation.Nonnull TypeEnum type) {
        this.type = type;
    }

    public DSLChildWorkflowStep name(@jakarta.annotation.Nonnull String name) {
        this.name = name;
        return this;
    }

    /**
     * Get name
     * @return name
     */
    @jakarta.annotation.Nonnull
    public String getName() {
        return name;
    }

    public void setName(@jakarta.annotation.Nonnull String name) {
        this.name = name;
    }

    public DSLChildWorkflowStep vars(@jakarta.annotation.Nullable Map<String, Object> vars) {
        this.vars = vars;
        return this;
    }

    public DSLChildWorkflowStep putVarsItem(String key, Object varsItem) {
        if (this.vars == null) {
            this.vars = new HashMap<>();
        }
        this.vars.put(key, varsItem);
        return this;
    }

    /**
     * The parameters to pass to the child workflow. These parameters will be merged over the parent workflow vars and passed altogether to the child workflow.
     * @return vars
     */
    @jakarta.annotation.Nullable public Map<String, Object> getVars() {
        return vars;
    }

    public void setVars(@jakarta.annotation.Nullable Map<String, Object> vars) {
        this.vars = vars;
    }

    public DSLChildWorkflowStep async(@jakarta.annotation.Nullable Boolean async) {
        this.async = async;
        return this;
    }

    /**
     * Get async
     * @return async
     */
    @jakarta.annotation.Nullable public Boolean getAsync() {
        return async;
    }

    public void setAsync(@jakarta.annotation.Nullable Boolean async) {
        this.async = async;
    }

    public DSLChildWorkflowStep output(@jakarta.annotation.Nullable String output) {
        this.output = output;
        return this;
    }

    /**
     * The name of the workflow variable that will store the result of the child workflow (if async the workflow id is stored) If not specified the result will not be stored The parameters describe how the actual parameters will be obtained from the workflow execution vars. since it may contain references to workflow execution vars.
     * @return output
     */
    @jakarta.annotation.Nullable public String getOutput() {
        return output;
    }

    public void setOutput(@jakarta.annotation.Nullable String output) {
        this.output = output;
    }

    public DSLChildWorkflowStep condition(
            @jakarta.annotation.Nullable Map<String, Object> condition) {
        this.condition = condition;
        return this;
    }

    public DSLChildWorkflowStep putConditionItem(String key, Object conditionItem) {
        if (this.condition == null) {
            this.condition = new HashMap<>();
        }
        this.condition.put(key, conditionItem);
        return this;
    }

    /**
     * A JSON expression which evaluates to true or false similar to mongo matches. The child workflow will only execute if the condition is satisfied. Example: {$eq: {wfVarName: value}}
     * @return condition
     */
    @jakarta.annotation.Nullable public Map<String, Object> getCondition() {
        return condition;
    }

    public void setCondition(@jakarta.annotation.Nullable Map<String, Object> condition) {
        this.condition = condition;
    }

    public DSLChildWorkflowStep spec(@jakarta.annotation.Nullable DSLWorkflowSpec spec) {
        this.spec = spec;
        return this;
    }

    /**
     * In case the dslWorkflow is used as a child workflow the spec is used to define the child workflow. If spec is defined then the name must be \&quot;dslWorkflow\&quot;
     * @return spec
     */
    @jakarta.annotation.Nullable public DSLWorkflowSpec getSpec() {
        return spec;
    }

    public void setSpec(@jakarta.annotation.Nullable DSLWorkflowSpec spec) {
        this.spec = spec;
    }

    public DSLChildWorkflowStep options(
            @jakarta.annotation.Nullable DSLChildWorkflowStepOptions options) {
        this.options = options;
        return this;
    }

    /**
     * Get options
     * @return options
     */
    @jakarta.annotation.Nullable public DSLChildWorkflowStepOptions getOptions() {
        return options;
    }

    public void setOptions(@jakarta.annotation.Nullable DSLChildWorkflowStepOptions options) {
        this.options = options;
    }

    /**
     * A container for additional, undeclared properties.
     * This is a holder for any undeclared properties as specified with
     * the 'additionalProperties' keyword in the OAS document.
     */
    private Map<String, Object> additionalProperties;

    /**
     * Set the additional (undeclared) property with the specified name and value.
     * If the property does not already exist, create it otherwise replace it.
     *
     * @param key name of the property
     * @param value value of the property
     * @return the DSLChildWorkflowStep instance itself
     */
    public DSLChildWorkflowStep putAdditionalProperty(String key, Object value) {
        if (this.additionalProperties == null) {
            this.additionalProperties = new HashMap<String, Object>();
        }
        this.additionalProperties.put(key, value);
        return this;
    }

    /**
     * Return the additional (undeclared) property.
     *
     * @return a map of objects
     */
    public Map<String, Object> getAdditionalProperties() {
        return additionalProperties;
    }

    /**
     * Return the additional (undeclared) property with the specified name.
     *
     * @param key name of the property
     * @return an object
     */
    public Object getAdditionalProperty(String key) {
        if (this.additionalProperties == null) {
            return null;
        }
        return this.additionalProperties.get(key);
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        DSLChildWorkflowStep dsLChildWorkflowStep = (DSLChildWorkflowStep) o;
        return Objects.equals(this.type, dsLChildWorkflowStep.type)
                && Objects.equals(this.name, dsLChildWorkflowStep.name)
                && Objects.equals(this.vars, dsLChildWorkflowStep.vars)
                && Objects.equals(this.async, dsLChildWorkflowStep.async)
                && Objects.equals(this.output, dsLChildWorkflowStep.output)
                && Objects.equals(this.condition, dsLChildWorkflowStep.condition)
                && Objects.equals(this.spec, dsLChildWorkflowStep.spec)
                && Objects.equals(this.options, dsLChildWorkflowStep.options)
                && Objects.equals(
                        this.additionalProperties, dsLChildWorkflowStep.additionalProperties);
    }

    @Override
    public int hashCode() {
        return Objects.hash(
                type, name, vars, async, output, condition, spec, options, additionalProperties);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class DSLChildWorkflowStep {\n");
        sb.append("    type: ").append(toIndentedString(type)).append("\n");
        sb.append("    name: ").append(toIndentedString(name)).append("\n");
        sb.append("    vars: ").append(toIndentedString(vars)).append("\n");
        sb.append("    async: ").append(toIndentedString(async)).append("\n");
        sb.append("    output: ").append(toIndentedString(output)).append("\n");
        sb.append("    condition: ").append(toIndentedString(condition)).append("\n");
        sb.append("    spec: ").append(toIndentedString(spec)).append("\n");
        sb.append("    options: ").append(toIndentedString(options)).append("\n");
        sb.append("    additionalProperties: ")
                .append(toIndentedString(additionalProperties))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields =
                new HashSet<String>(
                        Arrays.asList(
                                "type",
                                "name",
                                "vars",
                                "async",
                                "output",
                                "condition",
                                "spec",
                                "options"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(Arrays.asList("type", "name"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to DSLChildWorkflowStep
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!DSLChildWorkflowStep.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in DSLChildWorkflowStep is not found in the empty JSON string",
                                DSLChildWorkflowStep.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : DSLChildWorkflowStep.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (!jsonObj.get("type").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `type` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("type").toString()));
        }
        // validate the required field `type`
        TypeEnum.validateJsonElement(jsonObj.get("type"));
        if (!jsonObj.get("name").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `name` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("name").toString()));
        }
        if ((jsonObj.get("output") != null && !jsonObj.get("output").isJsonNull())
                && !jsonObj.get("output").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `output` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("output").toString()));
        }
        // validate the optional field `spec`
        if (jsonObj.get("spec") != null && !jsonObj.get("spec").isJsonNull()) {
            DSLWorkflowSpec.validateJsonElement(jsonObj.get("spec"));
        }
        // validate the optional field `options`
        if (jsonObj.get("options") != null && !jsonObj.get("options").isJsonNull()) {
            DSLChildWorkflowStepOptions.validateJsonElement(jsonObj.get("options"));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!DSLChildWorkflowStep.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'DSLChildWorkflowStep' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<DSLChildWorkflowStep> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(DSLChildWorkflowStep.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<DSLChildWorkflowStep>() {
                        @Override
                        public void write(JsonWriter out, DSLChildWorkflowStep value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            obj.remove("additionalProperties");
                            // serialize additional properties
                            if (value.getAdditionalProperties() != null) {
                                for (Map.Entry<String, Object> entry :
                                        value.getAdditionalProperties().entrySet()) {
                                    if (entry.getValue() instanceof String)
                                        obj.addProperty(entry.getKey(), (String) entry.getValue());
                                    else if (entry.getValue() instanceof Number)
                                        obj.addProperty(entry.getKey(), (Number) entry.getValue());
                                    else if (entry.getValue() instanceof Boolean)
                                        obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                                    else if (entry.getValue() instanceof Character)
                                        obj.addProperty(
                                                entry.getKey(), (Character) entry.getValue());
                                    else {
                                        JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                                        if (jsonElement.isJsonArray()) {
                                            obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                                        } else {
                                            obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                                        }
                                    }
                                }
                            }
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public DSLChildWorkflowStep read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            JsonObject jsonObj = jsonElement.getAsJsonObject();
                            // store additional fields in the deserialized instance
                            DSLChildWorkflowStep instance = thisAdapter.fromJsonTree(jsonObj);
                            for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
                                if (!openapiFields.contains(entry.getKey())) {
                                    if (entry.getValue().isJsonPrimitive()) { // primitive type
                                        if (entry.getValue().getAsJsonPrimitive().isString())
                                            instance.putAdditionalProperty(
                                                    entry.getKey(), entry.getValue().getAsString());
                                        else if (entry.getValue().getAsJsonPrimitive().isNumber())
                                            instance.putAdditionalProperty(
                                                    entry.getKey(), entry.getValue().getAsNumber());
                                        else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                                            instance.putAdditionalProperty(
                                                    entry.getKey(),
                                                    entry.getValue().getAsBoolean());
                                        else
                                            throw new IllegalArgumentException(
                                                    String.format(
                                                            java.util.Locale.ROOT,
                                                            "The field `%s` has unknown primitive type. Value: %s",
                                                            entry.getKey(),
                                                            entry.getValue().toString()));
                                    } else if (entry.getValue().isJsonArray()) {
                                        instance.putAdditionalProperty(
                                                entry.getKey(),
                                                gson.fromJson(entry.getValue(), List.class));
                                    } else { // JSON object
                                        instance.putAdditionalProperty(
                                                entry.getKey(),
                                                gson.fromJson(entry.getValue(), HashMap.class));
                                    }
                                }
                            }
                            return instance;
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of DSLChildWorkflowStep given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of DSLChildWorkflowStep
     * @throws IOException if the JSON string is invalid with respect to DSLChildWorkflowStep
     */
    public static DSLChildWorkflowStep fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, DSLChildWorkflowStep.class);
    }

    /**
     * Convert an instance of DSLChildWorkflowStep to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
