/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.6.0-dev.20260923.085325Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.Arrays;
import java.util.HashSet;
import java.util.Objects;

/**
 * OpenAiTranscriptionOptions
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class OpenAiTranscriptionOptions {
    /**
     * Gets or Sets optionId
     */
    @JsonAdapter(OptionIdEnum.Adapter.class)
    public enum OptionIdEnum {
        OPENAI_TRANSCRIPTION("openai-transcription"),

        UNKNOWN_DEFAULT_OPEN_API("unknown_default_open_api");

        private String value;

        OptionIdEnum(String value) {
            this.value = value;
        }

        public String getValue() {
            return value;
        }

        @Override
        public String toString() {
            return String.valueOf(value);
        }

        public static OptionIdEnum fromValue(String value) {
            for (OptionIdEnum b : OptionIdEnum.values()) {
                if (b.value.equals(value)) {
                    return b;
                }
            }
            return UNKNOWN_DEFAULT_OPEN_API;
        }

        public static class Adapter extends TypeAdapter<OptionIdEnum> {
            @Override
            public void write(final JsonWriter jsonWriter, final OptionIdEnum enumeration)
                    throws IOException {
                jsonWriter.value(enumeration.getValue());
            }

            @Override
            public OptionIdEnum read(final JsonReader jsonReader) throws IOException {
                String value = jsonReader.nextString();
                return OptionIdEnum.fromValue(value);
            }
        }

        public static void validateJsonElement(JsonElement jsonElement) throws IOException {
            String value = jsonElement.getAsString();
            OptionIdEnum.fromValue(value);
        }
    }

    public static final String SERIALIZED_NAME_OPTION_ID = "_option_id";

    @SerializedName(SERIALIZED_NAME_OPTION_ID)
    @jakarta.annotation.Nullable private OptionIdEnum optionId;

    public static final String SERIALIZED_NAME_LANGUAGE = "language";

    @SerializedName(SERIALIZED_NAME_LANGUAGE)
    @jakarta.annotation.Nullable private String language;

    public OpenAiTranscriptionOptions() {}

    public OpenAiTranscriptionOptions optionId(@jakarta.annotation.Nullable OptionIdEnum optionId) {
        this.optionId = optionId;
        return this;
    }

    /**
     * Get optionId
     * @return optionId
     */
    @jakarta.annotation.Nullable public OptionIdEnum getOptionId() {
        return optionId;
    }

    public void setOptionId(@jakarta.annotation.Nullable OptionIdEnum optionId) {
        this.optionId = optionId;
    }

    public OpenAiTranscriptionOptions language(@jakarta.annotation.Nullable String language) {
        this.language = language;
        return this;
    }

    /**
     * Get language
     * @return language
     */
    @jakarta.annotation.Nullable public String getLanguage() {
        return language;
    }

    public void setLanguage(@jakarta.annotation.Nullable String language) {
        this.language = language;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        OpenAiTranscriptionOptions openAiTranscriptionOptions = (OpenAiTranscriptionOptions) o;
        return Objects.equals(this.optionId, openAiTranscriptionOptions.optionId)
                && Objects.equals(this.language, openAiTranscriptionOptions.language);
    }

    @Override
    public int hashCode() {
        return Objects.hash(optionId, language);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class OpenAiTranscriptionOptions {\n");
        sb.append("    optionId: ").append(toIndentedString(optionId)).append("\n");
        sb.append("    language: ").append(toIndentedString(language)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>(Arrays.asList("_option_id", "language"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(0);
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to OpenAiTranscriptionOptions
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!OpenAiTranscriptionOptions.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in OpenAiTranscriptionOptions is not found in the empty JSON string",
                                OpenAiTranscriptionOptions.openapiRequiredFields.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if ((jsonObj.get("_option_id") != null && !jsonObj.get("_option_id").isJsonNull())
                && !jsonObj.get("_option_id").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `_option_id` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("_option_id").toString()));
        }
        // validate the optional field `_option_id`
        if (jsonObj.get("_option_id") != null && !jsonObj.get("_option_id").isJsonNull()) {
            OptionIdEnum.validateJsonElement(jsonObj.get("_option_id"));
        }
        if ((jsonObj.get("language") != null && !jsonObj.get("language").isJsonNull())
                && !jsonObj.get("language").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `language` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("language").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!OpenAiTranscriptionOptions.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'OpenAiTranscriptionOptions' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<OpenAiTranscriptionOptions> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(OpenAiTranscriptionOptions.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<OpenAiTranscriptionOptions>() {
                        @Override
                        public void write(JsonWriter out, OpenAiTranscriptionOptions value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public OpenAiTranscriptionOptions read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of OpenAiTranscriptionOptions given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of OpenAiTranscriptionOptions
     * @throws IOException if the JSON string is invalid with respect to OpenAiTranscriptionOptions
     */
    public static OpenAiTranscriptionOptions fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, OpenAiTranscriptionOptions.class);
    }

    /**
     * Convert an instance of OpenAiTranscriptionOptions to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
