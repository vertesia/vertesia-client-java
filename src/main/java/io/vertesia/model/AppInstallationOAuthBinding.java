/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260804.124748Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.Arrays;
import java.util.HashSet;
import java.util.Objects;

/**
 * Binding between an MCP collection and an OAuth provider created at install time. Stored on AppInstallation so the runtime can look up the correct OAuth provider by ID, independent of manifest oauth_provider references (which may change).
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class AppInstallationOAuthBinding {
    public static final String SERIALIZED_NAME_COLLECTION_ID = "collection_id";

    @SerializedName(SERIALIZED_NAME_COLLECTION_ID)
    @jakarta.annotation.Nonnull
    private String collectionId;

    public static final String SERIALIZED_NAME_OAUTH_PROVIDER_ID = "oauth_provider_id";

    @SerializedName(SERIALIZED_NAME_OAUTH_PROVIDER_ID)
    @jakarta.annotation.Nonnull
    private String oauthProviderId;

    public static final String SERIALIZED_NAME_OAUTH_PROVIDER_NAME = "oauth_provider_name";

    @SerializedName(SERIALIZED_NAME_OAUTH_PROVIDER_NAME)
    @jakarta.annotation.Nonnull
    private String oauthProviderName;

    public AppInstallationOAuthBinding() {}

    public AppInstallationOAuthBinding collectionId(
            @jakarta.annotation.Nonnull String collectionId) {
        this.collectionId = collectionId;
        return this;
    }

    /**
     * Stable collection identifier: MCPToolCollectionObject.id for new manifests. Legacy installations may still contain a name-based fallback value.
     * @return collectionId
     */
    @jakarta.annotation.Nonnull
    public String getCollectionId() {
        return collectionId;
    }

    public void setCollectionId(@jakarta.annotation.Nonnull String collectionId) {
        this.collectionId = collectionId;
    }

    public AppInstallationOAuthBinding oauthProviderId(
            @jakarta.annotation.Nonnull String oauthProviderId) {
        this.oauthProviderId = oauthProviderId;
        return this;
    }

    /**
     * MongoDB ObjectId of the OAuth provider in this project. Used for ID-based lookups (rename-proof).
     * @return oauthProviderId
     */
    @jakarta.annotation.Nonnull
    public String getOauthProviderId() {
        return oauthProviderId;
    }

    public void setOauthProviderId(@jakarta.annotation.Nonnull String oauthProviderId) {
        this.oauthProviderId = oauthProviderId;
    }

    public AppInstallationOAuthBinding oauthProviderName(
            @jakarta.annotation.Nonnull String oauthProviderName) {
        this.oauthProviderName = oauthProviderName;
        return this;
    }

    /**
     * Name of the OAuth provider at creation time. Used by the workflow token path (getMCPClient → remoteMcpConnections.getToken) which looks up by name.
     * @return oauthProviderName
     */
    @jakarta.annotation.Nonnull
    public String getOauthProviderName() {
        return oauthProviderName;
    }

    public void setOauthProviderName(@jakarta.annotation.Nonnull String oauthProviderName) {
        this.oauthProviderName = oauthProviderName;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        AppInstallationOAuthBinding appInstallationOAuthBinding = (AppInstallationOAuthBinding) o;
        return Objects.equals(this.collectionId, appInstallationOAuthBinding.collectionId)
                && Objects.equals(this.oauthProviderId, appInstallationOAuthBinding.oauthProviderId)
                && Objects.equals(
                        this.oauthProviderName, appInstallationOAuthBinding.oauthProviderName);
    }

    @Override
    public int hashCode() {
        return Objects.hash(collectionId, oauthProviderId, oauthProviderName);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class AppInstallationOAuthBinding {\n");
        sb.append("    collectionId: ").append(toIndentedString(collectionId)).append("\n");
        sb.append("    oauthProviderId: ").append(toIndentedString(oauthProviderId)).append("\n");
        sb.append("    oauthProviderName: ")
                .append(toIndentedString(oauthProviderName))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields =
                new HashSet<String>(
                        Arrays.asList("collection_id", "oauth_provider_id", "oauth_provider_name"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields =
                new HashSet<String>(
                        Arrays.asList("collection_id", "oauth_provider_id", "oauth_provider_name"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to AppInstallationOAuthBinding
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!AppInstallationOAuthBinding.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in AppInstallationOAuthBinding is not found in the empty JSON string",
                                AppInstallationOAuthBinding.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : AppInstallationOAuthBinding.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (!jsonObj.get("collection_id").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `collection_id` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("collection_id").toString()));
        }
        if (!jsonObj.get("oauth_provider_id").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `oauth_provider_id` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("oauth_provider_id").toString()));
        }
        if (!jsonObj.get("oauth_provider_name").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `oauth_provider_name` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("oauth_provider_name").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!AppInstallationOAuthBinding.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'AppInstallationOAuthBinding' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<AppInstallationOAuthBinding> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(AppInstallationOAuthBinding.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<AppInstallationOAuthBinding>() {
                        @Override
                        public void write(JsonWriter out, AppInstallationOAuthBinding value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public AppInstallationOAuthBinding read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of AppInstallationOAuthBinding given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of AppInstallationOAuthBinding
     * @throws IOException if the JSON string is invalid with respect to AppInstallationOAuthBinding
     */
    public static AppInstallationOAuthBinding fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, AppInstallationOAuthBinding.class);
    }

    /**
     * Convert an instance of AppInstallationOAuthBinding to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
