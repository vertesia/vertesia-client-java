/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260807.073259Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.JsonElement;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

/**
 * Origin of a tool in the unified project-scoped tool registry.  - &#x60;builtin&#x60;: anything statically registered on the worker — workflow builtins, studio agent tools, and sys skills - &#x60;app&#x60;: tools contributed by an installed app for the project - &#x60;interaction&#x60;: a project interaction exposed via &#x60;tag&#x3D;tool&#x60; / &#x60;is_tool&#x3D;true&#x60; / &#x60;is_skill&#x3D;true&#x60;  Skill semantics (the tool unlocks others when invoked) are NOT part of the source. They are identified universally by the &#x60;learn_&#x60; prefix in the tool name. This matches how the runtime registry stores them and avoids splitting one logical origin into two sources.
 */
@JsonAdapter(ToolSource.Adapter.class)
public enum ToolSource {
    BUILTIN("builtin"),

    APP("app"),

    INTERACTION("interaction"),

    UNKNOWN_DEFAULT_OPEN_API("unknown_default_open_api");

    private String value;

    ToolSource(String value) {
        this.value = value;
    }

    public String getValue() {
        return value;
    }

    @Override
    public String toString() {
        return String.valueOf(value);
    }

    public static ToolSource fromValue(String value) {
        for (ToolSource b : ToolSource.values()) {
            if (b.value.equals(value)) {
                return b;
            }
        }
        return UNKNOWN_DEFAULT_OPEN_API;
    }

    public static class Adapter extends TypeAdapter<ToolSource> {
        @Override
        public void write(final JsonWriter jsonWriter, final ToolSource enumeration)
                throws IOException {
            jsonWriter.value(enumeration.getValue());
        }

        @Override
        public ToolSource read(final JsonReader jsonReader) throws IOException {
            String value = jsonReader.nextString();
            return ToolSource.fromValue(value);
        }
    }

    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        String value = jsonElement.getAsString();
        ToolSource.fromValue(value);
    }
}
