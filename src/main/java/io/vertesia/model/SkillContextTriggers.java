/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashSet;
import java.util.List;
import java.util.Objects;

/**
 * Context triggers for auto-injection of skills. When these conditions match, the skill is automatically injected into the agent context.
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class SkillContextTriggers {
    public static final String SERIALIZED_NAME_KEYWORDS = "keywords";

    @SerializedName(SERIALIZED_NAME_KEYWORDS)
    @jakarta.annotation.Nullable private List<String> keywords = new ArrayList<>();

    public static final String SERIALIZED_NAME_TOOL_NAMES = "tool_names";

    @SerializedName(SERIALIZED_NAME_TOOL_NAMES)
    @jakarta.annotation.Nullable private List<String> toolNames = new ArrayList<>();

    public static final String SERIALIZED_NAME_DATA_PATTERNS = "data_patterns";

    @SerializedName(SERIALIZED_NAME_DATA_PATTERNS)
    @jakarta.annotation.Nullable private List<String> dataPatterns = new ArrayList<>();

    public SkillContextTriggers() {}

    public SkillContextTriggers keywords(@jakarta.annotation.Nullable List<String> keywords) {
        this.keywords = keywords;
        return this;
    }

    public SkillContextTriggers addKeywordsItem(String keywordsItem) {
        if (this.keywords == null) {
            this.keywords = new ArrayList<>();
        }
        this.keywords.add(keywordsItem);
        return this;
    }

    /**
     * Keywords in user input that should trigger this skill
     * @return keywords
     */
    @jakarta.annotation.Nullable public List<String> getKeywords() {
        return keywords;
    }

    public void setKeywords(@jakarta.annotation.Nullable List<String> keywords) {
        this.keywords = keywords;
    }

    public SkillContextTriggers toolNames(@jakarta.annotation.Nullable List<String> toolNames) {
        this.toolNames = toolNames;
        return this;
    }

    public SkillContextTriggers addToolNamesItem(String toolNamesItem) {
        if (this.toolNames == null) {
            this.toolNames = new ArrayList<>();
        }
        this.toolNames.add(toolNamesItem);
        return this;
    }

    /**
     * If these tools are being used, suggest this skill
     * @return toolNames
     */
    @jakarta.annotation.Nullable public List<String> getToolNames() {
        return toolNames;
    }

    public void setToolNames(@jakarta.annotation.Nullable List<String> toolNames) {
        this.toolNames = toolNames;
    }

    public SkillContextTriggers dataPatterns(
            @jakarta.annotation.Nullable List<String> dataPatterns) {
        this.dataPatterns = dataPatterns;
        return this;
    }

    public SkillContextTriggers addDataPatternsItem(String dataPatternsItem) {
        if (this.dataPatterns == null) {
            this.dataPatterns = new ArrayList<>();
        }
        this.dataPatterns.add(dataPatternsItem);
        return this;
    }

    /**
     * Regex patterns to match against input data
     * @return dataPatterns
     */
    @jakarta.annotation.Nullable public List<String> getDataPatterns() {
        return dataPatterns;
    }

    public void setDataPatterns(@jakarta.annotation.Nullable List<String> dataPatterns) {
        this.dataPatterns = dataPatterns;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        SkillContextTriggers skillContextTriggers = (SkillContextTriggers) o;
        return Objects.equals(this.keywords, skillContextTriggers.keywords)
                && Objects.equals(this.toolNames, skillContextTriggers.toolNames)
                && Objects.equals(this.dataPatterns, skillContextTriggers.dataPatterns);
    }

    @Override
    public int hashCode() {
        return Objects.hash(keywords, toolNames, dataPatterns);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class SkillContextTriggers {\n");
        sb.append("    keywords: ").append(toIndentedString(keywords)).append("\n");
        sb.append("    toolNames: ").append(toIndentedString(toolNames)).append("\n");
        sb.append("    dataPatterns: ").append(toIndentedString(dataPatterns)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields =
                new HashSet<String>(Arrays.asList("keywords", "tool_names", "data_patterns"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(0);
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to SkillContextTriggers
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!SkillContextTriggers.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in SkillContextTriggers is not found in the empty JSON string",
                                SkillContextTriggers.openapiRequiredFields.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // ensure the optional json data is an array if present
        if (jsonObj.get("keywords") != null
                && !jsonObj.get("keywords").isJsonNull()
                && !jsonObj.get("keywords").isJsonArray()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `keywords` to be an array in the JSON string but got `%s`",
                            jsonObj.get("keywords").toString()));
        }
        // ensure the optional json data is an array if present
        if (jsonObj.get("tool_names") != null
                && !jsonObj.get("tool_names").isJsonNull()
                && !jsonObj.get("tool_names").isJsonArray()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `tool_names` to be an array in the JSON string but got `%s`",
                            jsonObj.get("tool_names").toString()));
        }
        // ensure the optional json data is an array if present
        if (jsonObj.get("data_patterns") != null
                && !jsonObj.get("data_patterns").isJsonNull()
                && !jsonObj.get("data_patterns").isJsonArray()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `data_patterns` to be an array in the JSON string but got `%s`",
                            jsonObj.get("data_patterns").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!SkillContextTriggers.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'SkillContextTriggers' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<SkillContextTriggers> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(SkillContextTriggers.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<SkillContextTriggers>() {
                        @Override
                        public void write(JsonWriter out, SkillContextTriggers value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public SkillContextTriggers read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of SkillContextTriggers given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of SkillContextTriggers
     * @throws IOException if the JSON string is invalid with respect to SkillContextTriggers
     */
    public static SkillContextTriggers fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, SkillContextTriggers.class);
    }

    /**
     * Convert an instance of SkillContextTriggers to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
