/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260725.083715Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashSet;
import java.util.List;
import java.util.Objects;

/**
 * ListEventDeliveriesResponse
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class ListEventDeliveriesResponse {
    public static final String SERIALIZED_NAME_DELIVERIES = "deliveries";

    @SerializedName(SERIALIZED_NAME_DELIVERIES)
    @jakarta.annotation.Nonnull
    private List<EventDeliverySummary> deliveries = new ArrayList<>();

    public ListEventDeliveriesResponse() {}

    public ListEventDeliveriesResponse deliveries(
            @jakarta.annotation.Nonnull List<EventDeliverySummary> deliveries) {
        this.deliveries = deliveries;
        return this;
    }

    public ListEventDeliveriesResponse addDeliveriesItem(EventDeliverySummary deliveriesItem) {
        if (this.deliveries == null) {
            this.deliveries = new ArrayList<>();
        }
        this.deliveries.add(deliveriesItem);
        return this;
    }

    /**
     * Get deliveries
     * @return deliveries
     */
    @jakarta.annotation.Nonnull
    public List<EventDeliverySummary> getDeliveries() {
        return deliveries;
    }

    public void setDeliveries(@jakarta.annotation.Nonnull List<EventDeliverySummary> deliveries) {
        this.deliveries = deliveries;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ListEventDeliveriesResponse listEventDeliveriesResponse = (ListEventDeliveriesResponse) o;
        return Objects.equals(this.deliveries, listEventDeliveriesResponse.deliveries);
    }

    @Override
    public int hashCode() {
        return Objects.hash(deliveries);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class ListEventDeliveriesResponse {\n");
        sb.append("    deliveries: ").append(toIndentedString(deliveries)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>(Arrays.asList("deliveries"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(Arrays.asList("deliveries"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to ListEventDeliveriesResponse
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!ListEventDeliveriesResponse.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in ListEventDeliveriesResponse is not found in the empty JSON string",
                                ListEventDeliveriesResponse.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : ListEventDeliveriesResponse.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (jsonObj.get("deliveries") != null) {
            if (!jsonObj.get("deliveries").isJsonArray()) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "Expected the field `deliveries` to be an array in the JSON string but got `%s`",
                                jsonObj.get("deliveries").toString()));
            }
            JsonArray jsonArraydeliveries = jsonObj.getAsJsonArray("deliveries");
            // validate the required field `deliveries` (array)
            for (int i = 0; i < jsonArraydeliveries.size(); i++) {
                EventDeliverySummary.validateJsonElement(jsonArraydeliveries.get(i));
            }
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ListEventDeliveriesResponse.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'ListEventDeliveriesResponse' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ListEventDeliveriesResponse> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(ListEventDeliveriesResponse.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<ListEventDeliveriesResponse>() {
                        @Override
                        public void write(JsonWriter out, ListEventDeliveriesResponse value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public ListEventDeliveriesResponse read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of ListEventDeliveriesResponse given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of ListEventDeliveriesResponse
     * @throws IOException if the JSON string is invalid with respect to ListEventDeliveriesResponse
     */
    public static ListEventDeliveriesResponse fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, ListEventDeliveriesResponse.class);
    }

    /**
     * Convert an instance of ListEventDeliveriesResponse to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
