/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260901.052906Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.math.BigDecimal;
import java.util.Arrays;
import java.util.HashSet;
import java.util.Objects;

/**
 * AgentCheckpointConfiguration
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class AgentCheckpointConfiguration {
    public static final String SERIALIZED_NAME_CONTEXT_THRESHOLD = "context_threshold";

    @SerializedName(SERIALIZED_NAME_CONTEXT_THRESHOLD)
    @jakarta.annotation.Nullable private BigDecimal contextThreshold;

    public static final String SERIALIZED_NAME_MAX_TOKENS = "max_tokens";

    @SerializedName(SERIALIZED_NAME_MAX_TOKENS)
    @jakarta.annotation.Nullable private BigDecimal maxTokens;

    public AgentCheckpointConfiguration() {}

    public AgentCheckpointConfiguration contextThreshold(
            @jakarta.annotation.Nullable BigDecimal contextThreshold) {
        this.contextThreshold = contextThreshold;
        return this;
    }

    /**
     * Fraction of the model&#39;s context window to use before the conversation is summarized and compacted (0-1, e.g. 0.95). Model-independent: applies to whatever model each run uses. Setting it replaces the default hard cap — a project asking for 0.95 of a 1M-window model checkpoints at 950k. Clamped at runtime to 0.95 so the prompt still fits.
     * @return contextThreshold
     */
    @jakarta.annotation.Nullable public BigDecimal getContextThreshold() {
        return contextThreshold;
    }

    public void setContextThreshold(@jakarta.annotation.Nullable BigDecimal contextThreshold) {
        this.contextThreshold = contextThreshold;
    }

    public AgentCheckpointConfiguration maxTokens(
            @jakarta.annotation.Nullable BigDecimal maxTokens) {
        this.maxTokens = maxTokens;
        return this;
    }

    /**
     * Absolute hard cap in tokens, regardless of the window fraction. Alone it acts as the threshold; combined with context_threshold the lower of the two wins. Clamped at runtime to 95% of the model&#39;s window. Unset means the default cap (500k), or no cap beyond the 95% clamp when context_threshold is set.
     * @return maxTokens
     */
    @jakarta.annotation.Nullable public BigDecimal getMaxTokens() {
        return maxTokens;
    }

    public void setMaxTokens(@jakarta.annotation.Nullable BigDecimal maxTokens) {
        this.maxTokens = maxTokens;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        AgentCheckpointConfiguration agentCheckpointConfiguration =
                (AgentCheckpointConfiguration) o;
        return Objects.equals(this.contextThreshold, agentCheckpointConfiguration.contextThreshold)
                && Objects.equals(this.maxTokens, agentCheckpointConfiguration.maxTokens);
    }

    @Override
    public int hashCode() {
        return Objects.hash(contextThreshold, maxTokens);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class AgentCheckpointConfiguration {\n");
        sb.append("    contextThreshold: ").append(toIndentedString(contextThreshold)).append("\n");
        sb.append("    maxTokens: ").append(toIndentedString(maxTokens)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>(Arrays.asList("context_threshold", "max_tokens"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(0);
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to AgentCheckpointConfiguration
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!AgentCheckpointConfiguration.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in AgentCheckpointConfiguration is not found in the empty JSON string",
                                AgentCheckpointConfiguration.openapiRequiredFields.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!AgentCheckpointConfiguration.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'AgentCheckpointConfiguration' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<AgentCheckpointConfiguration> thisAdapter =
                    gson.getDelegateAdapter(
                            this, TypeToken.get(AgentCheckpointConfiguration.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<AgentCheckpointConfiguration>() {
                        @Override
                        public void write(JsonWriter out, AgentCheckpointConfiguration value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public AgentCheckpointConfiguration read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of AgentCheckpointConfiguration given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of AgentCheckpointConfiguration
     * @throws IOException if the JSON string is invalid with respect to AgentCheckpointConfiguration
     */
    public static AgentCheckpointConfiguration fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, AgentCheckpointConfiguration.class);
    }

    /**
     * Convert an instance of AgentCheckpointConfiguration to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
