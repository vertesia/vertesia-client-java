/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260725.083715Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.Arrays;
import java.util.HashSet;
import java.util.Objects;

/**
 * ViewExperienceLayout
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class ViewExperienceLayout {
    /**
     * Gets or Sets mode
     */
    @JsonAdapter(ModeEnum.Adapter.class)
    public enum ModeEnum {
        BROWSE("browse"),

        WORKLIST("worklist"),

        UNKNOWN_DEFAULT_OPEN_API("unknown_default_open_api");

        private String value;

        ModeEnum(String value) {
            this.value = value;
        }

        public String getValue() {
            return value;
        }

        @Override
        public String toString() {
            return String.valueOf(value);
        }

        public static ModeEnum fromValue(String value) {
            for (ModeEnum b : ModeEnum.values()) {
                if (b.value.equals(value)) {
                    return b;
                }
            }
            return UNKNOWN_DEFAULT_OPEN_API;
        }

        public static class Adapter extends TypeAdapter<ModeEnum> {
            @Override
            public void write(final JsonWriter jsonWriter, final ModeEnum enumeration)
                    throws IOException {
                jsonWriter.value(enumeration.getValue());
            }

            @Override
            public ModeEnum read(final JsonReader jsonReader) throws IOException {
                String value = jsonReader.nextString();
                return ModeEnum.fromValue(value);
            }
        }

        public static void validateJsonElement(JsonElement jsonElement) throws IOException {
            String value = jsonElement.getAsString();
            ModeEnum.fromValue(value);
        }
    }

    public static final String SERIALIZED_NAME_MODE = "mode";

    @SerializedName(SERIALIZED_NAME_MODE)
    @jakarta.annotation.Nullable private ModeEnum mode;

    /**
     * Gets or Sets navigationPosition
     */
    @JsonAdapter(NavigationPositionEnum.Adapter.class)
    public enum NavigationPositionEnum {
        SIDEBAR("sidebar"),

        TOP("top"),

        UNKNOWN_DEFAULT_OPEN_API("unknown_default_open_api");

        private String value;

        NavigationPositionEnum(String value) {
            this.value = value;
        }

        public String getValue() {
            return value;
        }

        @Override
        public String toString() {
            return String.valueOf(value);
        }

        public static NavigationPositionEnum fromValue(String value) {
            for (NavigationPositionEnum b : NavigationPositionEnum.values()) {
                if (b.value.equals(value)) {
                    return b;
                }
            }
            return UNKNOWN_DEFAULT_OPEN_API;
        }

        public static class Adapter extends TypeAdapter<NavigationPositionEnum> {
            @Override
            public void write(final JsonWriter jsonWriter, final NavigationPositionEnum enumeration)
                    throws IOException {
                jsonWriter.value(enumeration.getValue());
            }

            @Override
            public NavigationPositionEnum read(final JsonReader jsonReader) throws IOException {
                String value = jsonReader.nextString();
                return NavigationPositionEnum.fromValue(value);
            }
        }

        public static void validateJsonElement(JsonElement jsonElement) throws IOException {
            String value = jsonElement.getAsString();
            NavigationPositionEnum.fromValue(value);
        }
    }

    public static final String SERIALIZED_NAME_NAVIGATION_POSITION = "navigation_position";

    @SerializedName(SERIALIZED_NAME_NAVIGATION_POSITION)
    @jakarta.annotation.Nullable private NavigationPositionEnum navigationPosition;

    public ViewExperienceLayout() {}

    public ViewExperienceLayout mode(@jakarta.annotation.Nullable ModeEnum mode) {
        this.mode = mode;
        return this;
    }

    /**
     * Get mode
     * @return mode
     */
    @jakarta.annotation.Nullable public ModeEnum getMode() {
        return mode;
    }

    public void setMode(@jakarta.annotation.Nullable ModeEnum mode) {
        this.mode = mode;
    }

    public ViewExperienceLayout navigationPosition(
            @jakarta.annotation.Nullable NavigationPositionEnum navigationPosition) {
        this.navigationPosition = navigationPosition;
        return this;
    }

    /**
     * Get navigationPosition
     * @return navigationPosition
     */
    @jakarta.annotation.Nullable public NavigationPositionEnum getNavigationPosition() {
        return navigationPosition;
    }

    public void setNavigationPosition(
            @jakarta.annotation.Nullable NavigationPositionEnum navigationPosition) {
        this.navigationPosition = navigationPosition;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ViewExperienceLayout viewExperienceLayout = (ViewExperienceLayout) o;
        return Objects.equals(this.mode, viewExperienceLayout.mode)
                && Objects.equals(this.navigationPosition, viewExperienceLayout.navigationPosition);
    }

    @Override
    public int hashCode() {
        return Objects.hash(mode, navigationPosition);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class ViewExperienceLayout {\n");
        sb.append("    mode: ").append(toIndentedString(mode)).append("\n");
        sb.append("    navigationPosition: ")
                .append(toIndentedString(navigationPosition))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>(Arrays.asList("mode", "navigation_position"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(0);
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to ViewExperienceLayout
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!ViewExperienceLayout.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in ViewExperienceLayout is not found in the empty JSON string",
                                ViewExperienceLayout.openapiRequiredFields.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if ((jsonObj.get("mode") != null && !jsonObj.get("mode").isJsonNull())
                && !jsonObj.get("mode").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `mode` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("mode").toString()));
        }
        // validate the optional field `mode`
        if (jsonObj.get("mode") != null && !jsonObj.get("mode").isJsonNull()) {
            ModeEnum.validateJsonElement(jsonObj.get("mode"));
        }
        if ((jsonObj.get("navigation_position") != null
                        && !jsonObj.get("navigation_position").isJsonNull())
                && !jsonObj.get("navigation_position").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `navigation_position` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("navigation_position").toString()));
        }
        // validate the optional field `navigation_position`
        if (jsonObj.get("navigation_position") != null
                && !jsonObj.get("navigation_position").isJsonNull()) {
            NavigationPositionEnum.validateJsonElement(jsonObj.get("navigation_position"));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ViewExperienceLayout.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'ViewExperienceLayout' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ViewExperienceLayout> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(ViewExperienceLayout.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<ViewExperienceLayout>() {
                        @Override
                        public void write(JsonWriter out, ViewExperienceLayout value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public ViewExperienceLayout read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of ViewExperienceLayout given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of ViewExperienceLayout
     * @throws IOException if the JSON string is invalid with respect to ViewExperienceLayout
     */
    public static ViewExperienceLayout fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, ViewExperienceLayout.class);
    }

    /**
     * Convert an instance of ViewExperienceLayout to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
