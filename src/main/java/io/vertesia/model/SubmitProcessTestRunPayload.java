/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.6.0-dev
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashSet;
import java.util.List;
import java.util.Objects;

/**
 * SubmitProcessTestRunPayload
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class SubmitProcessTestRunPayload {
    public static final String SERIALIZED_NAME_PROCESS = "process";

    @SerializedName(SERIALIZED_NAME_PROCESS)
    @jakarta.annotation.Nonnull
    private ProcessTestTarget process;

    public static final String SERIALIZED_NAME_SCENARIOS = "scenarios";

    @SerializedName(SERIALIZED_NAME_SCENARIOS)
    @jakarta.annotation.Nonnull
    private List<ProcessTestScenario> scenarios = new ArrayList<>();

    public SubmitProcessTestRunPayload() {}

    public SubmitProcessTestRunPayload process(
            @jakarta.annotation.Nonnull ProcessTestTarget process) {
        this.process = process;
        return this;
    }

    /**
     * Get process
     * @return process
     */
    @jakarta.annotation.Nonnull
    public ProcessTestTarget getProcess() {
        return process;
    }

    public void setProcess(@jakarta.annotation.Nonnull ProcessTestTarget process) {
        this.process = process;
    }

    public SubmitProcessTestRunPayload scenarios(
            @jakarta.annotation.Nonnull List<ProcessTestScenario> scenarios) {
        this.scenarios = scenarios;
        return this;
    }

    public SubmitProcessTestRunPayload addScenariosItem(ProcessTestScenario scenariosItem) {
        if (this.scenarios == null) {
            this.scenarios = new ArrayList<>();
        }
        this.scenarios.add(scenariosItem);
        return this;
    }

    /**
     * Get scenarios
     * @return scenarios
     */
    @jakarta.annotation.Nonnull
    public List<ProcessTestScenario> getScenarios() {
        return scenarios;
    }

    public void setScenarios(@jakarta.annotation.Nonnull List<ProcessTestScenario> scenarios) {
        this.scenarios = scenarios;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        SubmitProcessTestRunPayload submitProcessTestRunPayload = (SubmitProcessTestRunPayload) o;
        return Objects.equals(this.process, submitProcessTestRunPayload.process)
                && Objects.equals(this.scenarios, submitProcessTestRunPayload.scenarios);
    }

    @Override
    public int hashCode() {
        return Objects.hash(process, scenarios);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class SubmitProcessTestRunPayload {\n");
        sb.append("    process: ").append(toIndentedString(process)).append("\n");
        sb.append("    scenarios: ").append(toIndentedString(scenarios)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>(Arrays.asList("process", "scenarios"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(Arrays.asList("process", "scenarios"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to SubmitProcessTestRunPayload
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!SubmitProcessTestRunPayload.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in SubmitProcessTestRunPayload is not found in the empty JSON string",
                                SubmitProcessTestRunPayload.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : SubmitProcessTestRunPayload.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // validate the required field `process`
        ProcessTestTarget.validateJsonElement(jsonObj.get("process"));
        if (jsonObj.get("scenarios") != null) {
            if (!jsonObj.get("scenarios").isJsonArray()) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "Expected the field `scenarios` to be an array in the JSON string but got `%s`",
                                jsonObj.get("scenarios").toString()));
            }
            JsonArray jsonArrayscenarios = jsonObj.getAsJsonArray("scenarios");
            // validate the required field `scenarios` (array)
            for (int i = 0; i < jsonArrayscenarios.size(); i++) {
                ProcessTestScenario.validateJsonElement(jsonArrayscenarios.get(i));
            }
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!SubmitProcessTestRunPayload.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'SubmitProcessTestRunPayload' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<SubmitProcessTestRunPayload> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(SubmitProcessTestRunPayload.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<SubmitProcessTestRunPayload>() {
                        @Override
                        public void write(JsonWriter out, SubmitProcessTestRunPayload value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public SubmitProcessTestRunPayload read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of SubmitProcessTestRunPayload given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of SubmitProcessTestRunPayload
     * @throws IOException if the JSON string is invalid with respect to SubmitProcessTestRunPayload
     */
    public static SubmitProcessTestRunPayload fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, SubmitProcessTestRunPayload.class);
    }

    /**
     * Convert an instance of SubmitProcessTestRunPayload to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
