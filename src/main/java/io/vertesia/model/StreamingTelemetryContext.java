/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.6.0-dev
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.math.BigDecimal;
import java.util.Arrays;
import java.util.HashSet;
import java.util.Objects;

/**
 * Telemetry context for streaming mode. Contains info not available in current_state needed to send LlmCallEvent.
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class StreamingTelemetryContext {
    public static final String SERIALIZED_NAME_CALL_TYPE = "callType";

    @SerializedName(SERIALIZED_NAME_CALL_TYPE)
    @jakarta.annotation.Nonnull
    private LlmCallType callType;

    public static final String SERIALIZED_NAME_ATTEMPT_NUMBER = "attemptNumber";

    @SerializedName(SERIALIZED_NAME_ATTEMPT_NUMBER)
    @jakarta.annotation.Nullable private BigDecimal attemptNumber;

    public static final String SERIALIZED_NAME_INFERENCE_START_TIME = "inferenceStartTime";

    @SerializedName(SERIALIZED_NAME_INFERENCE_START_TIME)
    @jakarta.annotation.Nonnull
    private BigDecimal inferenceStartTime;

    public StreamingTelemetryContext() {}

    public StreamingTelemetryContext callType(@jakarta.annotation.Nonnull LlmCallType callType) {
        this.callType = callType;
        return this;
    }

    /**
     * Type of LLM call: start, resume after user message, or resume after tool results
     * @return callType
     */
    @jakarta.annotation.Nonnull
    public LlmCallType getCallType() {
        return callType;
    }

    public void setCallType(@jakarta.annotation.Nonnull LlmCallType callType) {
        this.callType = callType;
    }

    public StreamingTelemetryContext attemptNumber(
            @jakarta.annotation.Nullable BigDecimal attemptNumber) {
        this.attemptNumber = attemptNumber;
        return this;
    }

    /**
     * Activity retry attempt number
     * @return attemptNumber
     */
    @jakarta.annotation.Nullable public BigDecimal getAttemptNumber() {
        return attemptNumber;
    }

    public void setAttemptNumber(@jakarta.annotation.Nullable BigDecimal attemptNumber) {
        this.attemptNumber = attemptNumber;
    }

    public StreamingTelemetryContext inferenceStartTime(
            @jakarta.annotation.Nonnull BigDecimal inferenceStartTime) {
        this.inferenceStartTime = inferenceStartTime;
        return this;
    }

    /**
     * Timestamp when inference started (for duration calculation)
     * @return inferenceStartTime
     */
    @jakarta.annotation.Nonnull
    public BigDecimal getInferenceStartTime() {
        return inferenceStartTime;
    }

    public void setInferenceStartTime(@jakarta.annotation.Nonnull BigDecimal inferenceStartTime) {
        this.inferenceStartTime = inferenceStartTime;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        StreamingTelemetryContext streamingTelemetryContext = (StreamingTelemetryContext) o;
        return Objects.equals(this.callType, streamingTelemetryContext.callType)
                && Objects.equals(this.attemptNumber, streamingTelemetryContext.attemptNumber)
                && Objects.equals(
                        this.inferenceStartTime, streamingTelemetryContext.inferenceStartTime);
    }

    @Override
    public int hashCode() {
        return Objects.hash(callType, attemptNumber, inferenceStartTime);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class StreamingTelemetryContext {\n");
        sb.append("    callType: ").append(toIndentedString(callType)).append("\n");
        sb.append("    attemptNumber: ").append(toIndentedString(attemptNumber)).append("\n");
        sb.append("    inferenceStartTime: ")
                .append(toIndentedString(inferenceStartTime))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields =
                new HashSet<String>(
                        Arrays.asList("callType", "attemptNumber", "inferenceStartTime"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields =
                new HashSet<String>(Arrays.asList("callType", "inferenceStartTime"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to StreamingTelemetryContext
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!StreamingTelemetryContext.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in StreamingTelemetryContext is not found in the empty JSON string",
                                StreamingTelemetryContext.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : StreamingTelemetryContext.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // validate the required field `callType`
        LlmCallType.validateJsonElement(jsonObj.get("callType"));
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!StreamingTelemetryContext.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'StreamingTelemetryContext' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<StreamingTelemetryContext> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(StreamingTelemetryContext.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<StreamingTelemetryContext>() {
                        @Override
                        public void write(JsonWriter out, StreamingTelemetryContext value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public StreamingTelemetryContext read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of StreamingTelemetryContext given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of StreamingTelemetryContext
     * @throws IOException if the JSON string is invalid with respect to StreamingTelemetryContext
     */
    public static StreamingTelemetryContext fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, StreamingTelemetryContext.class);
    }

    /**
     * Convert an instance of StreamingTelemetryContext to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
