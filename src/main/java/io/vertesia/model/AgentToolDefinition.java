/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` SDK for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.2.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;

/**
 * Tool definition with optional activation control for agent exposure.
 */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class AgentToolDefinition {
    public static final String SERIALIZED_NAME_NAME = "name";

    @SerializedName(SERIALIZED_NAME_NAME)
    @javax.annotation.Nonnull
    private String name;

    public static final String SERIALIZED_NAME_DESCRIPTION = "description";

    @SerializedName(SERIALIZED_NAME_DESCRIPTION)
    @javax.annotation.Nullable private String description;

    public static final String SERIALIZED_NAME_INPUT_SCHEMA = "input_schema";

    @SerializedName(SERIALIZED_NAME_INPUT_SCHEMA)
    @javax.annotation.Nonnull
    private Map<String, Object> inputSchema = new HashMap<>();

    public static final String SERIALIZED_NAME_URL = "url";

    @SerializedName(SERIALIZED_NAME_URL)
    @javax.annotation.Nullable private String url;

    public static final String SERIALIZED_NAME_CATEGORY = "category";

    @SerializedName(SERIALIZED_NAME_CATEGORY)
    @javax.annotation.Nullable private String category;

    public static final String SERIALIZED_NAME_DEFAULT = "default";

    @SerializedName(SERIALIZED_NAME_DEFAULT)
    @javax.annotation.Nullable private Boolean _default;

    public static final String SERIALIZED_NAME_TOOLS = "tools";

    @SerializedName(SERIALIZED_NAME_TOOLS)
    @javax.annotation.Nullable private List<String> tools = new ArrayList<>();

    public static final String SERIALIZED_NAME_ANNOTATIONS = "annotations";

    @SerializedName(SERIALIZED_NAME_ANNOTATIONS)
    @javax.annotation.Nullable private MCPToolAnnotations annotations;

    public static final String SERIALIZED_NAME_REQUIRES_USER_CONFIRMATION =
            "requires_user_confirmation";

    @SerializedName(SERIALIZED_NAME_REQUIRES_USER_CONFIRMATION)
    @javax.annotation.Nullable private Boolean requiresUserConfirmation;

    public AgentToolDefinition() {}

    public AgentToolDefinition name(@javax.annotation.Nonnull String name) {
        this.name = name;
        return this;
    }

    /**
     * Get name
     * @return name
     */
    @javax.annotation.Nonnull
    public String getName() {
        return name;
    }

    public void setName(@javax.annotation.Nonnull String name) {
        this.name = name;
    }

    public AgentToolDefinition description(@javax.annotation.Nullable String description) {
        this.description = description;
        return this;
    }

    /**
     * Get description
     * @return description
     */
    @javax.annotation.Nullable public String getDescription() {
        return description;
    }

    public void setDescription(@javax.annotation.Nullable String description) {
        this.description = description;
    }

    public AgentToolDefinition inputSchema(
            @javax.annotation.Nonnull Map<String, Object> inputSchema) {
        this.inputSchema = inputSchema;
        return this;
    }

    public AgentToolDefinition putInputSchemaItem(String key, Object inputSchemaItem) {
        if (this.inputSchema == null) {
            this.inputSchema = new HashMap<>();
        }
        this.inputSchema.put(key, inputSchemaItem);
        return this;
    }

    /**
     * Get inputSchema
     * @return inputSchema
     */
    @javax.annotation.Nonnull
    public Map<String, Object> getInputSchema() {
        return inputSchema;
    }

    public void setInputSchema(@javax.annotation.Nonnull Map<String, Object> inputSchema) {
        this.inputSchema = inputSchema;
    }

    public AgentToolDefinition url(@javax.annotation.Nullable String url) {
        this.url = url;
        return this;
    }

    /**
     * The tool execution URL. It can be an absolute URL or a path in which case the URL is obtained  using the base URL of the tool server API. Ex: http://tool-server.com/api/ Example of relative URLs: \&quot;tools/my-tool-collection\&quot; or \&quot;/api/tools/my-tool-collection\&quot;
     * @return url
     */
    @javax.annotation.Nullable public String getUrl() {
        return url;
    }

    public void setUrl(@javax.annotation.Nullable String url) {
        this.url = url;
    }

    public AgentToolDefinition category(@javax.annotation.Nullable String category) {
        this.category = category;
        return this;
    }

    /**
     * The tool category if any - for UI purposes.
     * @return category
     */
    @javax.annotation.Nullable public String getCategory() {
        return category;
    }

    public void setCategory(@javax.annotation.Nullable String category) {
        this.category = category;
    }

    public AgentToolDefinition _default(@javax.annotation.Nullable Boolean _default) {
        this._default = _default;
        return this;
    }

    /**
     * Whether this tool is available by default. - true/undefined: Tool is always available to agents - false: Tool is only available when enabled by a skill via &#x60;tools&#x60;
     * @return _default
     */
    @javax.annotation.Nullable public Boolean getDefault() {
        return _default;
    }

    public void setDefault(@javax.annotation.Nullable Boolean _default) {
        this._default = _default;
    }

    public AgentToolDefinition tools(@javax.annotation.Nullable List<String> tools) {
        this.tools = tools;
        return this;
    }

    public AgentToolDefinition addToolsItem(String toolsItem) {
        if (this.tools == null) {
            this.tools = new ArrayList<>();
        }
        this.tools.add(toolsItem);
        return this;
    }

    /**
     * For skill tools (&#x60;learn_*&#x60;): the tool names this skill enables when called. Matches the &#x60;tools:&#x60; key used in SKILL.md frontmatter and built-in skill definitions — one name across the whole stack.
     * @return tools
     */
    @javax.annotation.Nullable public List<String> getTools() {
        return tools;
    }

    public void setTools(@javax.annotation.Nullable List<String> tools) {
        this.tools = tools;
    }

    public AgentToolDefinition annotations(
            @javax.annotation.Nullable MCPToolAnnotations annotations) {
        this.annotations = annotations;
        return this;
    }

    /**
     * MCP tool annotations providing hints about tool behavior and safety.
     * @return annotations
     */
    @javax.annotation.Nullable public MCPToolAnnotations getAnnotations() {
        return annotations;
    }

    public void setAnnotations(@javax.annotation.Nullable MCPToolAnnotations annotations) {
        this.annotations = annotations;
    }

    public AgentToolDefinition requiresUserConfirmation(
            @javax.annotation.Nullable Boolean requiresUserConfirmation) {
        this.requiresUserConfirmation = requiresUserConfirmation;
        return this;
    }

    /**
     * When true, agents must obtain explicit user confirmation via &#x60;ask_user&#x60; (Yes/No) before invoking this tool. If the user answers No, the tool must not run and should return an error indicating the user declined.  Stronger than &#x60;annotations.destructiveHint&#x60; (which is only a hint) — this is a hard contract the agent is expected to honor. Set on tools that perform irreversible or destructive actions (e.g. delete_*).
     * @return requiresUserConfirmation
     */
    @javax.annotation.Nullable public Boolean getRequiresUserConfirmation() {
        return requiresUserConfirmation;
    }

    public void setRequiresUserConfirmation(
            @javax.annotation.Nullable Boolean requiresUserConfirmation) {
        this.requiresUserConfirmation = requiresUserConfirmation;
    }

    /**
     * A container for additional, undeclared properties.
     * This is a holder for any undeclared properties as specified with
     * the 'additionalProperties' keyword in the OAS document.
     */
    private Map<String, Object> additionalProperties;

    /**
     * Set the additional (undeclared) property with the specified name and value.
     * If the property does not already exist, create it otherwise replace it.
     *
     * @param key name of the property
     * @param value value of the property
     * @return the AgentToolDefinition instance itself
     */
    public AgentToolDefinition putAdditionalProperty(String key, Object value) {
        if (this.additionalProperties == null) {
            this.additionalProperties = new HashMap<String, Object>();
        }
        this.additionalProperties.put(key, value);
        return this;
    }

    /**
     * Return the additional (undeclared) property.
     *
     * @return a map of objects
     */
    public Map<String, Object> getAdditionalProperties() {
        return additionalProperties;
    }

    /**
     * Return the additional (undeclared) property with the specified name.
     *
     * @param key name of the property
     * @return an object
     */
    public Object getAdditionalProperty(String key) {
        if (this.additionalProperties == null) {
            return null;
        }
        return this.additionalProperties.get(key);
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        AgentToolDefinition agentToolDefinition = (AgentToolDefinition) o;
        return Objects.equals(this.name, agentToolDefinition.name)
                && Objects.equals(this.description, agentToolDefinition.description)
                && Objects.equals(this.inputSchema, agentToolDefinition.inputSchema)
                && Objects.equals(this.url, agentToolDefinition.url)
                && Objects.equals(this.category, agentToolDefinition.category)
                && Objects.equals(this._default, agentToolDefinition._default)
                && Objects.equals(this.tools, agentToolDefinition.tools)
                && Objects.equals(this.annotations, agentToolDefinition.annotations)
                && Objects.equals(
                        this.requiresUserConfirmation, agentToolDefinition.requiresUserConfirmation)
                && Objects.equals(
                        this.additionalProperties, agentToolDefinition.additionalProperties);
    }

    @Override
    public int hashCode() {
        return Objects.hash(
                name,
                description,
                inputSchema,
                url,
                category,
                _default,
                tools,
                annotations,
                requiresUserConfirmation,
                additionalProperties);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class AgentToolDefinition {\n");
        sb.append("    name: ").append(toIndentedString(name)).append("\n");
        sb.append("    description: ").append(toIndentedString(description)).append("\n");
        sb.append("    inputSchema: ").append(toIndentedString(inputSchema)).append("\n");
        sb.append("    url: ").append(toIndentedString(url)).append("\n");
        sb.append("    category: ").append(toIndentedString(category)).append("\n");
        sb.append("    _default: ").append(toIndentedString(_default)).append("\n");
        sb.append("    tools: ").append(toIndentedString(tools)).append("\n");
        sb.append("    annotations: ").append(toIndentedString(annotations)).append("\n");
        sb.append("    requiresUserConfirmation: ")
                .append(toIndentedString(requiresUserConfirmation))
                .append("\n");
        sb.append("    additionalProperties: ")
                .append(toIndentedString(additionalProperties))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields =
                new HashSet<String>(
                        Arrays.asList(
                                "name",
                                "description",
                                "input_schema",
                                "url",
                                "category",
                                "default",
                                "tools",
                                "annotations",
                                "requires_user_confirmation"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(Arrays.asList("name", "input_schema"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to AgentToolDefinition
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!AgentToolDefinition.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in AgentToolDefinition is not found in the empty JSON string",
                                AgentToolDefinition.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : AgentToolDefinition.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (!jsonObj.get("name").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `name` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("name").toString()));
        }
        if ((jsonObj.get("description") != null && !jsonObj.get("description").isJsonNull())
                && !jsonObj.get("description").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `description` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("description").toString()));
        }
        if ((jsonObj.get("url") != null && !jsonObj.get("url").isJsonNull())
                && !jsonObj.get("url").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `url` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("url").toString()));
        }
        if ((jsonObj.get("category") != null && !jsonObj.get("category").isJsonNull())
                && !jsonObj.get("category").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `category` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("category").toString()));
        }
        // ensure the optional json data is an array if present
        if (jsonObj.get("tools") != null
                && !jsonObj.get("tools").isJsonNull()
                && !jsonObj.get("tools").isJsonArray()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `tools` to be an array in the JSON string but got `%s`",
                            jsonObj.get("tools").toString()));
        }
        // validate the optional field `annotations`
        if (jsonObj.get("annotations") != null && !jsonObj.get("annotations").isJsonNull()) {
            MCPToolAnnotations.validateJsonElement(jsonObj.get("annotations"));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!AgentToolDefinition.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'AgentToolDefinition' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<AgentToolDefinition> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(AgentToolDefinition.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<AgentToolDefinition>() {
                        @Override
                        public void write(JsonWriter out, AgentToolDefinition value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            obj.remove("additionalProperties");
                            // serialize additional properties
                            if (value.getAdditionalProperties() != null) {
                                for (Map.Entry<String, Object> entry :
                                        value.getAdditionalProperties().entrySet()) {
                                    if (entry.getValue() instanceof String)
                                        obj.addProperty(entry.getKey(), (String) entry.getValue());
                                    else if (entry.getValue() instanceof Number)
                                        obj.addProperty(entry.getKey(), (Number) entry.getValue());
                                    else if (entry.getValue() instanceof Boolean)
                                        obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                                    else if (entry.getValue() instanceof Character)
                                        obj.addProperty(
                                                entry.getKey(), (Character) entry.getValue());
                                    else {
                                        JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                                        if (jsonElement.isJsonArray()) {
                                            obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                                        } else {
                                            obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                                        }
                                    }
                                }
                            }
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public AgentToolDefinition read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            JsonObject jsonObj = jsonElement.getAsJsonObject();
                            // store additional fields in the deserialized instance
                            AgentToolDefinition instance = thisAdapter.fromJsonTree(jsonObj);
                            for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
                                if (!openapiFields.contains(entry.getKey())) {
                                    if (entry.getValue().isJsonPrimitive()) { // primitive type
                                        if (entry.getValue().getAsJsonPrimitive().isString())
                                            instance.putAdditionalProperty(
                                                    entry.getKey(), entry.getValue().getAsString());
                                        else if (entry.getValue().getAsJsonPrimitive().isNumber())
                                            instance.putAdditionalProperty(
                                                    entry.getKey(), entry.getValue().getAsNumber());
                                        else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                                            instance.putAdditionalProperty(
                                                    entry.getKey(),
                                                    entry.getValue().getAsBoolean());
                                        else
                                            throw new IllegalArgumentException(
                                                    String.format(
                                                            java.util.Locale.ROOT,
                                                            "The field `%s` has unknown primitive type. Value: %s",
                                                            entry.getKey(),
                                                            entry.getValue().toString()));
                                    } else if (entry.getValue().isJsonArray()) {
                                        instance.putAdditionalProperty(
                                                entry.getKey(),
                                                gson.fromJson(entry.getValue(), List.class));
                                    } else { // JSON object
                                        instance.putAdditionalProperty(
                                                entry.getKey(),
                                                gson.fromJson(entry.getValue(), HashMap.class));
                                    }
                                }
                            }
                            return instance;
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of AgentToolDefinition given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of AgentToolDefinition
     * @throws IOException if the JSON string is invalid with respect to AgentToolDefinition
     */
    public static AgentToolDefinition fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, AgentToolDefinition.class);
    }

    /**
     * Convert an instance of AgentToolDefinition to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
