/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260901.052906Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashSet;
import java.util.List;
import java.util.Objects;

/**
 * ParallelCollectDefinition
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class ParallelCollectDefinition {
    public static final String SERIALIZED_NAME_INTO = "into";

    @SerializedName(SERIALIZED_NAME_INTO)
    @jakarta.annotation.Nonnull
    private String into;

    public static final String SERIALIZED_NAME_MODE = "mode";

    @SerializedName(SERIALIZED_NAME_MODE)
    @jakarta.annotation.Nullable private ParallelCollectMode mode;

    public static final String SERIALIZED_NAME_INCLUDE = "include";

    @SerializedName(SERIALIZED_NAME_INCLUDE)
    @jakarta.annotation.Nullable private List<ParallelCollectField> include = new ArrayList<>();

    public ParallelCollectDefinition() {}

    public ParallelCollectDefinition into(@jakarta.annotation.Nonnull String into) {
        this.into = into;
        return this;
    }

    /**
     * Context key that receives the collected results.
     * @return into
     */
    @jakarta.annotation.Nonnull
    public String getInto() {
        return into;
    }

    public void setInto(@jakarta.annotation.Nonnull String into) {
        this.into = into;
    }

    public ParallelCollectDefinition mode(@jakarta.annotation.Nullable ParallelCollectMode mode) {
        this.mode = mode;
        return this;
    }

    /**
     * Get mode
     * @return mode
     */
    @jakarta.annotation.Nullable public ParallelCollectMode getMode() {
        return mode;
    }

    public void setMode(@jakarta.annotation.Nullable ParallelCollectMode mode) {
        this.mode = mode;
    }

    public ParallelCollectDefinition include(
            @jakarta.annotation.Nullable List<ParallelCollectField> include) {
        this.include = include;
        return this;
    }

    public ParallelCollectDefinition addIncludeItem(ParallelCollectField includeItem) {
        if (this.include == null) {
            this.include = new ArrayList<>();
        }
        this.include.add(includeItem);
        return this;
    }

    /**
     * Fields to include in each collected item. Defaults to the operational envelope: status, index, item_id, output, error, and child_run_id.
     * @return include
     */
    @jakarta.annotation.Nullable public List<ParallelCollectField> getInclude() {
        return include;
    }

    public void setInclude(@jakarta.annotation.Nullable List<ParallelCollectField> include) {
        this.include = include;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ParallelCollectDefinition parallelCollectDefinition = (ParallelCollectDefinition) o;
        return Objects.equals(this.into, parallelCollectDefinition.into)
                && Objects.equals(this.mode, parallelCollectDefinition.mode)
                && Objects.equals(this.include, parallelCollectDefinition.include);
    }

    @Override
    public int hashCode() {
        return Objects.hash(into, mode, include);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class ParallelCollectDefinition {\n");
        sb.append("    into: ").append(toIndentedString(into)).append("\n");
        sb.append("    mode: ").append(toIndentedString(mode)).append("\n");
        sb.append("    include: ").append(toIndentedString(include)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>(Arrays.asList("into", "mode", "include"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(Arrays.asList("into"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to ParallelCollectDefinition
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!ParallelCollectDefinition.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in ParallelCollectDefinition is not found in the empty JSON string",
                                ParallelCollectDefinition.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : ParallelCollectDefinition.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (!jsonObj.get("into").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `into` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("into").toString()));
        }
        // validate the optional field `mode`
        if (jsonObj.get("mode") != null && !jsonObj.get("mode").isJsonNull()) {
            ParallelCollectMode.validateJsonElement(jsonObj.get("mode"));
        }
        // ensure the optional json data is an array if present
        if (jsonObj.get("include") != null
                && !jsonObj.get("include").isJsonNull()
                && !jsonObj.get("include").isJsonArray()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `include` to be an array in the JSON string but got `%s`",
                            jsonObj.get("include").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ParallelCollectDefinition.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'ParallelCollectDefinition' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ParallelCollectDefinition> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(ParallelCollectDefinition.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<ParallelCollectDefinition>() {
                        @Override
                        public void write(JsonWriter out, ParallelCollectDefinition value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public ParallelCollectDefinition read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of ParallelCollectDefinition given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of ParallelCollectDefinition
     * @throws IOException if the JSON string is invalid with respect to ParallelCollectDefinition
     */
    public static ParallelCollectDefinition fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, ParallelCollectDefinition.class);
    }

    /**
     * Convert an instance of ParallelCollectDefinition to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
