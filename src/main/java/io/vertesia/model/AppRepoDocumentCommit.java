/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260725.083715Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashSet;
import java.util.List;
import java.util.Objects;

/**
 * Result of committing one or more uploaded documents to an app repository.
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class AppRepoDocumentCommit {
    public static final String SERIALIZED_NAME_REF = "ref";

    @SerializedName(SERIALIZED_NAME_REF)
    @jakarta.annotation.Nonnull
    private String ref;

    public static final String SERIALIZED_NAME_PREVIOUS_COMMIT = "previous_commit";

    @SerializedName(SERIALIZED_NAME_PREVIOUS_COMMIT)
    @jakarta.annotation.Nonnull
    private String previousCommit;

    public static final String SERIALIZED_NAME_COMMIT = "commit";

    @SerializedName(SERIALIZED_NAME_COMMIT)
    @jakarta.annotation.Nonnull
    private String commit;

    public static final String SERIALIZED_NAME_PATHS = "paths";

    @SerializedName(SERIALIZED_NAME_PATHS)
    @jakarta.annotation.Nonnull
    private List<String> paths = new ArrayList<>();

    public AppRepoDocumentCommit() {}

    public AppRepoDocumentCommit ref(@jakarta.annotation.Nonnull String ref) {
        this.ref = ref;
        return this;
    }

    /**
     * Updated branch name.
     * @return ref
     */
    @jakarta.annotation.Nonnull
    public String getRef() {
        return ref;
    }

    public void setRef(@jakarta.annotation.Nonnull String ref) {
        this.ref = ref;
    }

    public AppRepoDocumentCommit previousCommit(@jakarta.annotation.Nonnull String previousCommit) {
        this.previousCommit = previousCommit;
        return this;
    }

    /**
     * Branch HEAD before the commit.
     * @return previousCommit
     */
    @jakarta.annotation.Nonnull
    public String getPreviousCommit() {
        return previousCommit;
    }

    public void setPreviousCommit(@jakarta.annotation.Nonnull String previousCommit) {
        this.previousCommit = previousCommit;
    }

    public AppRepoDocumentCommit commit(@jakarta.annotation.Nonnull String commit) {
        this.commit = commit;
        return this;
    }

    /**
     * Newly created commit SHA.
     * @return commit
     */
    @jakarta.annotation.Nonnull
    public String getCommit() {
        return commit;
    }

    public void setCommit(@jakarta.annotation.Nonnull String commit) {
        this.commit = commit;
    }

    public AppRepoDocumentCommit paths(@jakarta.annotation.Nonnull List<String> paths) {
        this.paths = paths;
        return this;
    }

    public AppRepoDocumentCommit addPathsItem(String pathsItem) {
        if (this.paths == null) {
            this.paths = new ArrayList<>();
        }
        this.paths.add(pathsItem);
        return this;
    }

    /**
     * Repository paths changed by the commit.
     * @return paths
     */
    @jakarta.annotation.Nonnull
    public List<String> getPaths() {
        return paths;
    }

    public void setPaths(@jakarta.annotation.Nonnull List<String> paths) {
        this.paths = paths;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        AppRepoDocumentCommit appRepoDocumentCommit = (AppRepoDocumentCommit) o;
        return Objects.equals(this.ref, appRepoDocumentCommit.ref)
                && Objects.equals(this.previousCommit, appRepoDocumentCommit.previousCommit)
                && Objects.equals(this.commit, appRepoDocumentCommit.commit)
                && Objects.equals(this.paths, appRepoDocumentCommit.paths);
    }

    @Override
    public int hashCode() {
        return Objects.hash(ref, previousCommit, commit, paths);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class AppRepoDocumentCommit {\n");
        sb.append("    ref: ").append(toIndentedString(ref)).append("\n");
        sb.append("    previousCommit: ").append(toIndentedString(previousCommit)).append("\n");
        sb.append("    commit: ").append(toIndentedString(commit)).append("\n");
        sb.append("    paths: ").append(toIndentedString(paths)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields =
                new HashSet<String>(Arrays.asList("ref", "previous_commit", "commit", "paths"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields =
                new HashSet<String>(Arrays.asList("ref", "previous_commit", "commit", "paths"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to AppRepoDocumentCommit
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!AppRepoDocumentCommit.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in AppRepoDocumentCommit is not found in the empty JSON string",
                                AppRepoDocumentCommit.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : AppRepoDocumentCommit.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (!jsonObj.get("ref").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `ref` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("ref").toString()));
        }
        if (!jsonObj.get("previous_commit").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `previous_commit` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("previous_commit").toString()));
        }
        if (!jsonObj.get("commit").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `commit` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("commit").toString()));
        }
        // ensure the required json array is present
        if (jsonObj.get("paths") == null) {
            throw new IllegalArgumentException(
                    "Expected the field `linkedContent` to be an array in the JSON string but got `null`");
        } else if (!jsonObj.get("paths").isJsonArray()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `paths` to be an array in the JSON string but got `%s`",
                            jsonObj.get("paths").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!AppRepoDocumentCommit.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'AppRepoDocumentCommit' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<AppRepoDocumentCommit> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(AppRepoDocumentCommit.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<AppRepoDocumentCommit>() {
                        @Override
                        public void write(JsonWriter out, AppRepoDocumentCommit value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public AppRepoDocumentCommit read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of AppRepoDocumentCommit given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of AppRepoDocumentCommit
     * @throws IOException if the JSON string is invalid with respect to AppRepoDocumentCommit
     */
    public static AppRepoDocumentCommit fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, AppRepoDocumentCommit.class);
    }

    /**
     * Convert an instance of AppRepoDocumentCommit to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
