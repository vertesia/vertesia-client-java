/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260901.052906Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.Arrays;
import java.util.HashSet;
import java.util.Objects;

/**
 * ViewResultMedia
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class ViewResultMedia {
    /**
     * Gets or Sets source
     */
    @JsonAdapter(SourceEnum.Adapter.class)
    public enum SourceEnum {
        CONTENT_THUMBNAIL("content_thumbnail"),

        PROPERTY("property"),

        TYPE_ICON("type_icon"),

        UNKNOWN_DEFAULT_OPEN_API("unknown_default_open_api");

        private String value;

        SourceEnum(String value) {
            this.value = value;
        }

        public String getValue() {
            return value;
        }

        @Override
        public String toString() {
            return String.valueOf(value);
        }

        public static SourceEnum fromValue(String value) {
            for (SourceEnum b : SourceEnum.values()) {
                if (b.value.equals(value)) {
                    return b;
                }
            }
            return UNKNOWN_DEFAULT_OPEN_API;
        }

        public static class Adapter extends TypeAdapter<SourceEnum> {
            @Override
            public void write(final JsonWriter jsonWriter, final SourceEnum enumeration)
                    throws IOException {
                jsonWriter.value(enumeration.getValue());
            }

            @Override
            public SourceEnum read(final JsonReader jsonReader) throws IOException {
                String value = jsonReader.nextString();
                return SourceEnum.fromValue(value);
            }
        }

        public static void validateJsonElement(JsonElement jsonElement) throws IOException {
            String value = jsonElement.getAsString();
            SourceEnum.fromValue(value);
        }
    }

    public static final String SERIALIZED_NAME_SOURCE = "source";

    @SerializedName(SERIALIZED_NAME_SOURCE)
    @jakarta.annotation.Nonnull
    private SourceEnum source;

    public static final String SERIALIZED_NAME_FIELD = "field";

    @SerializedName(SERIALIZED_NAME_FIELD)
    @jakarta.annotation.Nullable private String field;

    /**
     * Gets or Sets fit
     */
    @JsonAdapter(FitEnum.Adapter.class)
    public enum FitEnum {
        COVER("cover"),

        CONTAIN("contain"),

        UNKNOWN_DEFAULT_OPEN_API("unknown_default_open_api");

        private String value;

        FitEnum(String value) {
            this.value = value;
        }

        public String getValue() {
            return value;
        }

        @Override
        public String toString() {
            return String.valueOf(value);
        }

        public static FitEnum fromValue(String value) {
            for (FitEnum b : FitEnum.values()) {
                if (b.value.equals(value)) {
                    return b;
                }
            }
            return UNKNOWN_DEFAULT_OPEN_API;
        }

        public static class Adapter extends TypeAdapter<FitEnum> {
            @Override
            public void write(final JsonWriter jsonWriter, final FitEnum enumeration)
                    throws IOException {
                jsonWriter.value(enumeration.getValue());
            }

            @Override
            public FitEnum read(final JsonReader jsonReader) throws IOException {
                String value = jsonReader.nextString();
                return FitEnum.fromValue(value);
            }
        }

        public static void validateJsonElement(JsonElement jsonElement) throws IOException {
            String value = jsonElement.getAsString();
            FitEnum.fromValue(value);
        }
    }

    public static final String SERIALIZED_NAME_FIT = "fit";

    @SerializedName(SERIALIZED_NAME_FIT)
    @jakarta.annotation.Nullable private FitEnum fit;

    /**
     * Gets or Sets fallback
     */
    @JsonAdapter(FallbackEnum.Adapter.class)
    public enum FallbackEnum {
        TYPE_ICON("type_icon"),

        PLACEHOLDER("placeholder"),

        NONE("none"),

        UNKNOWN_DEFAULT_OPEN_API("unknown_default_open_api");

        private String value;

        FallbackEnum(String value) {
            this.value = value;
        }

        public String getValue() {
            return value;
        }

        @Override
        public String toString() {
            return String.valueOf(value);
        }

        public static FallbackEnum fromValue(String value) {
            for (FallbackEnum b : FallbackEnum.values()) {
                if (b.value.equals(value)) {
                    return b;
                }
            }
            return UNKNOWN_DEFAULT_OPEN_API;
        }

        public static class Adapter extends TypeAdapter<FallbackEnum> {
            @Override
            public void write(final JsonWriter jsonWriter, final FallbackEnum enumeration)
                    throws IOException {
                jsonWriter.value(enumeration.getValue());
            }

            @Override
            public FallbackEnum read(final JsonReader jsonReader) throws IOException {
                String value = jsonReader.nextString();
                return FallbackEnum.fromValue(value);
            }
        }

        public static void validateJsonElement(JsonElement jsonElement) throws IOException {
            String value = jsonElement.getAsString();
            FallbackEnum.fromValue(value);
        }
    }

    public static final String SERIALIZED_NAME_FALLBACK = "fallback";

    @SerializedName(SERIALIZED_NAME_FALLBACK)
    @jakarta.annotation.Nullable private FallbackEnum fallback;

    public ViewResultMedia() {}

    public ViewResultMedia source(@jakarta.annotation.Nonnull SourceEnum source) {
        this.source = source;
        return this;
    }

    /**
     * Get source
     * @return source
     */
    @jakarta.annotation.Nonnull
    public SourceEnum getSource() {
        return source;
    }

    public void setSource(@jakarta.annotation.Nonnull SourceEnum source) {
        this.source = source;
    }

    public ViewResultMedia field(@jakarta.annotation.Nullable String field) {
        this.field = field;
        return this;
    }

    /**
     * Get field
     * @return field
     */
    @jakarta.annotation.Nullable public String getField() {
        return field;
    }

    public void setField(@jakarta.annotation.Nullable String field) {
        this.field = field;
    }

    public ViewResultMedia fit(@jakarta.annotation.Nullable FitEnum fit) {
        this.fit = fit;
        return this;
    }

    /**
     * Get fit
     * @return fit
     */
    @jakarta.annotation.Nullable public FitEnum getFit() {
        return fit;
    }

    public void setFit(@jakarta.annotation.Nullable FitEnum fit) {
        this.fit = fit;
    }

    public ViewResultMedia fallback(@jakarta.annotation.Nullable FallbackEnum fallback) {
        this.fallback = fallback;
        return this;
    }

    /**
     * Get fallback
     * @return fallback
     */
    @jakarta.annotation.Nullable public FallbackEnum getFallback() {
        return fallback;
    }

    public void setFallback(@jakarta.annotation.Nullable FallbackEnum fallback) {
        this.fallback = fallback;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ViewResultMedia viewResultMedia = (ViewResultMedia) o;
        return Objects.equals(this.source, viewResultMedia.source)
                && Objects.equals(this.field, viewResultMedia.field)
                && Objects.equals(this.fit, viewResultMedia.fit)
                && Objects.equals(this.fallback, viewResultMedia.fallback);
    }

    @Override
    public int hashCode() {
        return Objects.hash(source, field, fit, fallback);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class ViewResultMedia {\n");
        sb.append("    source: ").append(toIndentedString(source)).append("\n");
        sb.append("    field: ").append(toIndentedString(field)).append("\n");
        sb.append("    fit: ").append(toIndentedString(fit)).append("\n");
        sb.append("    fallback: ").append(toIndentedString(fallback)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>(Arrays.asList("source", "field", "fit", "fallback"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(Arrays.asList("source"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to ViewResultMedia
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!ViewResultMedia.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in ViewResultMedia is not found in the empty JSON string",
                                ViewResultMedia.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : ViewResultMedia.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (!jsonObj.get("source").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `source` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("source").toString()));
        }
        // validate the required field `source`
        SourceEnum.validateJsonElement(jsonObj.get("source"));
        if ((jsonObj.get("field") != null && !jsonObj.get("field").isJsonNull())
                && !jsonObj.get("field").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `field` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("field").toString()));
        }
        if ((jsonObj.get("fit") != null && !jsonObj.get("fit").isJsonNull())
                && !jsonObj.get("fit").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `fit` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("fit").toString()));
        }
        // validate the optional field `fit`
        if (jsonObj.get("fit") != null && !jsonObj.get("fit").isJsonNull()) {
            FitEnum.validateJsonElement(jsonObj.get("fit"));
        }
        if ((jsonObj.get("fallback") != null && !jsonObj.get("fallback").isJsonNull())
                && !jsonObj.get("fallback").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `fallback` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("fallback").toString()));
        }
        // validate the optional field `fallback`
        if (jsonObj.get("fallback") != null && !jsonObj.get("fallback").isJsonNull()) {
            FallbackEnum.validateJsonElement(jsonObj.get("fallback"));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ViewResultMedia.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'ViewResultMedia' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ViewResultMedia> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(ViewResultMedia.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<ViewResultMedia>() {
                        @Override
                        public void write(JsonWriter out, ViewResultMedia value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public ViewResultMedia read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of ViewResultMedia given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of ViewResultMedia
     * @throws IOException if the JSON string is invalid with respect to ViewResultMedia
     */
    public static ViewResultMedia fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, ViewResultMedia.class);
    }

    /**
     * Convert an instance of ViewResultMedia to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
