/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260807.073259Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.Arrays;
import java.util.HashSet;
import java.util.Objects;

/**
 * Install-time provisioning blueprint for an &#x60;auth: &#39;api_key&#39;&#x60; MCP collection. Declares whether the installer is prompted for the key. Never holds the key itself — manifests are shareable documents.
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class MCPApiKeyConfig {
    public static final String SERIALIZED_NAME_REQUIRED_AT_INSTALL = "required_at_install";

    @SerializedName(SERIALIZED_NAME_REQUIRED_AT_INSTALL)
    @jakarta.annotation.Nullable private Boolean requiredAtInstall;

    public static final String SERIALIZED_NAME_INSTRUCTIONS = "instructions";

    @SerializedName(SERIALIZED_NAME_INSTRUCTIONS)
    @jakarta.annotation.Nullable private String instructions;

    public MCPApiKeyConfig() {}

    public MCPApiKeyConfig requiredAtInstall(
            @jakarta.annotation.Nullable Boolean requiredAtInstall) {
        this.requiredAtInstall = requiredAtInstall;
        return this;
    }

    /**
     * When true, the installer must supply the key in the install dialog. Use this for a manifest published to projects that each hold their own key. Leave unset when the key was already stored by whoever registered the server.
     * @return requiredAtInstall
     */
    @jakarta.annotation.Nullable public Boolean getRequiredAtInstall() {
        return requiredAtInstall;
    }

    public void setRequiredAtInstall(@jakarta.annotation.Nullable Boolean requiredAtInstall) {
        this.requiredAtInstall = requiredAtInstall;
    }

    public MCPApiKeyConfig instructions(@jakarta.annotation.Nullable String instructions) {
        this.instructions = instructions;
        return this;
    }

    /**
     * Shown in the install dialog above the key field — typically where to generate the key on the remote service.
     * @return instructions
     */
    @jakarta.annotation.Nullable public String getInstructions() {
        return instructions;
    }

    public void setInstructions(@jakarta.annotation.Nullable String instructions) {
        this.instructions = instructions;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        MCPApiKeyConfig mcPApiKeyConfig = (MCPApiKeyConfig) o;
        return Objects.equals(this.requiredAtInstall, mcPApiKeyConfig.requiredAtInstall)
                && Objects.equals(this.instructions, mcPApiKeyConfig.instructions);
    }

    @Override
    public int hashCode() {
        return Objects.hash(requiredAtInstall, instructions);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class MCPApiKeyConfig {\n");
        sb.append("    requiredAtInstall: ")
                .append(toIndentedString(requiredAtInstall))
                .append("\n");
        sb.append("    instructions: ").append(toIndentedString(instructions)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>(Arrays.asList("required_at_install", "instructions"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(0);
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to MCPApiKeyConfig
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!MCPApiKeyConfig.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in MCPApiKeyConfig is not found in the empty JSON string",
                                MCPApiKeyConfig.openapiRequiredFields.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if ((jsonObj.get("instructions") != null && !jsonObj.get("instructions").isJsonNull())
                && !jsonObj.get("instructions").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `instructions` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("instructions").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!MCPApiKeyConfig.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'MCPApiKeyConfig' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<MCPApiKeyConfig> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(MCPApiKeyConfig.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<MCPApiKeyConfig>() {
                        @Override
                        public void write(JsonWriter out, MCPApiKeyConfig value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public MCPApiKeyConfig read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of MCPApiKeyConfig given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of MCPApiKeyConfig
     * @throws IOException if the JSON string is invalid with respect to MCPApiKeyConfig
     */
    public static MCPApiKeyConfig fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, MCPApiKeyConfig.class);
    }

    /**
     * Convert an instance of MCPApiKeyConfig to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
