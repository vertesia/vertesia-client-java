/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260725.083715Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.Arrays;
import java.util.HashSet;
import java.util.Objects;
import org.openapitools.jackson.nullable.JsonNullable;

/**
 * VersionAppVersionRecordAppAppManifest
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class VersionAppVersionRecordAppAppManifest {
    public static final String SERIALIZED_NAME_VERSION = "version";

    @SerializedName(SERIALIZED_NAME_VERSION)
    @jakarta.annotation.Nonnull
    private AppVersionRecord version;

    public static final String SERIALIZED_NAME_APP = "app";

    @SerializedName(SERIALIZED_NAME_APP)
    @jakarta.annotation.Nullable private Object app = null;

    public VersionAppVersionRecordAppAppManifest() {}

    public VersionAppVersionRecordAppAppManifest version(
            @jakarta.annotation.Nonnull AppVersionRecord version) {
        this.version = version;
        return this;
    }

    /**
     * Get version
     * @return version
     */
    @jakarta.annotation.Nonnull
    public AppVersionRecord getVersion() {
        return version;
    }

    public void setVersion(@jakarta.annotation.Nonnull AppVersionRecord version) {
        this.version = version;
    }

    public VersionAppVersionRecordAppAppManifest app(@jakarta.annotation.Nullable Object app) {
        this.app = app;
        return this;
    }

    /**
     * Get app
     * @return app
     */
    @jakarta.annotation.Nullable public Object getApp() {
        return app;
    }

    public void setApp(@jakarta.annotation.Nullable Object app) {
        this.app = app;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        VersionAppVersionRecordAppAppManifest versionAppVersionRecordAppAppManifest =
                (VersionAppVersionRecordAppAppManifest) o;
        return Objects.equals(this.version, versionAppVersionRecordAppAppManifest.version)
                && Objects.equals(this.app, versionAppVersionRecordAppAppManifest.app);
    }

    private static <T> boolean equalsNullable(JsonNullable<T> a, JsonNullable<T> b) {
        return a == b
                || (a != null
                        && b != null
                        && a.isPresent()
                        && b.isPresent()
                        && Objects.deepEquals(a.get(), b.get()));
    }

    @Override
    public int hashCode() {
        return Objects.hash(version, app);
    }

    private static <T> int hashCodeNullable(JsonNullable<T> a) {
        if (a == null) {
            return 1;
        }
        return a.isPresent() ? Arrays.deepHashCode(new Object[] {a.get()}) : 31;
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class VersionAppVersionRecordAppAppManifest {\n");
        sb.append("    version: ").append(toIndentedString(version)).append("\n");
        sb.append("    app: ").append(toIndentedString(app)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>(Arrays.asList("version", "app"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(Arrays.asList("version"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to VersionAppVersionRecordAppAppManifest
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!VersionAppVersionRecordAppAppManifest.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in VersionAppVersionRecordAppAppManifest is not found in the empty JSON string",
                                VersionAppVersionRecordAppAppManifest.openapiRequiredFields
                                        .toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : VersionAppVersionRecordAppAppManifest.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // validate the required field `version`
        AppVersionRecord.validateJsonElement(jsonObj.get("version"));
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!VersionAppVersionRecordAppAppManifest.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'VersionAppVersionRecordAppAppManifest'
                // and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<VersionAppVersionRecordAppAppManifest> thisAdapter =
                    gson.getDelegateAdapter(
                            this, TypeToken.get(VersionAppVersionRecordAppAppManifest.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<VersionAppVersionRecordAppAppManifest>() {
                        @Override
                        public void write(
                                JsonWriter out, VersionAppVersionRecordAppAppManifest value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public VersionAppVersionRecordAppAppManifest read(JsonReader in)
                                throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of VersionAppVersionRecordAppAppManifest given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of VersionAppVersionRecordAppAppManifest
     * @throws IOException if the JSON string is invalid with respect to VersionAppVersionRecordAppAppManifest
     */
    public static VersionAppVersionRecordAppAppManifest fromJson(String jsonString)
            throws IOException {
        return JSON.getGson().fromJson(jsonString, VersionAppVersionRecordAppAppManifest.class);
    }

    /**
     * Convert an instance of VersionAppVersionRecordAppAppManifest to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
