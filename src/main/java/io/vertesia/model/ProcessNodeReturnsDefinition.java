/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.4.1
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashSet;
import java.util.List;
import java.util.Objects;

/**
 * ProcessNodeReturnsDefinition
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class ProcessNodeReturnsDefinition {
    public static final String SERIALIZED_NAME_FROM = "from";

    @SerializedName(SERIALIZED_NAME_FROM)
    @jakarta.annotation.Nullable private String from;

    public static final String SERIALIZED_NAME_CONTEXT = "context";

    @SerializedName(SERIALIZED_NAME_CONTEXT)
    @jakarta.annotation.Nullable private List<String> context = new ArrayList<>();

    public ProcessNodeReturnsDefinition() {}

    public ProcessNodeReturnsDefinition from(@jakarta.annotation.Nullable String from) {
        this.from = from;
        return this;
    }

    /**
     * Path to read from the completed child process state. Use &#x60;context.foo&#x60; for child context values or &#x60;state.sequence&#x60; for process-state fields. If omitted, the child context is used as the node output.
     * @return from
     */
    @jakarta.annotation.Nullable public String getFrom() {
        return from;
    }

    public void setFrom(@jakarta.annotation.Nullable String from) {
        this.from = from;
    }

    public ProcessNodeReturnsDefinition context(@jakarta.annotation.Nullable List<String> context) {
        this.context = context;
        return this;
    }

    public ProcessNodeReturnsDefinition addContextItem(String contextItem) {
        if (this.context == null) {
            this.context = new ArrayList<>();
        }
        this.context.add(contextItem);
        return this;
    }

    /**
     * Select specific fields from the completed child process context. Ignored when &#x60;from&#x60; is set.
     * @return context
     */
    @jakarta.annotation.Nullable public List<String> getContext() {
        return context;
    }

    public void setContext(@jakarta.annotation.Nullable List<String> context) {
        this.context = context;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ProcessNodeReturnsDefinition processNodeReturnsDefinition =
                (ProcessNodeReturnsDefinition) o;
        return Objects.equals(this.from, processNodeReturnsDefinition.from)
                && Objects.equals(this.context, processNodeReturnsDefinition.context);
    }

    @Override
    public int hashCode() {
        return Objects.hash(from, context);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class ProcessNodeReturnsDefinition {\n");
        sb.append("    from: ").append(toIndentedString(from)).append("\n");
        sb.append("    context: ").append(toIndentedString(context)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>(Arrays.asList("from", "context"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(0);
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to ProcessNodeReturnsDefinition
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!ProcessNodeReturnsDefinition.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in ProcessNodeReturnsDefinition is not found in the empty JSON string",
                                ProcessNodeReturnsDefinition.openapiRequiredFields.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if ((jsonObj.get("from") != null && !jsonObj.get("from").isJsonNull())
                && !jsonObj.get("from").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `from` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("from").toString()));
        }
        // ensure the optional json data is an array if present
        if (jsonObj.get("context") != null
                && !jsonObj.get("context").isJsonNull()
                && !jsonObj.get("context").isJsonArray()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `context` to be an array in the JSON string but got `%s`",
                            jsonObj.get("context").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ProcessNodeReturnsDefinition.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'ProcessNodeReturnsDefinition' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ProcessNodeReturnsDefinition> thisAdapter =
                    gson.getDelegateAdapter(
                            this, TypeToken.get(ProcessNodeReturnsDefinition.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<ProcessNodeReturnsDefinition>() {
                        @Override
                        public void write(JsonWriter out, ProcessNodeReturnsDefinition value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public ProcessNodeReturnsDefinition read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of ProcessNodeReturnsDefinition given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of ProcessNodeReturnsDefinition
     * @throws IOException if the JSON string is invalid with respect to ProcessNodeReturnsDefinition
     */
    public static ProcessNodeReturnsDefinition fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, ProcessNodeReturnsDefinition.class);
    }

    /**
     * Convert an instance of ProcessNodeReturnsDefinition to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
