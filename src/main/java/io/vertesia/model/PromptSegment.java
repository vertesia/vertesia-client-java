/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.4.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashSet;
import java.util.List;
import java.util.Objects;

/**
 * PromptSegment
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class PromptSegment {
    public static final String SERIALIZED_NAME_ROLE = "role";

    @SerializedName(SERIALIZED_NAME_ROLE)
    @jakarta.annotation.Nonnull
    private PromptRole role;

    public static final String SERIALIZED_NAME_CONTENT = "content";

    @SerializedName(SERIALIZED_NAME_CONTENT)
    @jakarta.annotation.Nonnull
    private String content;

    public static final String SERIALIZED_NAME_TOOL_USE_ID = "tool_use_id";

    @SerializedName(SERIALIZED_NAME_TOOL_USE_ID)
    @jakarta.annotation.Nullable private String toolUseId;

    public static final String SERIALIZED_NAME_THOUGHT_SIGNATURE = "thought_signature";

    @SerializedName(SERIALIZED_NAME_THOUGHT_SIGNATURE)
    @jakarta.annotation.Nullable private String thoughtSignature;

    public static final String SERIALIZED_NAME_FILES = "files";

    @SerializedName(SERIALIZED_NAME_FILES)
    @jakarta.annotation.Nullable private List<DataSource> files = new ArrayList<>();

    public PromptSegment() {}

    public PromptSegment role(@jakarta.annotation.Nonnull PromptRole role) {
        this.role = role;
        return this;
    }

    /**
     * Get role
     * @return role
     */
    @jakarta.annotation.Nonnull
    public PromptRole getRole() {
        return role;
    }

    public void setRole(@jakarta.annotation.Nonnull PromptRole role) {
        this.role = role;
    }

    public PromptSegment content(@jakarta.annotation.Nonnull String content) {
        this.content = content;
        return this;
    }

    /**
     * Get content
     * @return content
     */
    @jakarta.annotation.Nonnull
    public String getContent() {
        return content;
    }

    public void setContent(@jakarta.annotation.Nonnull String content) {
        this.content = content;
    }

    public PromptSegment toolUseId(@jakarta.annotation.Nullable String toolUseId) {
        this.toolUseId = toolUseId;
        return this;
    }

    /**
     * The tool use id if the segment is a tool response
     * @return toolUseId
     */
    @jakarta.annotation.Nullable public String getToolUseId() {
        return toolUseId;
    }

    public void setToolUseId(@jakarta.annotation.Nullable String toolUseId) {
        this.toolUseId = toolUseId;
    }

    public PromptSegment thoughtSignature(@jakarta.annotation.Nullable String thoughtSignature) {
        this.thoughtSignature = thoughtSignature;
        return this;
    }

    /**
     * Gemini thinking models require thought_signature to be passed back with tool results. This should be copied from the ToolUse.thought_signature when sending tool responses.
     * @return thoughtSignature
     */
    @jakarta.annotation.Nullable public String getThoughtSignature() {
        return thoughtSignature;
    }

    public void setThoughtSignature(@jakarta.annotation.Nullable String thoughtSignature) {
        this.thoughtSignature = thoughtSignature;
    }

    public PromptSegment files(@jakarta.annotation.Nullable List<DataSource> files) {
        this.files = files;
        return this;
    }

    public PromptSegment addFilesItem(DataSource filesItem) {
        if (this.files == null) {
            this.files = new ArrayList<>();
        }
        this.files.add(filesItem);
        return this;
    }

    /**
     * Get files
     * @return files
     */
    @jakarta.annotation.Nullable public List<DataSource> getFiles() {
        return files;
    }

    public void setFiles(@jakarta.annotation.Nullable List<DataSource> files) {
        this.files = files;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        PromptSegment promptSegment = (PromptSegment) o;
        return Objects.equals(this.role, promptSegment.role)
                && Objects.equals(this.content, promptSegment.content)
                && Objects.equals(this.toolUseId, promptSegment.toolUseId)
                && Objects.equals(this.thoughtSignature, promptSegment.thoughtSignature)
                && Objects.equals(this.files, promptSegment.files);
    }

    @Override
    public int hashCode() {
        return Objects.hash(role, content, toolUseId, thoughtSignature, files);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class PromptSegment {\n");
        sb.append("    role: ").append(toIndentedString(role)).append("\n");
        sb.append("    content: ").append(toIndentedString(content)).append("\n");
        sb.append("    toolUseId: ").append(toIndentedString(toolUseId)).append("\n");
        sb.append("    thoughtSignature: ").append(toIndentedString(thoughtSignature)).append("\n");
        sb.append("    files: ").append(toIndentedString(files)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields =
                new HashSet<String>(
                        Arrays.asList(
                                "role", "content", "tool_use_id", "thought_signature", "files"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(Arrays.asList("role", "content"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to PromptSegment
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!PromptSegment.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in PromptSegment is not found in the empty JSON string",
                                PromptSegment.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : PromptSegment.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // validate the required field `role`
        PromptRole.validateJsonElement(jsonObj.get("role"));
        if (!jsonObj.get("content").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `content` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("content").toString()));
        }
        if ((jsonObj.get("tool_use_id") != null && !jsonObj.get("tool_use_id").isJsonNull())
                && !jsonObj.get("tool_use_id").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `tool_use_id` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("tool_use_id").toString()));
        }
        if ((jsonObj.get("thought_signature") != null
                        && !jsonObj.get("thought_signature").isJsonNull())
                && !jsonObj.get("thought_signature").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `thought_signature` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("thought_signature").toString()));
        }
        if (jsonObj.get("files") != null && !jsonObj.get("files").isJsonNull()) {
            JsonArray jsonArrayfiles = jsonObj.getAsJsonArray("files");
            if (jsonArrayfiles != null) {
                // ensure the json data is an array
                if (!jsonObj.get("files").isJsonArray()) {
                    throw new IllegalArgumentException(
                            String.format(
                                    java.util.Locale.ROOT,
                                    "Expected the field `files` to be an array in the JSON string but got `%s`",
                                    jsonObj.get("files").toString()));
                }

                // validate the optional field `files` (array)
                for (int i = 0; i < jsonArrayfiles.size(); i++) {
                    DataSource.validateJsonElement(jsonArrayfiles.get(i));
                }
                ;
            }
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!PromptSegment.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'PromptSegment' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<PromptSegment> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(PromptSegment.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<PromptSegment>() {
                        @Override
                        public void write(JsonWriter out, PromptSegment value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public PromptSegment read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of PromptSegment given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of PromptSegment
     * @throws IOException if the JSON string is invalid with respect to PromptSegment
     */
    public static PromptSegment fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, PromptSegment.class);
    }

    /**
     * Convert an instance of PromptSegment to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
