/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260725.083715Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashSet;
import java.util.List;
import java.util.Objects;

/**
 * Response from &#x60;POST /tools/validate&#x60;.
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class ValidateToolNamesResponse {
    public static final String SERIALIZED_NAME_RESULTS = "results";

    @SerializedName(SERIALIZED_NAME_RESULTS)
    @jakarta.annotation.Nonnull
    private List<ToolValidationResult> results = new ArrayList<>();

    public static final String SERIALIZED_NAME_INVALID_COUNT = "invalid_count";

    @SerializedName(SERIALIZED_NAME_INVALID_COUNT)
    @jakarta.annotation.Nonnull
    private Integer invalidCount;

    public ValidateToolNamesResponse() {}

    public ValidateToolNamesResponse results(
            @jakarta.annotation.Nonnull List<ToolValidationResult> results) {
        this.results = results;
        return this;
    }

    public ValidateToolNamesResponse addResultsItem(ToolValidationResult resultsItem) {
        if (this.results == null) {
            this.results = new ArrayList<>();
        }
        this.results.add(resultsItem);
        return this;
    }

    /**
     * One entry per requested name, in the same order.
     * @return results
     */
    @jakarta.annotation.Nonnull
    public List<ToolValidationResult> getResults() {
        return results;
    }

    public void setResults(@jakarta.annotation.Nonnull List<ToolValidationResult> results) {
        this.results = results;
    }

    public ValidateToolNamesResponse invalidCount(
            @jakarta.annotation.Nonnull Integer invalidCount) {
        this.invalidCount = invalidCount;
        return this;
    }

    /**
     * Convenience count of &#x60;results.filter(r &#x3D;&gt; !r.valid).length&#x60;.
     * minimum: 0
     * @return invalidCount
     */
    @jakarta.annotation.Nonnull
    public Integer getInvalidCount() {
        return invalidCount;
    }

    public void setInvalidCount(@jakarta.annotation.Nonnull Integer invalidCount) {
        this.invalidCount = invalidCount;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ValidateToolNamesResponse validateToolNamesResponse = (ValidateToolNamesResponse) o;
        return Objects.equals(this.results, validateToolNamesResponse.results)
                && Objects.equals(this.invalidCount, validateToolNamesResponse.invalidCount);
    }

    @Override
    public int hashCode() {
        return Objects.hash(results, invalidCount);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class ValidateToolNamesResponse {\n");
        sb.append("    results: ").append(toIndentedString(results)).append("\n");
        sb.append("    invalidCount: ").append(toIndentedString(invalidCount)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>(Arrays.asList("results", "invalid_count"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(Arrays.asList("results", "invalid_count"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to ValidateToolNamesResponse
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!ValidateToolNamesResponse.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in ValidateToolNamesResponse is not found in the empty JSON string",
                                ValidateToolNamesResponse.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : ValidateToolNamesResponse.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (jsonObj.get("results") != null) {
            if (!jsonObj.get("results").isJsonArray()) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "Expected the field `results` to be an array in the JSON string but got `%s`",
                                jsonObj.get("results").toString()));
            }
            JsonArray jsonArrayresults = jsonObj.getAsJsonArray("results");
            // validate the required field `results` (array)
            for (int i = 0; i < jsonArrayresults.size(); i++) {
                ToolValidationResult.validateJsonElement(jsonArrayresults.get(i));
            }
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ValidateToolNamesResponse.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'ValidateToolNamesResponse' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ValidateToolNamesResponse> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(ValidateToolNamesResponse.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<ValidateToolNamesResponse>() {
                        @Override
                        public void write(JsonWriter out, ValidateToolNamesResponse value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public ValidateToolNamesResponse read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of ValidateToolNamesResponse given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of ValidateToolNamesResponse
     * @throws IOException if the JSON string is invalid with respect to ValidateToolNamesResponse
     */
    public static ValidateToolNamesResponse fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, ValidateToolNamesResponse.class);
    }

    /**
     * Convert an instance of ValidateToolNamesResponse to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
