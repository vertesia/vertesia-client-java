/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260826.064249Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.JsonElement;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

/**
 * Access control policy for an app installation. Declares which access surfaces are gated by per-user ACEs.  - &#39;all&#39; (default): every surface (UI portal, tool/endpoint use, contributions) requires   an explicit app_member ACE — the historical behavior. - &#39;ui&#39;: UI portal visibility requires an ACE, but tool/endpoint use and contributions   are open to anyone in the project. - &#39;none&#39;: fully open within the project — no ACE required for any surface.  Declared on the manifest as the app&#39;s default. May be overridden per-installation.
 */
@JsonAdapter(AppAccessControl.Adapter.class)
public enum AppAccessControl {
    ALL("all"),

    UI("ui"),

    NONE("none"),

    UNKNOWN_DEFAULT_OPEN_API("unknown_default_open_api");

    private String value;

    AppAccessControl(String value) {
        this.value = value;
    }

    public String getValue() {
        return value;
    }

    @Override
    public String toString() {
        return String.valueOf(value);
    }

    public static AppAccessControl fromValue(String value) {
        for (AppAccessControl b : AppAccessControl.values()) {
            if (b.value.equals(value)) {
                return b;
            }
        }
        return UNKNOWN_DEFAULT_OPEN_API;
    }

    public static class Adapter extends TypeAdapter<AppAccessControl> {
        @Override
        public void write(final JsonWriter jsonWriter, final AppAccessControl enumeration)
                throws IOException {
            jsonWriter.value(enumeration.getValue());
        }

        @Override
        public AppAccessControl read(final JsonReader jsonReader) throws IOException {
            String value = jsonReader.nextString();
            return AppAccessControl.fromValue(value);
        }
    }

    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        String value = jsonElement.getAsString();
        AppAccessControl.fromValue(value);
    }
}
