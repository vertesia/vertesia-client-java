/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260901.005903Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashSet;
import java.util.List;
import java.util.Objects;

/**
 * Custom configuration for virtual environments
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class LoadBalancingEnvConfig {
    public static final String SERIALIZED_NAME_ENTRIES = "entries";

    @SerializedName(SERIALIZED_NAME_ENTRIES)
    @jakarta.annotation.Nullable private List<LoadBalancingEnvEntryConfig> entries = new ArrayList<>();

    public static final String SERIALIZED_NAME_FAILOVER = "failover";

    @SerializedName(SERIALIZED_NAME_FAILOVER)
    @jakarta.annotation.Nullable private Boolean failover;

    public LoadBalancingEnvConfig() {}

    public LoadBalancingEnvConfig entries(
            @jakarta.annotation.Nullable List<LoadBalancingEnvEntryConfig> entries) {
        this.entries = entries;
        return this;
    }

    public LoadBalancingEnvConfig addEntriesItem(LoadBalancingEnvEntryConfig entriesItem) {
        if (this.entries == null) {
            this.entries = new ArrayList<>();
        }
        this.entries.add(entriesItem);
        return this;
    }

    /**
     * Get entries
     * @return entries
     */
    @jakarta.annotation.Nullable public List<LoadBalancingEnvEntryConfig> getEntries() {
        return entries;
    }

    public void setEntries(@jakarta.annotation.Nullable List<LoadBalancingEnvEntryConfig> entries) {
        this.entries = entries;
    }

    public LoadBalancingEnvConfig failover(@jakarta.annotation.Nullable Boolean failover) {
        this.failover = failover;
        return this;
    }

    /**
     * Get failover
     * @return failover
     */
    @jakarta.annotation.Nullable public Boolean getFailover() {
        return failover;
    }

    public void setFailover(@jakarta.annotation.Nullable Boolean failover) {
        this.failover = failover;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        LoadBalancingEnvConfig loadBalancingEnvConfig = (LoadBalancingEnvConfig) o;
        return Objects.equals(this.entries, loadBalancingEnvConfig.entries)
                && Objects.equals(this.failover, loadBalancingEnvConfig.failover);
    }

    @Override
    public int hashCode() {
        return Objects.hash(entries, failover);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class LoadBalancingEnvConfig {\n");
        sb.append("    entries: ").append(toIndentedString(entries)).append("\n");
        sb.append("    failover: ").append(toIndentedString(failover)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>(Arrays.asList("entries", "failover"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(0);
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to LoadBalancingEnvConfig
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!LoadBalancingEnvConfig.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in LoadBalancingEnvConfig is not found in the empty JSON string",
                                LoadBalancingEnvConfig.openapiRequiredFields.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (jsonObj.get("entries") != null && !jsonObj.get("entries").isJsonNull()) {
            JsonArray jsonArrayentries = jsonObj.getAsJsonArray("entries");
            if (jsonArrayentries != null) {
                // ensure the json data is an array
                if (!jsonObj.get("entries").isJsonArray()) {
                    throw new IllegalArgumentException(
                            String.format(
                                    java.util.Locale.ROOT,
                                    "Expected the field `entries` to be an array in the JSON string but got `%s`",
                                    jsonObj.get("entries").toString()));
                }

                // validate the optional field `entries` (array)
                for (int i = 0; i < jsonArrayentries.size(); i++) {
                    LoadBalancingEnvEntryConfig.validateJsonElement(jsonArrayentries.get(i));
                }
                ;
            }
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!LoadBalancingEnvConfig.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'LoadBalancingEnvConfig' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<LoadBalancingEnvConfig> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(LoadBalancingEnvConfig.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<LoadBalancingEnvConfig>() {
                        @Override
                        public void write(JsonWriter out, LoadBalancingEnvConfig value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public LoadBalancingEnvConfig read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of LoadBalancingEnvConfig given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of LoadBalancingEnvConfig
     * @throws IOException if the JSON string is invalid with respect to LoadBalancingEnvConfig
     */
    public static LoadBalancingEnvConfig fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, LoadBalancingEnvConfig.class);
    }

    /**
     * Convert an instance of LoadBalancingEnvConfig to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
