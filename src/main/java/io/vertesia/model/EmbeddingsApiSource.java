/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.4.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.Arrays;
import java.util.HashSet;
import java.util.Objects;

/**
 * EmbeddingsApiSource
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class EmbeddingsApiSource {
    public static final String SERIALIZED_NAME_NAME = "name";

    @SerializedName(SERIALIZED_NAME_NAME)
    @jakarta.annotation.Nullable private String name;

    public static final String SERIALIZED_NAME_MIME_TYPE = "mime_type";

    @SerializedName(SERIALIZED_NAME_MIME_TYPE)
    @jakarta.annotation.Nullable private String mimeType;

    public static final String SERIALIZED_NAME_URL = "url";

    @SerializedName(SERIALIZED_NAME_URL)
    @jakarta.annotation.Nullable private String url;

    public static final String SERIALIZED_NAME_BASE64 = "base64";

    @SerializedName(SERIALIZED_NAME_BASE64)
    @jakarta.annotation.Nullable private String base64;

    public EmbeddingsApiSource() {}

    public EmbeddingsApiSource name(@jakarta.annotation.Nullable String name) {
        this.name = name;
        return this;
    }

    /**
     * Display name for the source (defaults to \&quot;embedding-input\&quot;).
     * @return name
     */
    @jakarta.annotation.Nullable public String getName() {
        return name;
    }

    public void setName(@jakarta.annotation.Nullable String name) {
        this.name = name;
    }

    public EmbeddingsApiSource mimeType(@jakarta.annotation.Nullable String mimeType) {
        this.mimeType = mimeType;
        return this;
    }

    /**
     * MIME type of the binary content. Required for most providers.
     * @return mimeType
     */
    @jakarta.annotation.Nullable public String getMimeType() {
        return mimeType;
    }

    public void setMimeType(@jakarta.annotation.Nullable String mimeType) {
        this.mimeType = mimeType;
    }

    public EmbeddingsApiSource url(@jakarta.annotation.Nullable String url) {
        this.url = url;
        return this;
    }

    /**
     * Provider-native URL the driver may pass through directly: - gs:// or https://storage.googleapis.com/ for Vertex AI - s3:// or https://_*.amazonaws.com for Bedrock - https:// for fetch fallback Mutually exclusive with base64.
     * @return url
     */
    @jakarta.annotation.Nullable public String getUrl() {
        return url;
    }

    public void setUrl(@jakarta.annotation.Nullable String url) {
        this.url = url;
    }

    public EmbeddingsApiSource base64(@jakarta.annotation.Nullable String base64) {
        this.base64 = base64;
        return this;
    }

    /**
     * Base64-encoded bytes. Mutually exclusive with url.
     * @return base64
     */
    @jakarta.annotation.Nullable public String getBase64() {
        return base64;
    }

    public void setBase64(@jakarta.annotation.Nullable String base64) {
        this.base64 = base64;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        EmbeddingsApiSource embeddingsApiSource = (EmbeddingsApiSource) o;
        return Objects.equals(this.name, embeddingsApiSource.name)
                && Objects.equals(this.mimeType, embeddingsApiSource.mimeType)
                && Objects.equals(this.url, embeddingsApiSource.url)
                && Objects.equals(this.base64, embeddingsApiSource.base64);
    }

    @Override
    public int hashCode() {
        return Objects.hash(name, mimeType, url, base64);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class EmbeddingsApiSource {\n");
        sb.append("    name: ").append(toIndentedString(name)).append("\n");
        sb.append("    mimeType: ").append(toIndentedString(mimeType)).append("\n");
        sb.append("    url: ").append(toIndentedString(url)).append("\n");
        sb.append("    base64: ").append(toIndentedString(base64)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>(Arrays.asList("name", "mime_type", "url", "base64"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(0);
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to EmbeddingsApiSource
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!EmbeddingsApiSource.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in EmbeddingsApiSource is not found in the empty JSON string",
                                EmbeddingsApiSource.openapiRequiredFields.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if ((jsonObj.get("name") != null && !jsonObj.get("name").isJsonNull())
                && !jsonObj.get("name").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `name` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("name").toString()));
        }
        if ((jsonObj.get("mime_type") != null && !jsonObj.get("mime_type").isJsonNull())
                && !jsonObj.get("mime_type").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `mime_type` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("mime_type").toString()));
        }
        if ((jsonObj.get("url") != null && !jsonObj.get("url").isJsonNull())
                && !jsonObj.get("url").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `url` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("url").toString()));
        }
        if ((jsonObj.get("base64") != null && !jsonObj.get("base64").isJsonNull())
                && !jsonObj.get("base64").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `base64` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("base64").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!EmbeddingsApiSource.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'EmbeddingsApiSource' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<EmbeddingsApiSource> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(EmbeddingsApiSource.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<EmbeddingsApiSource>() {
                        @Override
                        public void write(JsonWriter out, EmbeddingsApiSource value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public EmbeddingsApiSource read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of EmbeddingsApiSource given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of EmbeddingsApiSource
     * @throws IOException if the JSON string is invalid with respect to EmbeddingsApiSource
     */
    public static EmbeddingsApiSource fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, EmbeddingsApiSource.class);
    }

    /**
     * Convert an instance of EmbeddingsApiSource to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
