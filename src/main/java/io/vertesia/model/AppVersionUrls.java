/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260901.052906Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.Arrays;
import java.util.HashSet;
import java.util.Objects;

/**
 * AppVersionUrls
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class AppVersionUrls {
    public static final String SERIALIZED_NAME_LIVE_URL = "live_url";

    @SerializedName(SERIALIZED_NAME_LIVE_URL)
    @jakarta.annotation.Nullable private String liveUrl;

    public static final String SERIALIZED_NAME_APP_URL = "app_url";

    @SerializedName(SERIALIZED_NAME_APP_URL)
    @jakarta.annotation.Nullable private String appUrl;

    public static final String SERIALIZED_NAME_PLUGIN_URL = "plugin_url";

    @SerializedName(SERIALIZED_NAME_PLUGIN_URL)
    @jakarta.annotation.Nullable private String pluginUrl;

    public static final String SERIALIZED_NAME_PACKAGE_URL = "package_url";

    @SerializedName(SERIALIZED_NAME_PACKAGE_URL)
    @jakarta.annotation.Nullable private String packageUrl;

    public static final String SERIALIZED_NAME_INTERNAL_PREVIEW_URL = "internal_preview_url";

    @SerializedName(SERIALIZED_NAME_INTERNAL_PREVIEW_URL)
    @jakarta.annotation.Nullable private String internalPreviewUrl;

    public AppVersionUrls() {}

    public AppVersionUrls liveUrl(@jakarta.annotation.Nullable String liveUrl) {
        this.liveUrl = liveUrl;
        return this;
    }

    /**
     * Get liveUrl
     * @return liveUrl
     */
    @jakarta.annotation.Nullable public String getLiveUrl() {
        return liveUrl;
    }

    public void setLiveUrl(@jakarta.annotation.Nullable String liveUrl) {
        this.liveUrl = liveUrl;
    }

    public AppVersionUrls appUrl(@jakarta.annotation.Nullable String appUrl) {
        this.appUrl = appUrl;
        return this;
    }

    /**
     * Get appUrl
     * @return appUrl
     */
    @jakarta.annotation.Nullable public String getAppUrl() {
        return appUrl;
    }

    public void setAppUrl(@jakarta.annotation.Nullable String appUrl) {
        this.appUrl = appUrl;
    }

    public AppVersionUrls pluginUrl(@jakarta.annotation.Nullable String pluginUrl) {
        this.pluginUrl = pluginUrl;
        return this;
    }

    /**
     * Get pluginUrl
     * @return pluginUrl
     */
    @jakarta.annotation.Nullable public String getPluginUrl() {
        return pluginUrl;
    }

    public void setPluginUrl(@jakarta.annotation.Nullable String pluginUrl) {
        this.pluginUrl = pluginUrl;
    }

    public AppVersionUrls packageUrl(@jakarta.annotation.Nullable String packageUrl) {
        this.packageUrl = packageUrl;
        return this;
    }

    /**
     * Get packageUrl
     * @return packageUrl
     */
    @jakarta.annotation.Nullable public String getPackageUrl() {
        return packageUrl;
    }

    public void setPackageUrl(@jakarta.annotation.Nullable String packageUrl) {
        this.packageUrl = packageUrl;
    }

    public AppVersionUrls internalPreviewUrl(
            @jakarta.annotation.Nullable String internalPreviewUrl) {
        this.internalPreviewUrl = internalPreviewUrl;
        return this;
    }

    /**
     * Get internalPreviewUrl
     * @return internalPreviewUrl
     */
    @jakarta.annotation.Nullable public String getInternalPreviewUrl() {
        return internalPreviewUrl;
    }

    public void setInternalPreviewUrl(@jakarta.annotation.Nullable String internalPreviewUrl) {
        this.internalPreviewUrl = internalPreviewUrl;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        AppVersionUrls appVersionUrls = (AppVersionUrls) o;
        return Objects.equals(this.liveUrl, appVersionUrls.liveUrl)
                && Objects.equals(this.appUrl, appVersionUrls.appUrl)
                && Objects.equals(this.pluginUrl, appVersionUrls.pluginUrl)
                && Objects.equals(this.packageUrl, appVersionUrls.packageUrl)
                && Objects.equals(this.internalPreviewUrl, appVersionUrls.internalPreviewUrl);
    }

    @Override
    public int hashCode() {
        return Objects.hash(liveUrl, appUrl, pluginUrl, packageUrl, internalPreviewUrl);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class AppVersionUrls {\n");
        sb.append("    liveUrl: ").append(toIndentedString(liveUrl)).append("\n");
        sb.append("    appUrl: ").append(toIndentedString(appUrl)).append("\n");
        sb.append("    pluginUrl: ").append(toIndentedString(pluginUrl)).append("\n");
        sb.append("    packageUrl: ").append(toIndentedString(packageUrl)).append("\n");
        sb.append("    internalPreviewUrl: ")
                .append(toIndentedString(internalPreviewUrl))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields =
                new HashSet<String>(
                        Arrays.asList(
                                "live_url",
                                "app_url",
                                "plugin_url",
                                "package_url",
                                "internal_preview_url"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(0);
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to AppVersionUrls
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!AppVersionUrls.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in AppVersionUrls is not found in the empty JSON string",
                                AppVersionUrls.openapiRequiredFields.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if ((jsonObj.get("live_url") != null && !jsonObj.get("live_url").isJsonNull())
                && !jsonObj.get("live_url").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `live_url` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("live_url").toString()));
        }
        if ((jsonObj.get("app_url") != null && !jsonObj.get("app_url").isJsonNull())
                && !jsonObj.get("app_url").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `app_url` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("app_url").toString()));
        }
        if ((jsonObj.get("plugin_url") != null && !jsonObj.get("plugin_url").isJsonNull())
                && !jsonObj.get("plugin_url").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `plugin_url` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("plugin_url").toString()));
        }
        if ((jsonObj.get("package_url") != null && !jsonObj.get("package_url").isJsonNull())
                && !jsonObj.get("package_url").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `package_url` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("package_url").toString()));
        }
        if ((jsonObj.get("internal_preview_url") != null
                        && !jsonObj.get("internal_preview_url").isJsonNull())
                && !jsonObj.get("internal_preview_url").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `internal_preview_url` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("internal_preview_url").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!AppVersionUrls.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'AppVersionUrls' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<AppVersionUrls> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(AppVersionUrls.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<AppVersionUrls>() {
                        @Override
                        public void write(JsonWriter out, AppVersionUrls value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public AppVersionUrls read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of AppVersionUrls given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of AppVersionUrls
     * @throws IOException if the JSON string is invalid with respect to AppVersionUrls
     */
    public static AppVersionUrls fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, AppVersionUrls.class);
    }

    /**
     * Convert an instance of AppVersionUrls to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
