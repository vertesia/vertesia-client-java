/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260901.052906Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.Arrays;
import java.util.HashSet;
import java.util.Objects;

/**
 * Agent runtime configuration, scoped under project configuration so agent settings have one home (&#x60;configuration.agent&#x60;).
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class AgentProjectConfiguration {
    public static final String SERIALIZED_NAME_CHECKPOINT = "checkpoint";

    @SerializedName(SERIALIZED_NAME_CHECKPOINT)
    @jakarta.annotation.Nullable private AgentCheckpointConfiguration checkpoint;

    public AgentProjectConfiguration() {}

    public AgentProjectConfiguration checkpoint(
            @jakarta.annotation.Nullable AgentCheckpointConfiguration checkpoint) {
        this.checkpoint = checkpoint;
        return this;
    }

    /**
     * Conversation checkpoint (context compaction) tuning.
     * @return checkpoint
     */
    @jakarta.annotation.Nullable public AgentCheckpointConfiguration getCheckpoint() {
        return checkpoint;
    }

    public void setCheckpoint(
            @jakarta.annotation.Nullable AgentCheckpointConfiguration checkpoint) {
        this.checkpoint = checkpoint;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        AgentProjectConfiguration agentProjectConfiguration = (AgentProjectConfiguration) o;
        return Objects.equals(this.checkpoint, agentProjectConfiguration.checkpoint);
    }

    @Override
    public int hashCode() {
        return Objects.hash(checkpoint);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class AgentProjectConfiguration {\n");
        sb.append("    checkpoint: ").append(toIndentedString(checkpoint)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>(Arrays.asList("checkpoint"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(0);
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to AgentProjectConfiguration
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!AgentProjectConfiguration.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in AgentProjectConfiguration is not found in the empty JSON string",
                                AgentProjectConfiguration.openapiRequiredFields.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // validate the optional field `checkpoint`
        if (jsonObj.get("checkpoint") != null && !jsonObj.get("checkpoint").isJsonNull()) {
            AgentCheckpointConfiguration.validateJsonElement(jsonObj.get("checkpoint"));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!AgentProjectConfiguration.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'AgentProjectConfiguration' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<AgentProjectConfiguration> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(AgentProjectConfiguration.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<AgentProjectConfiguration>() {
                        @Override
                        public void write(JsonWriter out, AgentProjectConfiguration value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public AgentProjectConfiguration read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of AgentProjectConfiguration given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of AgentProjectConfiguration
     * @throws IOException if the JSON string is invalid with respect to AgentProjectConfiguration
     */
    public static AgentProjectConfiguration fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, AgentProjectConfiguration.class);
    }

    /**
     * Convert an instance of AgentProjectConfiguration to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
