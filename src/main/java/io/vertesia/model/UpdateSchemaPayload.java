/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260905.011013Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashSet;
import java.util.List;
import java.util.Objects;

/**
 * Payload for AI-driven bulk schema updates.
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class UpdateSchemaPayload {
    public static final String SERIALIZED_NAME_DESCRIPTION = "description";

    @SerializedName(SERIALIZED_NAME_DESCRIPTION)
    @jakarta.annotation.Nullable private String description;

    public static final String SERIALIZED_NAME_TABLES = "tables";

    @SerializedName(SERIALIZED_NAME_TABLES)
    @jakarta.annotation.Nullable private List<CreateTablePayload> tables = new ArrayList<>();

    public static final String SERIALIZED_NAME_RELATIONSHIPS = "relationships";

    @SerializedName(SERIALIZED_NAME_RELATIONSHIPS)
    @jakarta.annotation.Nullable private List<DataRelationship> relationships = new ArrayList<>();

    public static final String SERIALIZED_NAME_DROP_TABLES = "drop_tables";

    @SerializedName(SERIALIZED_NAME_DROP_TABLES)
    @jakarta.annotation.Nullable private List<String> dropTables = new ArrayList<>();

    public static final String SERIALIZED_NAME_MESSAGE = "message";

    @SerializedName(SERIALIZED_NAME_MESSAGE)
    @jakarta.annotation.Nonnull
    private String message;

    public UpdateSchemaPayload() {}

    public UpdateSchemaPayload description(@jakarta.annotation.Nullable String description) {
        this.description = description;
        return this;
    }

    /**
     * Updated schema description
     * @return description
     */
    @jakarta.annotation.Nullable public String getDescription() {
        return description;
    }

    public void setDescription(@jakarta.annotation.Nullable String description) {
        this.description = description;
    }

    public UpdateSchemaPayload tables(
            @jakarta.annotation.Nullable List<CreateTablePayload> tables) {
        this.tables = tables;
        return this;
    }

    public UpdateSchemaPayload addTablesItem(CreateTablePayload tablesItem) {
        if (this.tables == null) {
            this.tables = new ArrayList<>();
        }
        this.tables.add(tablesItem);
        return this;
    }

    /**
     * Tables to create or update
     * @return tables
     */
    @jakarta.annotation.Nullable public List<CreateTablePayload> getTables() {
        return tables;
    }

    public void setTables(@jakarta.annotation.Nullable List<CreateTablePayload> tables) {
        this.tables = tables;
    }

    public UpdateSchemaPayload relationships(
            @jakarta.annotation.Nullable List<DataRelationship> relationships) {
        this.relationships = relationships;
        return this;
    }

    public UpdateSchemaPayload addRelationshipsItem(DataRelationship relationshipsItem) {
        if (this.relationships == null) {
            this.relationships = new ArrayList<>();
        }
        this.relationships.add(relationshipsItem);
        return this;
    }

    /**
     * Relationships to set
     * @return relationships
     */
    @jakarta.annotation.Nullable public List<DataRelationship> getRelationships() {
        return relationships;
    }

    public void setRelationships(
            @jakarta.annotation.Nullable List<DataRelationship> relationships) {
        this.relationships = relationships;
    }

    public UpdateSchemaPayload dropTables(@jakarta.annotation.Nullable List<String> dropTables) {
        this.dropTables = dropTables;
        return this;
    }

    public UpdateSchemaPayload addDropTablesItem(String dropTablesItem) {
        if (this.dropTables == null) {
            this.dropTables = new ArrayList<>();
        }
        this.dropTables.add(dropTablesItem);
        return this;
    }

    /**
     * Tables to drop
     * @return dropTables
     */
    @jakarta.annotation.Nullable public List<String> getDropTables() {
        return dropTables;
    }

    public void setDropTables(@jakarta.annotation.Nullable List<String> dropTables) {
        this.dropTables = dropTables;
    }

    public UpdateSchemaPayload message(@jakarta.annotation.Nonnull String message) {
        this.message = message;
        return this;
    }

    /**
     * Commit message (required)
     * @return message
     */
    @jakarta.annotation.Nonnull
    public String getMessage() {
        return message;
    }

    public void setMessage(@jakarta.annotation.Nonnull String message) {
        this.message = message;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        UpdateSchemaPayload updateSchemaPayload = (UpdateSchemaPayload) o;
        return Objects.equals(this.description, updateSchemaPayload.description)
                && Objects.equals(this.tables, updateSchemaPayload.tables)
                && Objects.equals(this.relationships, updateSchemaPayload.relationships)
                && Objects.equals(this.dropTables, updateSchemaPayload.dropTables)
                && Objects.equals(this.message, updateSchemaPayload.message);
    }

    @Override
    public int hashCode() {
        return Objects.hash(description, tables, relationships, dropTables, message);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class UpdateSchemaPayload {\n");
        sb.append("    description: ").append(toIndentedString(description)).append("\n");
        sb.append("    tables: ").append(toIndentedString(tables)).append("\n");
        sb.append("    relationships: ").append(toIndentedString(relationships)).append("\n");
        sb.append("    dropTables: ").append(toIndentedString(dropTables)).append("\n");
        sb.append("    message: ").append(toIndentedString(message)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields =
                new HashSet<String>(
                        Arrays.asList(
                                "description",
                                "tables",
                                "relationships",
                                "drop_tables",
                                "message"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(Arrays.asList("message"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to UpdateSchemaPayload
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!UpdateSchemaPayload.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in UpdateSchemaPayload is not found in the empty JSON string",
                                UpdateSchemaPayload.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : UpdateSchemaPayload.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if ((jsonObj.get("description") != null && !jsonObj.get("description").isJsonNull())
                && !jsonObj.get("description").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `description` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("description").toString()));
        }
        if (jsonObj.get("tables") != null && !jsonObj.get("tables").isJsonNull()) {
            JsonArray jsonArraytables = jsonObj.getAsJsonArray("tables");
            if (jsonArraytables != null) {
                // ensure the json data is an array
                if (!jsonObj.get("tables").isJsonArray()) {
                    throw new IllegalArgumentException(
                            String.format(
                                    java.util.Locale.ROOT,
                                    "Expected the field `tables` to be an array in the JSON string but got `%s`",
                                    jsonObj.get("tables").toString()));
                }

                // validate the optional field `tables` (array)
                for (int i = 0; i < jsonArraytables.size(); i++) {
                    CreateTablePayload.validateJsonElement(jsonArraytables.get(i));
                }
                ;
            }
        }
        if (jsonObj.get("relationships") != null && !jsonObj.get("relationships").isJsonNull()) {
            JsonArray jsonArrayrelationships = jsonObj.getAsJsonArray("relationships");
            if (jsonArrayrelationships != null) {
                // ensure the json data is an array
                if (!jsonObj.get("relationships").isJsonArray()) {
                    throw new IllegalArgumentException(
                            String.format(
                                    java.util.Locale.ROOT,
                                    "Expected the field `relationships` to be an array in the JSON string but got `%s`",
                                    jsonObj.get("relationships").toString()));
                }

                // validate the optional field `relationships` (array)
                for (int i = 0; i < jsonArrayrelationships.size(); i++) {
                    DataRelationship.validateJsonElement(jsonArrayrelationships.get(i));
                }
                ;
            }
        }
        // ensure the optional json data is an array if present
        if (jsonObj.get("drop_tables") != null
                && !jsonObj.get("drop_tables").isJsonNull()
                && !jsonObj.get("drop_tables").isJsonArray()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `drop_tables` to be an array in the JSON string but got `%s`",
                            jsonObj.get("drop_tables").toString()));
        }
        if (!jsonObj.get("message").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `message` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("message").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!UpdateSchemaPayload.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'UpdateSchemaPayload' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<UpdateSchemaPayload> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(UpdateSchemaPayload.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<UpdateSchemaPayload>() {
                        @Override
                        public void write(JsonWriter out, UpdateSchemaPayload value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public UpdateSchemaPayload read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of UpdateSchemaPayload given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of UpdateSchemaPayload
     * @throws IOException if the JSON string is invalid with respect to UpdateSchemaPayload
     */
    public static UpdateSchemaPayload fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, UpdateSchemaPayload.class);
    }

    /**
     * Convert an instance of UpdateSchemaPayload to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
