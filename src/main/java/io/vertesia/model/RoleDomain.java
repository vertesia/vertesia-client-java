/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260901.005903Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.JsonElement;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

/**
 * Logical grouping of roles by the service area that owns them. One domain may declare roles applicable to multiple scopes (e.g. the &#x60;content&#x60; domain owns roles applicable to both &#x60;document&#x60; and &#x60;collection&#x60; scopes). The &#x60;system&#x60; domain owns the built-in foundational roles (currently exposed as &#x60;SystemRoles&#x60;) — registered first so domain partitions cannot shadow them.
 */
@JsonAdapter(RoleDomain.Adapter.class)
public enum RoleDomain {
    SYSTEM("system"),

    CONTENT("content"),

    TASKS("tasks"),

    UNKNOWN_DEFAULT_OPEN_API("unknown_default_open_api");

    private String value;

    RoleDomain(String value) {
        this.value = value;
    }

    public String getValue() {
        return value;
    }

    @Override
    public String toString() {
        return String.valueOf(value);
    }

    public static RoleDomain fromValue(String value) {
        for (RoleDomain b : RoleDomain.values()) {
            if (b.value.equals(value)) {
                return b;
            }
        }
        return UNKNOWN_DEFAULT_OPEN_API;
    }

    public static class Adapter extends TypeAdapter<RoleDomain> {
        @Override
        public void write(final JsonWriter jsonWriter, final RoleDomain enumeration)
                throws IOException {
            jsonWriter.value(enumeration.getValue());
        }

        @Override
        public RoleDomain read(final JsonReader jsonReader) throws IOException {
            String value = jsonReader.nextString();
            return RoleDomain.fromValue(value);
        }
    }

    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        String value = jsonElement.getAsString();
        RoleDomain.fromValue(value);
    }
}
