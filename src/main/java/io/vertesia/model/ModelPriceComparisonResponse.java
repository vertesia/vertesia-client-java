/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260804.124748Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;

/**
 * ModelPriceComparisonResponse
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class ModelPriceComparisonResponse {
    public static final String SERIALIZED_NAME_PRICES = "prices";

    @SerializedName(SERIALIZED_NAME_PRICES)
    @jakarta.annotation.Nonnull
    private List<ModelPriceComparison> prices = new ArrayList<>();

    public static final String SERIALIZED_NAME_EFFECTIVE_RANGE = "effective_range";

    @SerializedName(SERIALIZED_NAME_EFFECTIVE_RANGE)
    @jakarta.annotation.Nonnull
    private CostAnalyticsResponseQueryRange effectiveRange;

    public static final String SERIALIZED_NAME_LIST_PRICE_DATE = "list_price_date";

    @SerializedName(SERIALIZED_NAME_LIST_PRICE_DATE)
    @jakarta.annotation.Nullable private String listPriceDate;

    public ModelPriceComparisonResponse() {}

    public ModelPriceComparisonResponse prices(
            @jakarta.annotation.Nonnull List<ModelPriceComparison> prices) {
        this.prices = prices;
        return this;
    }

    public ModelPriceComparisonResponse addPricesItem(ModelPriceComparison pricesItem) {
        if (this.prices == null) {
            this.prices = new ArrayList<>();
        }
        this.prices.add(pricesItem);
        return this;
    }

    /**
     * Get prices
     * @return prices
     */
    @jakarta.annotation.Nonnull
    public List<ModelPriceComparison> getPrices() {
        return prices;
    }

    public void setPrices(@jakarta.annotation.Nonnull List<ModelPriceComparison> prices) {
        this.prices = prices;
    }

    public ModelPriceComparisonResponse effectiveRange(
            @jakarta.annotation.Nonnull CostAnalyticsResponseQueryRange effectiveRange) {
        this.effectiveRange = effectiveRange;
        return this;
    }

    /**
     * Get effectiveRange
     * @return effectiveRange
     */
    @jakarta.annotation.Nonnull
    public CostAnalyticsResponseQueryRange getEffectiveRange() {
        return effectiveRange;
    }

    public void setEffectiveRange(
            @jakarta.annotation.Nonnull CostAnalyticsResponseQueryRange effectiveRange) {
        this.effectiveRange = effectiveRange;
    }

    public ModelPriceComparisonResponse listPriceDate(
            @jakarta.annotation.Nullable String listPriceDate) {
        this.listPriceDate = listPriceDate;
        return this;
    }

    /**
     * Get listPriceDate
     * @return listPriceDate
     */
    @jakarta.annotation.Nullable public String getListPriceDate() {
        return listPriceDate;
    }

    public void setListPriceDate(@jakarta.annotation.Nullable String listPriceDate) {
        this.listPriceDate = listPriceDate;
    }

    /**
     * A container for additional, undeclared properties.
     * This is a holder for any undeclared properties as specified with
     * the 'additionalProperties' keyword in the OAS document.
     */
    private Map<String, Object> additionalProperties;

    /**
     * Set the additional (undeclared) property with the specified name and value.
     * If the property does not already exist, create it otherwise replace it.
     *
     * @param key name of the property
     * @param value value of the property
     * @return the ModelPriceComparisonResponse instance itself
     */
    public ModelPriceComparisonResponse putAdditionalProperty(String key, Object value) {
        if (this.additionalProperties == null) {
            this.additionalProperties = new HashMap<String, Object>();
        }
        this.additionalProperties.put(key, value);
        return this;
    }

    /**
     * Return the additional (undeclared) property.
     *
     * @return a map of objects
     */
    public Map<String, Object> getAdditionalProperties() {
        return additionalProperties;
    }

    /**
     * Return the additional (undeclared) property with the specified name.
     *
     * @param key name of the property
     * @return an object
     */
    public Object getAdditionalProperty(String key) {
        if (this.additionalProperties == null) {
            return null;
        }
        return this.additionalProperties.get(key);
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ModelPriceComparisonResponse modelPriceComparisonResponse =
                (ModelPriceComparisonResponse) o;
        return Objects.equals(this.prices, modelPriceComparisonResponse.prices)
                && Objects.equals(this.effectiveRange, modelPriceComparisonResponse.effectiveRange)
                && Objects.equals(this.listPriceDate, modelPriceComparisonResponse.listPriceDate)
                && Objects.equals(
                        this.additionalProperties,
                        modelPriceComparisonResponse.additionalProperties);
    }

    @Override
    public int hashCode() {
        return Objects.hash(prices, effectiveRange, listPriceDate, additionalProperties);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class ModelPriceComparisonResponse {\n");
        sb.append("    prices: ").append(toIndentedString(prices)).append("\n");
        sb.append("    effectiveRange: ").append(toIndentedString(effectiveRange)).append("\n");
        sb.append("    listPriceDate: ").append(toIndentedString(listPriceDate)).append("\n");
        sb.append("    additionalProperties: ")
                .append(toIndentedString(additionalProperties))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields =
                new HashSet<String>(Arrays.asList("prices", "effective_range", "list_price_date"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(Arrays.asList("prices", "effective_range"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to ModelPriceComparisonResponse
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!ModelPriceComparisonResponse.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in ModelPriceComparisonResponse is not found in the empty JSON string",
                                ModelPriceComparisonResponse.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : ModelPriceComparisonResponse.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (jsonObj.get("prices") != null) {
            if (!jsonObj.get("prices").isJsonArray()) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "Expected the field `prices` to be an array in the JSON string but got `%s`",
                                jsonObj.get("prices").toString()));
            }
            JsonArray jsonArrayprices = jsonObj.getAsJsonArray("prices");
            // validate the required field `prices` (array)
            for (int i = 0; i < jsonArrayprices.size(); i++) {
                ModelPriceComparison.validateJsonElement(jsonArrayprices.get(i));
            }
        }
        // validate the required field `effective_range`
        CostAnalyticsResponseQueryRange.validateJsonElement(jsonObj.get("effective_range"));
        if ((jsonObj.get("list_price_date") != null && !jsonObj.get("list_price_date").isJsonNull())
                && !jsonObj.get("list_price_date").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `list_price_date` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("list_price_date").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ModelPriceComparisonResponse.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'ModelPriceComparisonResponse' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ModelPriceComparisonResponse> thisAdapter =
                    gson.getDelegateAdapter(
                            this, TypeToken.get(ModelPriceComparisonResponse.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<ModelPriceComparisonResponse>() {
                        @Override
                        public void write(JsonWriter out, ModelPriceComparisonResponse value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            obj.remove("additionalProperties");
                            // serialize additional properties
                            if (value.getAdditionalProperties() != null) {
                                for (Map.Entry<String, Object> entry :
                                        value.getAdditionalProperties().entrySet()) {
                                    if (entry.getValue() instanceof String)
                                        obj.addProperty(entry.getKey(), (String) entry.getValue());
                                    else if (entry.getValue() instanceof Number)
                                        obj.addProperty(entry.getKey(), (Number) entry.getValue());
                                    else if (entry.getValue() instanceof Boolean)
                                        obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                                    else if (entry.getValue() instanceof Character)
                                        obj.addProperty(
                                                entry.getKey(), (Character) entry.getValue());
                                    else {
                                        JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                                        if (jsonElement.isJsonArray()) {
                                            obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                                        } else {
                                            obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                                        }
                                    }
                                }
                            }
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public ModelPriceComparisonResponse read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            JsonObject jsonObj = jsonElement.getAsJsonObject();
                            // store additional fields in the deserialized instance
                            ModelPriceComparisonResponse instance =
                                    thisAdapter.fromJsonTree(jsonObj);
                            for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
                                if (!openapiFields.contains(entry.getKey())) {
                                    if (entry.getValue().isJsonPrimitive()) { // primitive type
                                        if (entry.getValue().getAsJsonPrimitive().isString())
                                            instance.putAdditionalProperty(
                                                    entry.getKey(), entry.getValue().getAsString());
                                        else if (entry.getValue().getAsJsonPrimitive().isNumber())
                                            instance.putAdditionalProperty(
                                                    entry.getKey(), entry.getValue().getAsNumber());
                                        else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                                            instance.putAdditionalProperty(
                                                    entry.getKey(),
                                                    entry.getValue().getAsBoolean());
                                        else
                                            throw new IllegalArgumentException(
                                                    String.format(
                                                            java.util.Locale.ROOT,
                                                            "The field `%s` has unknown primitive type. Value: %s",
                                                            entry.getKey(),
                                                            entry.getValue().toString()));
                                    } else if (entry.getValue().isJsonArray()) {
                                        instance.putAdditionalProperty(
                                                entry.getKey(),
                                                gson.fromJson(entry.getValue(), List.class));
                                    } else { // JSON object
                                        instance.putAdditionalProperty(
                                                entry.getKey(),
                                                gson.fromJson(entry.getValue(), HashMap.class));
                                    }
                                }
                            }
                            return instance;
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of ModelPriceComparisonResponse given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of ModelPriceComparisonResponse
     * @throws IOException if the JSON string is invalid with respect to ModelPriceComparisonResponse
     */
    public static ModelPriceComparisonResponse fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, ModelPriceComparisonResponse.class);
    }

    /**
     * Convert an instance of ModelPriceComparisonResponse to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
