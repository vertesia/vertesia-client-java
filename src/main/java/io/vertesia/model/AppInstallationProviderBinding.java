/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260804.124748Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.Arrays;
import java.util.HashSet;
import java.util.Objects;

/**
 * Binding between a named OAuth provider and the OAuth provider created for it at install time. Stored on AppInstallation so the runtime can resolve the correct OAuth provider for collections that reference a shared provider via MCPToolCollectionObject.oauth_provider.
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class AppInstallationProviderBinding {
    public static final String SERIALIZED_NAME_PROVIDER_KEY = "provider_key";

    @SerializedName(SERIALIZED_NAME_PROVIDER_KEY)
    @jakarta.annotation.Nonnull
    private String providerKey;

    public static final String SERIALIZED_NAME_OAUTH_PROVIDER_ID = "oauth_provider_id";

    @SerializedName(SERIALIZED_NAME_OAUTH_PROVIDER_ID)
    @jakarta.annotation.Nonnull
    private String oauthProviderId;

    public static final String SERIALIZED_NAME_OAUTH_PROVIDER_NAME = "oauth_provider_name";

    @SerializedName(SERIALIZED_NAME_OAUTH_PROVIDER_NAME)
    @jakarta.annotation.Nonnull
    private String oauthProviderName;

    public AppInstallationProviderBinding() {}

    public AppInstallationProviderBinding providerKey(
            @jakarta.annotation.Nonnull String providerKey) {
        this.providerKey = providerKey;
        return this;
    }

    /**
     * Key from AppManifestData.oauth_providers
     * @return providerKey
     */
    @jakarta.annotation.Nonnull
    public String getProviderKey() {
        return providerKey;
    }

    public void setProviderKey(@jakarta.annotation.Nonnull String providerKey) {
        this.providerKey = providerKey;
    }

    public AppInstallationProviderBinding oauthProviderId(
            @jakarta.annotation.Nonnull String oauthProviderId) {
        this.oauthProviderId = oauthProviderId;
        return this;
    }

    /**
     * MongoDB ObjectId of the created OAuth provider
     * @return oauthProviderId
     */
    @jakarta.annotation.Nonnull
    public String getOauthProviderId() {
        return oauthProviderId;
    }

    public void setOauthProviderId(@jakarta.annotation.Nonnull String oauthProviderId) {
        this.oauthProviderId = oauthProviderId;
    }

    public AppInstallationProviderBinding oauthProviderName(
            @jakarta.annotation.Nonnull String oauthProviderName) {
        this.oauthProviderName = oauthProviderName;
        return this;
    }

    /**
     * Name of the OAuth provider at creation time (for audit/display only)
     * @return oauthProviderName
     */
    @jakarta.annotation.Nonnull
    public String getOauthProviderName() {
        return oauthProviderName;
    }

    public void setOauthProviderName(@jakarta.annotation.Nonnull String oauthProviderName) {
        this.oauthProviderName = oauthProviderName;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        AppInstallationProviderBinding appInstallationProviderBinding =
                (AppInstallationProviderBinding) o;
        return Objects.equals(this.providerKey, appInstallationProviderBinding.providerKey)
                && Objects.equals(
                        this.oauthProviderId, appInstallationProviderBinding.oauthProviderId)
                && Objects.equals(
                        this.oauthProviderName, appInstallationProviderBinding.oauthProviderName);
    }

    @Override
    public int hashCode() {
        return Objects.hash(providerKey, oauthProviderId, oauthProviderName);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class AppInstallationProviderBinding {\n");
        sb.append("    providerKey: ").append(toIndentedString(providerKey)).append("\n");
        sb.append("    oauthProviderId: ").append(toIndentedString(oauthProviderId)).append("\n");
        sb.append("    oauthProviderName: ")
                .append(toIndentedString(oauthProviderName))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields =
                new HashSet<String>(
                        Arrays.asList("provider_key", "oauth_provider_id", "oauth_provider_name"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields =
                new HashSet<String>(
                        Arrays.asList("provider_key", "oauth_provider_id", "oauth_provider_name"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to AppInstallationProviderBinding
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!AppInstallationProviderBinding.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in AppInstallationProviderBinding is not found in the empty JSON string",
                                AppInstallationProviderBinding.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : AppInstallationProviderBinding.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (!jsonObj.get("provider_key").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `provider_key` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("provider_key").toString()));
        }
        if (!jsonObj.get("oauth_provider_id").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `oauth_provider_id` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("oauth_provider_id").toString()));
        }
        if (!jsonObj.get("oauth_provider_name").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `oauth_provider_name` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("oauth_provider_name").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!AppInstallationProviderBinding.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'AppInstallationProviderBinding' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<AppInstallationProviderBinding> thisAdapter =
                    gson.getDelegateAdapter(
                            this, TypeToken.get(AppInstallationProviderBinding.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<AppInstallationProviderBinding>() {
                        @Override
                        public void write(JsonWriter out, AppInstallationProviderBinding value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public AppInstallationProviderBinding read(JsonReader in)
                                throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of AppInstallationProviderBinding given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of AppInstallationProviderBinding
     * @throws IOException if the JSON string is invalid with respect to AppInstallationProviderBinding
     */
    public static AppInstallationProviderBinding fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, AppInstallationProviderBinding.class);
    }

    /**
     * Convert an instance of AppInstallationProviderBinding to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
