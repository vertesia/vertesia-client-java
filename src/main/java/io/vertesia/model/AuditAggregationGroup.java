/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260826.064249Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.Arrays;
import java.util.HashSet;
import java.util.Objects;

/**
 * AuditAggregationGroup
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class AuditAggregationGroup {
    public static final String SERIALIZED_NAME_DIMENSION = "dimension";

    @SerializedName(SERIALIZED_NAME_DIMENSION)
    @jakarta.annotation.Nonnull
    private AuditAggregationDimension dimension;

    public static final String SERIALIZED_NAME_RESOLUTION = "resolution";

    @SerializedName(SERIALIZED_NAME_RESOLUTION)
    @jakarta.annotation.Nullable private AuditAggregationResolution resolution;

    public AuditAggregationGroup() {}

    public AuditAggregationGroup dimension(
            @jakarta.annotation.Nonnull AuditAggregationDimension dimension) {
        this.dimension = dimension;
        return this;
    }

    /**
     * Get dimension
     * @return dimension
     */
    @jakarta.annotation.Nonnull
    public AuditAggregationDimension getDimension() {
        return dimension;
    }

    public void setDimension(@jakarta.annotation.Nonnull AuditAggregationDimension dimension) {
        this.dimension = dimension;
    }

    public AuditAggregationGroup resolution(
            @jakarta.annotation.Nullable AuditAggregationResolution resolution) {
        this.resolution = resolution;
        return this;
    }

    /**
     * Required for the time dimension; defaults to day.
     * @return resolution
     */
    @jakarta.annotation.Nullable public AuditAggregationResolution getResolution() {
        return resolution;
    }

    public void setResolution(@jakarta.annotation.Nullable AuditAggregationResolution resolution) {
        this.resolution = resolution;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        AuditAggregationGroup auditAggregationGroup = (AuditAggregationGroup) o;
        return Objects.equals(this.dimension, auditAggregationGroup.dimension)
                && Objects.equals(this.resolution, auditAggregationGroup.resolution);
    }

    @Override
    public int hashCode() {
        return Objects.hash(dimension, resolution);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class AuditAggregationGroup {\n");
        sb.append("    dimension: ").append(toIndentedString(dimension)).append("\n");
        sb.append("    resolution: ").append(toIndentedString(resolution)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>(Arrays.asList("dimension", "resolution"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(Arrays.asList("dimension"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to AuditAggregationGroup
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!AuditAggregationGroup.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in AuditAggregationGroup is not found in the empty JSON string",
                                AuditAggregationGroup.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : AuditAggregationGroup.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // validate the required field `dimension`
        AuditAggregationDimension.validateJsonElement(jsonObj.get("dimension"));
        // validate the optional field `resolution`
        if (jsonObj.get("resolution") != null && !jsonObj.get("resolution").isJsonNull()) {
            AuditAggregationResolution.validateJsonElement(jsonObj.get("resolution"));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!AuditAggregationGroup.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'AuditAggregationGroup' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<AuditAggregationGroup> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(AuditAggregationGroup.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<AuditAggregationGroup>() {
                        @Override
                        public void write(JsonWriter out, AuditAggregationGroup value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public AuditAggregationGroup read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of AuditAggregationGroup given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of AuditAggregationGroup
     * @throws IOException if the JSON string is invalid with respect to AuditAggregationGroup
     */
    public static AuditAggregationGroup fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, AuditAggregationGroup.class);
    }

    /**
     * Convert an instance of AuditAggregationGroup to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
