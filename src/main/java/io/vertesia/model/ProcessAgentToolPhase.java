/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.6.0-dev
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;

/**
 * ProcessAgentToolPhase
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class ProcessAgentToolPhase {
    public static final String SERIALIZED_NAME_ID = "id";

    @SerializedName(SERIALIZED_NAME_ID)
    @jakarta.annotation.Nonnull
    private String id;

    public static final String SERIALIZED_NAME_TOOLS = "tools";

    @SerializedName(SERIALIZED_NAME_TOOLS)
    @jakarta.annotation.Nonnull
    private List<String> tools = new ArrayList<>();

    public static final String SERIALIZED_NAME_CONTINUATION_TOOLS = "continuation_tools";

    @SerializedName(SERIALIZED_NAME_CONTINUATION_TOOLS)
    @jakarta.annotation.Nullable private List<String> continuationTools = new ArrayList<>();

    public static final String SERIALIZED_NAME_RECOVERY_TOOLS = "recovery_tools";

    @SerializedName(SERIALIZED_NAME_RECOVERY_TOOLS)
    @jakarta.annotation.Nullable private List<String> recoveryTools = new ArrayList<>();

    public static final String SERIALIZED_NAME_TOOL_INPUT_CONTAINS = "tool_input_contains";

    @SerializedName(SERIALIZED_NAME_TOOL_INPUT_CONTAINS)
    @jakarta.annotation.Nullable private List<ProcessAgentToolInputContains> toolInputContains = new ArrayList<>();

    public static final String SERIALIZED_NAME_MIN_SUCCESSES = "min_successes";

    @SerializedName(SERIALIZED_NAME_MIN_SUCCESSES)
    @jakarta.annotation.Nullable private Integer minSuccesses;

    public static final String SERIALIZED_NAME_MAX_TOKENS = "max_tokens";

    @SerializedName(SERIALIZED_NAME_MAX_TOKENS)
    @jakarta.annotation.Nullable private Integer maxTokens;

    public static final String SERIALIZED_NAME_RECOVERY_PROMPT = "recovery_prompt";

    @SerializedName(SERIALIZED_NAME_RECOVERY_PROMPT)
    @jakarta.annotation.Nonnull
    private String recoveryPrompt;

    public ProcessAgentToolPhase() {}

    public ProcessAgentToolPhase id(@jakarta.annotation.Nonnull String id) {
        this.id = id;
        return this;
    }

    /**
     * Get id
     * @return id
     */
    @jakarta.annotation.Nonnull
    public String getId() {
        return id;
    }

    public void setId(@jakarta.annotation.Nonnull String id) {
        this.id = id;
    }

    public ProcessAgentToolPhase tools(@jakarta.annotation.Nonnull List<String> tools) {
        this.tools = tools;
        return this;
    }

    public ProcessAgentToolPhase addToolsItem(String toolsItem) {
        if (this.tools == null) {
            this.tools = new ArrayList<>();
        }
        this.tools.add(toolsItem);
        return this;
    }

    /**
     * Any successful tool in this list advances the phase.
     * @return tools
     */
    @jakarta.annotation.Nonnull
    public List<String> getTools() {
        return tools;
    }

    public void setTools(@jakarta.annotation.Nonnull List<String> tools) {
        this.tools = tools;
    }

    public ProcessAgentToolPhase continuationTools(
            @jakarta.annotation.Nullable List<String> continuationTools) {
        this.continuationTools = continuationTools;
        return this;
    }

    public ProcessAgentToolPhase addContinuationToolsItem(String continuationToolsItem) {
        if (this.continuationTools == null) {
            this.continuationTools = new ArrayList<>();
        }
        this.continuationTools.add(continuationToolsItem);
        return this;
    }

    /**
     * Additional tools authorized while the phase is active. Successful calls do not advance the phase.
     * @return continuationTools
     */
    @jakarta.annotation.Nullable public List<String> getContinuationTools() {
        return continuationTools;
    }

    public void setContinuationTools(@jakarta.annotation.Nullable List<String> continuationTools) {
        this.continuationTools = continuationTools;
    }

    public ProcessAgentToolPhase recoveryTools(
            @jakarta.annotation.Nullable List<String> recoveryTools) {
        this.recoveryTools = recoveryTools;
        return this;
    }

    public ProcessAgentToolPhase addRecoveryToolsItem(String recoveryToolsItem) {
        if (this.recoveryTools == null) {
            this.recoveryTools = new ArrayList<>();
        }
        this.recoveryTools.add(recoveryToolsItem);
        return this;
    }

    /**
     * Additional tools authorized only after a matching phase tool fails. Successful calls do not advance the phase.
     * @return recoveryTools
     */
    @jakarta.annotation.Nullable public List<String> getRecoveryTools() {
        return recoveryTools;
    }

    public void setRecoveryTools(@jakarta.annotation.Nullable List<String> recoveryTools) {
        this.recoveryTools = recoveryTools;
    }

    public ProcessAgentToolPhase toolInputContains(
            @jakarta.annotation.Nullable List<ProcessAgentToolInputContains> toolInputContains) {
        this.toolInputContains = toolInputContains;
        return this;
    }

    public ProcessAgentToolPhase addToolInputContainsItem(
            ProcessAgentToolInputContains toolInputContainsItem) {
        if (this.toolInputContains == null) {
            this.toolInputContains = new ArrayList<>();
        }
        this.toolInputContains.add(toolInputContainsItem);
        return this;
    }

    /**
     * All declared top-level string-field presence, absence, and minimum-length checks must match before a tool call is authorized or advances the phase.
     * @return toolInputContains
     */
    @jakarta.annotation.Nullable public List<ProcessAgentToolInputContains> getToolInputContains() {
        return toolInputContains;
    }

    public void setToolInputContains(
            @jakarta.annotation.Nullable List<ProcessAgentToolInputContains> toolInputContains) {
        this.toolInputContains = toolInputContains;
    }

    public ProcessAgentToolPhase minSuccesses(@jakarta.annotation.Nullable Integer minSuccesses) {
        this.minSuccesses = minSuccesses;
        return this;
    }

    /**
     * Successful matching calls required to advance. Defaults to one.
     * minimum: 0
     * maximum: 9007199254740991
     * @return minSuccesses
     */
    @jakarta.annotation.Nullable public Integer getMinSuccesses() {
        return minSuccesses;
    }

    public void setMinSuccesses(@jakarta.annotation.Nullable Integer minSuccesses) {
        this.minSuccesses = minSuccesses;
    }

    public ProcessAgentToolPhase maxTokens(@jakarta.annotation.Nullable Integer maxTokens) {
        this.maxTokens = maxTokens;
        return this;
    }

    /**
     * Optional output-token ceiling for model turns while this action phase is active.
     * minimum: 0
     * maximum: 9007199254740991
     * @return maxTokens
     */
    @jakarta.annotation.Nullable public Integer getMaxTokens() {
        return maxTokens;
    }

    public void setMaxTokens(@jakarta.annotation.Nullable Integer maxTokens) {
        this.maxTokens = maxTokens;
    }

    public ProcessAgentToolPhase recoveryPrompt(@jakarta.annotation.Nonnull String recoveryPrompt) {
        this.recoveryPrompt = recoveryPrompt;
        return this;
    }

    /**
     * Process-authored instruction used when the agent tries to finish during this phase.
     * @return recoveryPrompt
     */
    @jakarta.annotation.Nonnull
    public String getRecoveryPrompt() {
        return recoveryPrompt;
    }

    public void setRecoveryPrompt(@jakarta.annotation.Nonnull String recoveryPrompt) {
        this.recoveryPrompt = recoveryPrompt;
    }

    /**
     * A container for additional, undeclared properties.
     * This is a holder for any undeclared properties as specified with
     * the 'additionalProperties' keyword in the OAS document.
     */
    private Map<String, Object> additionalProperties;

    /**
     * Set the additional (undeclared) property with the specified name and value.
     * If the property does not already exist, create it otherwise replace it.
     *
     * @param key name of the property
     * @param value value of the property
     * @return the ProcessAgentToolPhase instance itself
     */
    public ProcessAgentToolPhase putAdditionalProperty(String key, Object value) {
        if (this.additionalProperties == null) {
            this.additionalProperties = new HashMap<String, Object>();
        }
        this.additionalProperties.put(key, value);
        return this;
    }

    /**
     * Return the additional (undeclared) property.
     *
     * @return a map of objects
     */
    public Map<String, Object> getAdditionalProperties() {
        return additionalProperties;
    }

    /**
     * Return the additional (undeclared) property with the specified name.
     *
     * @param key name of the property
     * @return an object
     */
    public Object getAdditionalProperty(String key) {
        if (this.additionalProperties == null) {
            return null;
        }
        return this.additionalProperties.get(key);
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ProcessAgentToolPhase processAgentToolPhase = (ProcessAgentToolPhase) o;
        return Objects.equals(this.id, processAgentToolPhase.id)
                && Objects.equals(this.tools, processAgentToolPhase.tools)
                && Objects.equals(this.continuationTools, processAgentToolPhase.continuationTools)
                && Objects.equals(this.recoveryTools, processAgentToolPhase.recoveryTools)
                && Objects.equals(this.toolInputContains, processAgentToolPhase.toolInputContains)
                && Objects.equals(this.minSuccesses, processAgentToolPhase.minSuccesses)
                && Objects.equals(this.maxTokens, processAgentToolPhase.maxTokens)
                && Objects.equals(this.recoveryPrompt, processAgentToolPhase.recoveryPrompt)
                && Objects.equals(
                        this.additionalProperties, processAgentToolPhase.additionalProperties);
    }

    @Override
    public int hashCode() {
        return Objects.hash(
                id,
                tools,
                continuationTools,
                recoveryTools,
                toolInputContains,
                minSuccesses,
                maxTokens,
                recoveryPrompt,
                additionalProperties);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class ProcessAgentToolPhase {\n");
        sb.append("    id: ").append(toIndentedString(id)).append("\n");
        sb.append("    tools: ").append(toIndentedString(tools)).append("\n");
        sb.append("    continuationTools: ")
                .append(toIndentedString(continuationTools))
                .append("\n");
        sb.append("    recoveryTools: ").append(toIndentedString(recoveryTools)).append("\n");
        sb.append("    toolInputContains: ")
                .append(toIndentedString(toolInputContains))
                .append("\n");
        sb.append("    minSuccesses: ").append(toIndentedString(minSuccesses)).append("\n");
        sb.append("    maxTokens: ").append(toIndentedString(maxTokens)).append("\n");
        sb.append("    recoveryPrompt: ").append(toIndentedString(recoveryPrompt)).append("\n");
        sb.append("    additionalProperties: ")
                .append(toIndentedString(additionalProperties))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields =
                new HashSet<String>(
                        Arrays.asList(
                                "id",
                                "tools",
                                "continuation_tools",
                                "recovery_tools",
                                "tool_input_contains",
                                "min_successes",
                                "max_tokens",
                                "recovery_prompt"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields =
                new HashSet<String>(Arrays.asList("id", "tools", "recovery_prompt"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to ProcessAgentToolPhase
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!ProcessAgentToolPhase.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in ProcessAgentToolPhase is not found in the empty JSON string",
                                ProcessAgentToolPhase.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : ProcessAgentToolPhase.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (!jsonObj.get("id").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `id` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("id").toString()));
        }
        // ensure the required json array is present
        if (jsonObj.get("tools") == null) {
            throw new IllegalArgumentException(
                    "Expected the field `linkedContent` to be an array in the JSON string but got `null`");
        } else if (!jsonObj.get("tools").isJsonArray()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `tools` to be an array in the JSON string but got `%s`",
                            jsonObj.get("tools").toString()));
        }
        // ensure the optional json data is an array if present
        if (jsonObj.get("continuation_tools") != null
                && !jsonObj.get("continuation_tools").isJsonNull()
                && !jsonObj.get("continuation_tools").isJsonArray()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `continuation_tools` to be an array in the JSON string but got `%s`",
                            jsonObj.get("continuation_tools").toString()));
        }
        // ensure the optional json data is an array if present
        if (jsonObj.get("recovery_tools") != null
                && !jsonObj.get("recovery_tools").isJsonNull()
                && !jsonObj.get("recovery_tools").isJsonArray()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `recovery_tools` to be an array in the JSON string but got `%s`",
                            jsonObj.get("recovery_tools").toString()));
        }
        if (jsonObj.get("tool_input_contains") != null
                && !jsonObj.get("tool_input_contains").isJsonNull()) {
            JsonArray jsonArraytoolInputContains = jsonObj.getAsJsonArray("tool_input_contains");
            if (jsonArraytoolInputContains != null) {
                // ensure the json data is an array
                if (!jsonObj.get("tool_input_contains").isJsonArray()) {
                    throw new IllegalArgumentException(
                            String.format(
                                    java.util.Locale.ROOT,
                                    "Expected the field `tool_input_contains` to be an array in the JSON string but got `%s`",
                                    jsonObj.get("tool_input_contains").toString()));
                }

                // validate the optional field `tool_input_contains` (array)
                for (int i = 0; i < jsonArraytoolInputContains.size(); i++) {
                    ProcessAgentToolInputContains.validateJsonElement(
                            jsonArraytoolInputContains.get(i));
                }
                ;
            }
        }
        if (!jsonObj.get("recovery_prompt").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `recovery_prompt` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("recovery_prompt").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ProcessAgentToolPhase.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'ProcessAgentToolPhase' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ProcessAgentToolPhase> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(ProcessAgentToolPhase.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<ProcessAgentToolPhase>() {
                        @Override
                        public void write(JsonWriter out, ProcessAgentToolPhase value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            obj.remove("additionalProperties");
                            // serialize additional properties
                            if (value.getAdditionalProperties() != null) {
                                for (Map.Entry<String, Object> entry :
                                        value.getAdditionalProperties().entrySet()) {
                                    if (entry.getValue() instanceof String)
                                        obj.addProperty(entry.getKey(), (String) entry.getValue());
                                    else if (entry.getValue() instanceof Number)
                                        obj.addProperty(entry.getKey(), (Number) entry.getValue());
                                    else if (entry.getValue() instanceof Boolean)
                                        obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                                    else if (entry.getValue() instanceof Character)
                                        obj.addProperty(
                                                entry.getKey(), (Character) entry.getValue());
                                    else {
                                        JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                                        if (jsonElement.isJsonArray()) {
                                            obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                                        } else {
                                            obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                                        }
                                    }
                                }
                            }
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public ProcessAgentToolPhase read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            JsonObject jsonObj = jsonElement.getAsJsonObject();
                            // store additional fields in the deserialized instance
                            ProcessAgentToolPhase instance = thisAdapter.fromJsonTree(jsonObj);
                            for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
                                if (!openapiFields.contains(entry.getKey())) {
                                    if (entry.getValue().isJsonPrimitive()) { // primitive type
                                        if (entry.getValue().getAsJsonPrimitive().isString())
                                            instance.putAdditionalProperty(
                                                    entry.getKey(), entry.getValue().getAsString());
                                        else if (entry.getValue().getAsJsonPrimitive().isNumber())
                                            instance.putAdditionalProperty(
                                                    entry.getKey(), entry.getValue().getAsNumber());
                                        else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                                            instance.putAdditionalProperty(
                                                    entry.getKey(),
                                                    entry.getValue().getAsBoolean());
                                        else
                                            throw new IllegalArgumentException(
                                                    String.format(
                                                            java.util.Locale.ROOT,
                                                            "The field `%s` has unknown primitive type. Value: %s",
                                                            entry.getKey(),
                                                            entry.getValue().toString()));
                                    } else if (entry.getValue().isJsonArray()) {
                                        instance.putAdditionalProperty(
                                                entry.getKey(),
                                                gson.fromJson(entry.getValue(), List.class));
                                    } else { // JSON object
                                        instance.putAdditionalProperty(
                                                entry.getKey(),
                                                gson.fromJson(entry.getValue(), HashMap.class));
                                    }
                                }
                            }
                            return instance;
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of ProcessAgentToolPhase given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of ProcessAgentToolPhase
     * @throws IOException if the JSON string is invalid with respect to ProcessAgentToolPhase
     */
    public static ProcessAgentToolPhase fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, ProcessAgentToolPhase.class);
    }

    /**
     * Convert an instance of ProcessAgentToolPhase to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
