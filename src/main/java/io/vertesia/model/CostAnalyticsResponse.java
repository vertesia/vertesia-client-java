/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260804.124748Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;

/**
 * CostAnalyticsResponse
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class CostAnalyticsResponse {
    public static final String SERIALIZED_NAME_SUMMARY = "summary";

    @SerializedName(SERIALIZED_NAME_SUMMARY)
    @jakarta.annotation.Nonnull
    private CostSummary summary;

    public static final String SERIALIZED_NAME_BY_DIMENSION = "by_dimension";

    @SerializedName(SERIALIZED_NAME_BY_DIMENSION)
    @jakarta.annotation.Nonnull
    private List<CostByDimension> byDimension = new ArrayList<>();

    public static final String SERIALIZED_NAME_TIME_SERIES = "time_series";

    @SerializedName(SERIALIZED_NAME_TIME_SERIES)
    @jakarta.annotation.Nonnull
    private List<CostTimeSeriesPoint> timeSeries = new ArrayList<>();

    public static final String SERIALIZED_NAME_PRICING = "pricing";

    @SerializedName(SERIALIZED_NAME_PRICING)
    @jakarta.annotation.Nonnull
    private List<ModelPricing> pricing = new ArrayList<>();

    public static final String SERIALIZED_NAME_QUERY_RANGE = "query_range";

    @SerializedName(SERIALIZED_NAME_QUERY_RANGE)
    @jakarta.annotation.Nonnull
    private CostAnalyticsResponseQueryRange queryRange;

    public static final String SERIALIZED_NAME_CACHED = "cached";

    @SerializedName(SERIALIZED_NAME_CACHED)
    @jakarta.annotation.Nonnull
    private Boolean cached;

    public CostAnalyticsResponse() {}

    public CostAnalyticsResponse summary(@jakarta.annotation.Nonnull CostSummary summary) {
        this.summary = summary;
        return this;
    }

    /**
     * Get summary
     * @return summary
     */
    @jakarta.annotation.Nonnull
    public CostSummary getSummary() {
        return summary;
    }

    public void setSummary(@jakarta.annotation.Nonnull CostSummary summary) {
        this.summary = summary;
    }

    public CostAnalyticsResponse byDimension(
            @jakarta.annotation.Nonnull List<CostByDimension> byDimension) {
        this.byDimension = byDimension;
        return this;
    }

    public CostAnalyticsResponse addByDimensionItem(CostByDimension byDimensionItem) {
        if (this.byDimension == null) {
            this.byDimension = new ArrayList<>();
        }
        this.byDimension.add(byDimensionItem);
        return this;
    }

    /**
     * Get byDimension
     * @return byDimension
     */
    @jakarta.annotation.Nonnull
    public List<CostByDimension> getByDimension() {
        return byDimension;
    }

    public void setByDimension(@jakarta.annotation.Nonnull List<CostByDimension> byDimension) {
        this.byDimension = byDimension;
    }

    public CostAnalyticsResponse timeSeries(
            @jakarta.annotation.Nonnull List<CostTimeSeriesPoint> timeSeries) {
        this.timeSeries = timeSeries;
        return this;
    }

    public CostAnalyticsResponse addTimeSeriesItem(CostTimeSeriesPoint timeSeriesItem) {
        if (this.timeSeries == null) {
            this.timeSeries = new ArrayList<>();
        }
        this.timeSeries.add(timeSeriesItem);
        return this;
    }

    /**
     * Get timeSeries
     * @return timeSeries
     */
    @jakarta.annotation.Nonnull
    public List<CostTimeSeriesPoint> getTimeSeries() {
        return timeSeries;
    }

    public void setTimeSeries(@jakarta.annotation.Nonnull List<CostTimeSeriesPoint> timeSeries) {
        this.timeSeries = timeSeries;
    }

    public CostAnalyticsResponse pricing(@jakarta.annotation.Nonnull List<ModelPricing> pricing) {
        this.pricing = pricing;
        return this;
    }

    public CostAnalyticsResponse addPricingItem(ModelPricing pricingItem) {
        if (this.pricing == null) {
            this.pricing = new ArrayList<>();
        }
        this.pricing.add(pricingItem);
        return this;
    }

    /**
     * Get pricing
     * @return pricing
     */
    @jakarta.annotation.Nonnull
    public List<ModelPricing> getPricing() {
        return pricing;
    }

    public void setPricing(@jakarta.annotation.Nonnull List<ModelPricing> pricing) {
        this.pricing = pricing;
    }

    public CostAnalyticsResponse queryRange(
            @jakarta.annotation.Nonnull CostAnalyticsResponseQueryRange queryRange) {
        this.queryRange = queryRange;
        return this;
    }

    /**
     * Get queryRange
     * @return queryRange
     */
    @jakarta.annotation.Nonnull
    public CostAnalyticsResponseQueryRange getQueryRange() {
        return queryRange;
    }

    public void setQueryRange(
            @jakarta.annotation.Nonnull CostAnalyticsResponseQueryRange queryRange) {
        this.queryRange = queryRange;
    }

    public CostAnalyticsResponse cached(@jakarta.annotation.Nonnull Boolean cached) {
        this.cached = cached;
        return this;
    }

    /**
     * Get cached
     * @return cached
     */
    @jakarta.annotation.Nonnull
    public Boolean getCached() {
        return cached;
    }

    public void setCached(@jakarta.annotation.Nonnull Boolean cached) {
        this.cached = cached;
    }

    /**
     * A container for additional, undeclared properties.
     * This is a holder for any undeclared properties as specified with
     * the 'additionalProperties' keyword in the OAS document.
     */
    private Map<String, Object> additionalProperties;

    /**
     * Set the additional (undeclared) property with the specified name and value.
     * If the property does not already exist, create it otherwise replace it.
     *
     * @param key name of the property
     * @param value value of the property
     * @return the CostAnalyticsResponse instance itself
     */
    public CostAnalyticsResponse putAdditionalProperty(String key, Object value) {
        if (this.additionalProperties == null) {
            this.additionalProperties = new HashMap<String, Object>();
        }
        this.additionalProperties.put(key, value);
        return this;
    }

    /**
     * Return the additional (undeclared) property.
     *
     * @return a map of objects
     */
    public Map<String, Object> getAdditionalProperties() {
        return additionalProperties;
    }

    /**
     * Return the additional (undeclared) property with the specified name.
     *
     * @param key name of the property
     * @return an object
     */
    public Object getAdditionalProperty(String key) {
        if (this.additionalProperties == null) {
            return null;
        }
        return this.additionalProperties.get(key);
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        CostAnalyticsResponse costAnalyticsResponse = (CostAnalyticsResponse) o;
        return Objects.equals(this.summary, costAnalyticsResponse.summary)
                && Objects.equals(this.byDimension, costAnalyticsResponse.byDimension)
                && Objects.equals(this.timeSeries, costAnalyticsResponse.timeSeries)
                && Objects.equals(this.pricing, costAnalyticsResponse.pricing)
                && Objects.equals(this.queryRange, costAnalyticsResponse.queryRange)
                && Objects.equals(this.cached, costAnalyticsResponse.cached)
                && Objects.equals(
                        this.additionalProperties, costAnalyticsResponse.additionalProperties);
    }

    @Override
    public int hashCode() {
        return Objects.hash(
                summary,
                byDimension,
                timeSeries,
                pricing,
                queryRange,
                cached,
                additionalProperties);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class CostAnalyticsResponse {\n");
        sb.append("    summary: ").append(toIndentedString(summary)).append("\n");
        sb.append("    byDimension: ").append(toIndentedString(byDimension)).append("\n");
        sb.append("    timeSeries: ").append(toIndentedString(timeSeries)).append("\n");
        sb.append("    pricing: ").append(toIndentedString(pricing)).append("\n");
        sb.append("    queryRange: ").append(toIndentedString(queryRange)).append("\n");
        sb.append("    cached: ").append(toIndentedString(cached)).append("\n");
        sb.append("    additionalProperties: ")
                .append(toIndentedString(additionalProperties))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields =
                new HashSet<String>(
                        Arrays.asList(
                                "summary",
                                "by_dimension",
                                "time_series",
                                "pricing",
                                "query_range",
                                "cached"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields =
                new HashSet<String>(
                        Arrays.asList(
                                "summary",
                                "by_dimension",
                                "time_series",
                                "pricing",
                                "query_range",
                                "cached"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to CostAnalyticsResponse
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!CostAnalyticsResponse.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in CostAnalyticsResponse is not found in the empty JSON string",
                                CostAnalyticsResponse.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : CostAnalyticsResponse.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // validate the required field `summary`
        CostSummary.validateJsonElement(jsonObj.get("summary"));
        if (jsonObj.get("by_dimension") != null) {
            if (!jsonObj.get("by_dimension").isJsonArray()) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "Expected the field `by_dimension` to be an array in the JSON string but got `%s`",
                                jsonObj.get("by_dimension").toString()));
            }
            JsonArray jsonArraybyDimension = jsonObj.getAsJsonArray("by_dimension");
            // validate the required field `by_dimension` (array)
            for (int i = 0; i < jsonArraybyDimension.size(); i++) {
                CostByDimension.validateJsonElement(jsonArraybyDimension.get(i));
            }
        }
        if (jsonObj.get("time_series") != null) {
            if (!jsonObj.get("time_series").isJsonArray()) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "Expected the field `time_series` to be an array in the JSON string but got `%s`",
                                jsonObj.get("time_series").toString()));
            }
            JsonArray jsonArraytimeSeries = jsonObj.getAsJsonArray("time_series");
            // validate the required field `time_series` (array)
            for (int i = 0; i < jsonArraytimeSeries.size(); i++) {
                CostTimeSeriesPoint.validateJsonElement(jsonArraytimeSeries.get(i));
            }
        }
        if (jsonObj.get("pricing") != null) {
            if (!jsonObj.get("pricing").isJsonArray()) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "Expected the field `pricing` to be an array in the JSON string but got `%s`",
                                jsonObj.get("pricing").toString()));
            }
            JsonArray jsonArraypricing = jsonObj.getAsJsonArray("pricing");
            // validate the required field `pricing` (array)
            for (int i = 0; i < jsonArraypricing.size(); i++) {
                ModelPricing.validateJsonElement(jsonArraypricing.get(i));
            }
        }
        // validate the required field `query_range`
        CostAnalyticsResponseQueryRange.validateJsonElement(jsonObj.get("query_range"));
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!CostAnalyticsResponse.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'CostAnalyticsResponse' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<CostAnalyticsResponse> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(CostAnalyticsResponse.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<CostAnalyticsResponse>() {
                        @Override
                        public void write(JsonWriter out, CostAnalyticsResponse value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            obj.remove("additionalProperties");
                            // serialize additional properties
                            if (value.getAdditionalProperties() != null) {
                                for (Map.Entry<String, Object> entry :
                                        value.getAdditionalProperties().entrySet()) {
                                    if (entry.getValue() instanceof String)
                                        obj.addProperty(entry.getKey(), (String) entry.getValue());
                                    else if (entry.getValue() instanceof Number)
                                        obj.addProperty(entry.getKey(), (Number) entry.getValue());
                                    else if (entry.getValue() instanceof Boolean)
                                        obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                                    else if (entry.getValue() instanceof Character)
                                        obj.addProperty(
                                                entry.getKey(), (Character) entry.getValue());
                                    else {
                                        JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                                        if (jsonElement.isJsonArray()) {
                                            obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                                        } else {
                                            obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                                        }
                                    }
                                }
                            }
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public CostAnalyticsResponse read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            JsonObject jsonObj = jsonElement.getAsJsonObject();
                            // store additional fields in the deserialized instance
                            CostAnalyticsResponse instance = thisAdapter.fromJsonTree(jsonObj);
                            for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
                                if (!openapiFields.contains(entry.getKey())) {
                                    if (entry.getValue().isJsonPrimitive()) { // primitive type
                                        if (entry.getValue().getAsJsonPrimitive().isString())
                                            instance.putAdditionalProperty(
                                                    entry.getKey(), entry.getValue().getAsString());
                                        else if (entry.getValue().getAsJsonPrimitive().isNumber())
                                            instance.putAdditionalProperty(
                                                    entry.getKey(), entry.getValue().getAsNumber());
                                        else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                                            instance.putAdditionalProperty(
                                                    entry.getKey(),
                                                    entry.getValue().getAsBoolean());
                                        else
                                            throw new IllegalArgumentException(
                                                    String.format(
                                                            java.util.Locale.ROOT,
                                                            "The field `%s` has unknown primitive type. Value: %s",
                                                            entry.getKey(),
                                                            entry.getValue().toString()));
                                    } else if (entry.getValue().isJsonArray()) {
                                        instance.putAdditionalProperty(
                                                entry.getKey(),
                                                gson.fromJson(entry.getValue(), List.class));
                                    } else { // JSON object
                                        instance.putAdditionalProperty(
                                                entry.getKey(),
                                                gson.fromJson(entry.getValue(), HashMap.class));
                                    }
                                }
                            }
                            return instance;
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of CostAnalyticsResponse given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of CostAnalyticsResponse
     * @throws IOException if the JSON string is invalid with respect to CostAnalyticsResponse
     */
    public static CostAnalyticsResponse fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, CostAnalyticsResponse.class);
    }

    /**
     * Convert an instance of CostAnalyticsResponse to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
