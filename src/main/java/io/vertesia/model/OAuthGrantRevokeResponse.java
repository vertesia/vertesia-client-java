/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260714.072725Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.math.BigDecimal;
import java.util.Arrays;
import java.util.HashSet;
import java.util.Objects;

/**
 * OAuthGrantRevokeResponse
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class OAuthGrantRevokeResponse {
    public static final String SERIALIZED_NAME_REVOKED_TOKENS = "revoked_tokens";

    @SerializedName(SERIALIZED_NAME_REVOKED_TOKENS)
    @jakarta.annotation.Nonnull
    private BigDecimal revokedTokens;

    public static final String SERIALIZED_NAME_REVOKED_CONSENTS = "revoked_consents";

    @SerializedName(SERIALIZED_NAME_REVOKED_CONSENTS)
    @jakarta.annotation.Nonnull
    private BigDecimal revokedConsents;

    public OAuthGrantRevokeResponse() {}

    public OAuthGrantRevokeResponse revokedTokens(
            @jakarta.annotation.Nonnull BigDecimal revokedTokens) {
        this.revokedTokens = revokedTokens;
        return this;
    }

    /**
     * Get revokedTokens
     * @return revokedTokens
     */
    @jakarta.annotation.Nonnull
    public BigDecimal getRevokedTokens() {
        return revokedTokens;
    }

    public void setRevokedTokens(@jakarta.annotation.Nonnull BigDecimal revokedTokens) {
        this.revokedTokens = revokedTokens;
    }

    public OAuthGrantRevokeResponse revokedConsents(
            @jakarta.annotation.Nonnull BigDecimal revokedConsents) {
        this.revokedConsents = revokedConsents;
        return this;
    }

    /**
     * Get revokedConsents
     * @return revokedConsents
     */
    @jakarta.annotation.Nonnull
    public BigDecimal getRevokedConsents() {
        return revokedConsents;
    }

    public void setRevokedConsents(@jakarta.annotation.Nonnull BigDecimal revokedConsents) {
        this.revokedConsents = revokedConsents;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        OAuthGrantRevokeResponse oauthGrantRevokeResponse = (OAuthGrantRevokeResponse) o;
        return Objects.equals(this.revokedTokens, oauthGrantRevokeResponse.revokedTokens)
                && Objects.equals(this.revokedConsents, oauthGrantRevokeResponse.revokedConsents);
    }

    @Override
    public int hashCode() {
        return Objects.hash(revokedTokens, revokedConsents);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class OAuthGrantRevokeResponse {\n");
        sb.append("    revokedTokens: ").append(toIndentedString(revokedTokens)).append("\n");
        sb.append("    revokedConsents: ").append(toIndentedString(revokedConsents)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>(Arrays.asList("revoked_tokens", "revoked_consents"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields =
                new HashSet<String>(Arrays.asList("revoked_tokens", "revoked_consents"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to OAuthGrantRevokeResponse
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!OAuthGrantRevokeResponse.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in OAuthGrantRevokeResponse is not found in the empty JSON string",
                                OAuthGrantRevokeResponse.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : OAuthGrantRevokeResponse.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!OAuthGrantRevokeResponse.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'OAuthGrantRevokeResponse' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<OAuthGrantRevokeResponse> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(OAuthGrantRevokeResponse.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<OAuthGrantRevokeResponse>() {
                        @Override
                        public void write(JsonWriter out, OAuthGrantRevokeResponse value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public OAuthGrantRevokeResponse read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of OAuthGrantRevokeResponse given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of OAuthGrantRevokeResponse
     * @throws IOException if the JSON string is invalid with respect to OAuthGrantRevokeResponse
     */
    public static OAuthGrantRevokeResponse fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, OAuthGrantRevokeResponse.class);
    }

    /**
     * Convert an instance of OAuthGrantRevokeResponse to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
