/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.3.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.Arrays;
import java.util.HashSet;
import java.util.Objects;

/**
 * Modality-specific default model overrides. These override the base default when specific input modalities are detected.
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class ModalityDefaults {
    public static final String SERIALIZED_NAME_IMAGE = "image";

    @SerializedName(SERIALIZED_NAME_IMAGE)
    @jakarta.annotation.Nullable private ModelDefault image;

    public static final String SERIALIZED_NAME_VIDEO = "video";

    @SerializedName(SERIALIZED_NAME_VIDEO)
    @jakarta.annotation.Nullable private ModelDefault video;

    public ModalityDefaults() {}

    public ModalityDefaults image(@jakarta.annotation.Nullable ModelDefault image) {
        this.image = image;
        return this;
    }

    /**
     * Override for inputs containing images
     * @return image
     */
    @jakarta.annotation.Nullable public ModelDefault getImage() {
        return image;
    }

    public void setImage(@jakarta.annotation.Nullable ModelDefault image) {
        this.image = image;
    }

    public ModalityDefaults video(@jakarta.annotation.Nullable ModelDefault video) {
        this.video = video;
        return this;
    }

    /**
     * Override for inputs containing video (requires video-capable model)
     * @return video
     */
    @jakarta.annotation.Nullable public ModelDefault getVideo() {
        return video;
    }

    public void setVideo(@jakarta.annotation.Nullable ModelDefault video) {
        this.video = video;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ModalityDefaults modalityDefaults = (ModalityDefaults) o;
        return Objects.equals(this.image, modalityDefaults.image)
                && Objects.equals(this.video, modalityDefaults.video);
    }

    @Override
    public int hashCode() {
        return Objects.hash(image, video);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class ModalityDefaults {\n");
        sb.append("    image: ").append(toIndentedString(image)).append("\n");
        sb.append("    video: ").append(toIndentedString(video)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>(Arrays.asList("image", "video"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(0);
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to ModalityDefaults
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!ModalityDefaults.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in ModalityDefaults is not found in the empty JSON string",
                                ModalityDefaults.openapiRequiredFields.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // validate the optional field `image`
        if (jsonObj.get("image") != null && !jsonObj.get("image").isJsonNull()) {
            ModelDefault.validateJsonElement(jsonObj.get("image"));
        }
        // validate the optional field `video`
        if (jsonObj.get("video") != null && !jsonObj.get("video").isJsonNull()) {
            ModelDefault.validateJsonElement(jsonObj.get("video"));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ModalityDefaults.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'ModalityDefaults' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ModalityDefaults> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(ModalityDefaults.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<ModalityDefaults>() {
                        @Override
                        public void write(JsonWriter out, ModalityDefaults value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public ModalityDefaults read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of ModalityDefaults given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of ModalityDefaults
     * @throws IOException if the JSON string is invalid with respect to ModalityDefaults
     */
    public static ModalityDefaults fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, ModalityDefaults.class);
    }

    /**
     * Convert an instance of ModalityDefaults to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
