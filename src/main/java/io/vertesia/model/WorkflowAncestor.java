/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260901.005903Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.math.BigDecimal;
import java.util.Arrays;
import java.util.HashSet;
import java.util.Objects;

/**
 * WorkflowAncestor
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class WorkflowAncestor {
    public static final String SERIALIZED_NAME_RUN_ID = "run_id";

    @SerializedName(SERIALIZED_NAME_RUN_ID)
    @jakarta.annotation.Nonnull
    private String runId;

    public static final String SERIALIZED_NAME_WORKFLOW_ID = "workflow_id";

    @SerializedName(SERIALIZED_NAME_WORKFLOW_ID)
    @jakarta.annotation.Nonnull
    private String workflowId;

    public static final String SERIALIZED_NAME_RUN_DEPTH = "run_depth";

    @SerializedName(SERIALIZED_NAME_RUN_DEPTH)
    @jakarta.annotation.Nonnull
    private BigDecimal runDepth;

    public WorkflowAncestor() {}

    public WorkflowAncestor runId(@jakarta.annotation.Nonnull String runId) {
        this.runId = runId;
        return this;
    }

    /**
     * Get runId
     * @return runId
     */
    @jakarta.annotation.Nonnull
    public String getRunId() {
        return runId;
    }

    public void setRunId(@jakarta.annotation.Nonnull String runId) {
        this.runId = runId;
    }

    public WorkflowAncestor workflowId(@jakarta.annotation.Nonnull String workflowId) {
        this.workflowId = workflowId;
        return this;
    }

    /**
     * Get workflowId
     * @return workflowId
     */
    @jakarta.annotation.Nonnull
    public String getWorkflowId() {
        return workflowId;
    }

    public void setWorkflowId(@jakarta.annotation.Nonnull String workflowId) {
        this.workflowId = workflowId;
    }

    public WorkflowAncestor runDepth(@jakarta.annotation.Nonnull BigDecimal runDepth) {
        this.runDepth = runDepth;
        return this;
    }

    /**
     * the depth of nested parent workflows
     * @return runDepth
     */
    @jakarta.annotation.Nonnull
    public BigDecimal getRunDepth() {
        return runDepth;
    }

    public void setRunDepth(@jakarta.annotation.Nonnull BigDecimal runDepth) {
        this.runDepth = runDepth;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        WorkflowAncestor workflowAncestor = (WorkflowAncestor) o;
        return Objects.equals(this.runId, workflowAncestor.runId)
                && Objects.equals(this.workflowId, workflowAncestor.workflowId)
                && Objects.equals(this.runDepth, workflowAncestor.runDepth);
    }

    @Override
    public int hashCode() {
        return Objects.hash(runId, workflowId, runDepth);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class WorkflowAncestor {\n");
        sb.append("    runId: ").append(toIndentedString(runId)).append("\n");
        sb.append("    workflowId: ").append(toIndentedString(workflowId)).append("\n");
        sb.append("    runDepth: ").append(toIndentedString(runDepth)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>(Arrays.asList("run_id", "workflow_id", "run_depth"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields =
                new HashSet<String>(Arrays.asList("run_id", "workflow_id", "run_depth"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to WorkflowAncestor
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!WorkflowAncestor.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in WorkflowAncestor is not found in the empty JSON string",
                                WorkflowAncestor.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : WorkflowAncestor.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (!jsonObj.get("run_id").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `run_id` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("run_id").toString()));
        }
        if (!jsonObj.get("workflow_id").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `workflow_id` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("workflow_id").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!WorkflowAncestor.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'WorkflowAncestor' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<WorkflowAncestor> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(WorkflowAncestor.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<WorkflowAncestor>() {
                        @Override
                        public void write(JsonWriter out, WorkflowAncestor value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public WorkflowAncestor read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of WorkflowAncestor given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of WorkflowAncestor
     * @throws IOException if the JSON string is invalid with respect to WorkflowAncestor
     */
    public static WorkflowAncestor fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, WorkflowAncestor.class);
    }

    /**
     * Convert an instance of WorkflowAncestor to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
