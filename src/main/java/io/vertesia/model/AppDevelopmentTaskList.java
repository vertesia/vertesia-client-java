/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260826.064249Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashSet;
import java.util.List;
import java.util.Objects;

/**
 * Git-backed development tasks and the branch used for new tasks by default.
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class AppDevelopmentTaskList {
    public static final String SERIALIZED_NAME_DEFAULT_BRANCH = "default_branch";

    @SerializedName(SERIALIZED_NAME_DEFAULT_BRANCH)
    @jakarta.annotation.Nullable private String defaultBranch;

    public static final String SERIALIZED_NAME_TASKS = "tasks";

    @SerializedName(SERIALIZED_NAME_TASKS)
    @jakarta.annotation.Nonnull
    private List<AppDevelopmentTask> tasks = new ArrayList<>();

    public AppDevelopmentTaskList() {}

    public AppDevelopmentTaskList defaultBranch(@jakarta.annotation.Nullable String defaultBranch) {
        this.defaultBranch = defaultBranch;
        return this;
    }

    /**
     * Repository default branch, when resolvable.
     * @return defaultBranch
     */
    @jakarta.annotation.Nullable public String getDefaultBranch() {
        return defaultBranch;
    }

    public void setDefaultBranch(@jakarta.annotation.Nullable String defaultBranch) {
        this.defaultBranch = defaultBranch;
    }

    public AppDevelopmentTaskList tasks(
            @jakarta.annotation.Nonnull List<AppDevelopmentTask> tasks) {
        this.tasks = tasks;
        return this;
    }

    public AppDevelopmentTaskList addTasksItem(AppDevelopmentTask tasksItem) {
        if (this.tasks == null) {
            this.tasks = new ArrayList<>();
        }
        this.tasks.add(tasksItem);
        return this;
    }

    /**
     * Get tasks
     * @return tasks
     */
    @jakarta.annotation.Nonnull
    public List<AppDevelopmentTask> getTasks() {
        return tasks;
    }

    public void setTasks(@jakarta.annotation.Nonnull List<AppDevelopmentTask> tasks) {
        this.tasks = tasks;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        AppDevelopmentTaskList appDevelopmentTaskList = (AppDevelopmentTaskList) o;
        return Objects.equals(this.defaultBranch, appDevelopmentTaskList.defaultBranch)
                && Objects.equals(this.tasks, appDevelopmentTaskList.tasks);
    }

    @Override
    public int hashCode() {
        return Objects.hash(defaultBranch, tasks);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class AppDevelopmentTaskList {\n");
        sb.append("    defaultBranch: ").append(toIndentedString(defaultBranch)).append("\n");
        sb.append("    tasks: ").append(toIndentedString(tasks)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>(Arrays.asList("default_branch", "tasks"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(Arrays.asList("tasks"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to AppDevelopmentTaskList
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!AppDevelopmentTaskList.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in AppDevelopmentTaskList is not found in the empty JSON string",
                                AppDevelopmentTaskList.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : AppDevelopmentTaskList.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if ((jsonObj.get("default_branch") != null && !jsonObj.get("default_branch").isJsonNull())
                && !jsonObj.get("default_branch").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `default_branch` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("default_branch").toString()));
        }
        if (jsonObj.get("tasks") != null) {
            if (!jsonObj.get("tasks").isJsonArray()) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "Expected the field `tasks` to be an array in the JSON string but got `%s`",
                                jsonObj.get("tasks").toString()));
            }
            JsonArray jsonArraytasks = jsonObj.getAsJsonArray("tasks");
            // validate the required field `tasks` (array)
            for (int i = 0; i < jsonArraytasks.size(); i++) {
                AppDevelopmentTask.validateJsonElement(jsonArraytasks.get(i));
            }
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!AppDevelopmentTaskList.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'AppDevelopmentTaskList' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<AppDevelopmentTaskList> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(AppDevelopmentTaskList.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<AppDevelopmentTaskList>() {
                        @Override
                        public void write(JsonWriter out, AppDevelopmentTaskList value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public AppDevelopmentTaskList read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of AppDevelopmentTaskList given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of AppDevelopmentTaskList
     * @throws IOException if the JSON string is invalid with respect to AppDevelopmentTaskList
     */
    public static AppDevelopmentTaskList fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, AppDevelopmentTaskList.class);
    }

    /**
     * Convert an instance of AppDevelopmentTaskList to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
