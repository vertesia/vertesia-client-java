/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260826.064249Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.JsonElement;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

/**
 * Gets or Sets AppPackageScope
 */
@JsonAdapter(AppPackageScope.Adapter.class)
public enum AppPackageScope {
    UI("ui"),

    TOOLS("tools"),

    INTERACTIONS("interactions"),

    TYPES("types"),

    PROCESSES("processes"),

    VIEWS("views"),

    TEMPLATES("templates"),

    DASHBOARDS("dashboards"),

    SETTINGS("settings"),

    WIDGETS("widgets"),

    ACTIVITIES("activities"),

    HOOKS("hooks"),

    SUBSCRIPTIONS("subscriptions"),

    ALL("all"),

    UNKNOWN_DEFAULT_OPEN_API("unknown_default_open_api");

    private String value;

    AppPackageScope(String value) {
        this.value = value;
    }

    public String getValue() {
        return value;
    }

    @Override
    public String toString() {
        return String.valueOf(value);
    }

    public static AppPackageScope fromValue(String value) {
        for (AppPackageScope b : AppPackageScope.values()) {
            if (b.value.equals(value)) {
                return b;
            }
        }
        return UNKNOWN_DEFAULT_OPEN_API;
    }

    public static class Adapter extends TypeAdapter<AppPackageScope> {
        @Override
        public void write(final JsonWriter jsonWriter, final AppPackageScope enumeration)
                throws IOException {
            jsonWriter.value(enumeration.getValue());
        }

        @Override
        public AppPackageScope read(final JsonReader jsonReader) throws IOException {
            String value = jsonReader.nextString();
            return AppPackageScope.fromValue(value);
        }
    }

    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        String value = jsonElement.getAsString();
        AppPackageScope.fromValue(value);
    }
}
