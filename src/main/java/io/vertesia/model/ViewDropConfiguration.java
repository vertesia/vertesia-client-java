/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260826.064249Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashSet;
import java.util.List;
import java.util.Objects;

/**
 * A declarative result-area drop target. Stored JSON supports the built-in upload action only.
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class ViewDropConfiguration {
    /**
     * Gets or Sets handler
     */
    @JsonAdapter(HandlerEnum.Adapter.class)
    public enum HandlerEnum {
        UPLOAD("upload"),

        UNKNOWN_DEFAULT_OPEN_API("unknown_default_open_api");

        private String value;

        HandlerEnum(String value) {
            this.value = value;
        }

        public String getValue() {
            return value;
        }

        @Override
        public String toString() {
            return String.valueOf(value);
        }

        public static HandlerEnum fromValue(String value) {
            for (HandlerEnum b : HandlerEnum.values()) {
                if (b.value.equals(value)) {
                    return b;
                }
            }
            return UNKNOWN_DEFAULT_OPEN_API;
        }

        public static class Adapter extends TypeAdapter<HandlerEnum> {
            @Override
            public void write(final JsonWriter jsonWriter, final HandlerEnum enumeration)
                    throws IOException {
                jsonWriter.value(enumeration.getValue());
            }

            @Override
            public HandlerEnum read(final JsonReader jsonReader) throws IOException {
                String value = jsonReader.nextString();
                return HandlerEnum.fromValue(value);
            }
        }

        public static void validateJsonElement(JsonElement jsonElement) throws IOException {
            String value = jsonElement.getAsString();
            HandlerEnum.fromValue(value);
        }
    }

    public static final String SERIALIZED_NAME_HANDLER = "handler";

    @SerializedName(SERIALIZED_NAME_HANDLER)
    @jakarta.annotation.Nonnull
    private HandlerEnum handler;

    /**
     * Gets or Sets accept
     */
    @JsonAdapter(AcceptEnum.Adapter.class)
    public enum AcceptEnum {
        FILES("files"),

        UNKNOWN_DEFAULT_OPEN_API("unknown_default_open_api");

        private String value;

        AcceptEnum(String value) {
            this.value = value;
        }

        public String getValue() {
            return value;
        }

        @Override
        public String toString() {
            return String.valueOf(value);
        }

        public static AcceptEnum fromValue(String value) {
            for (AcceptEnum b : AcceptEnum.values()) {
                if (b.value.equals(value)) {
                    return b;
                }
            }
            return UNKNOWN_DEFAULT_OPEN_API;
        }

        public static class Adapter extends TypeAdapter<AcceptEnum> {
            @Override
            public void write(final JsonWriter jsonWriter, final AcceptEnum enumeration)
                    throws IOException {
                jsonWriter.value(enumeration.getValue());
            }

            @Override
            public AcceptEnum read(final JsonReader jsonReader) throws IOException {
                String value = jsonReader.nextString();
                return AcceptEnum.fromValue(value);
            }
        }

        public static void validateJsonElement(JsonElement jsonElement) throws IOException {
            String value = jsonElement.getAsString();
            AcceptEnum.fromValue(value);
        }
    }

    public static final String SERIALIZED_NAME_ACCEPT = "accept";

    @SerializedName(SERIALIZED_NAME_ACCEPT)
    @jakarta.annotation.Nullable private List<AcceptEnum> accept = new ArrayList<>();

    public static final String SERIALIZED_NAME_PARAMS = "params";

    @SerializedName(SERIALIZED_NAME_PARAMS)
    @jakarta.annotation.Nullable private ViewUploadDropParameters params;

    public ViewDropConfiguration() {}

    public ViewDropConfiguration handler(@jakarta.annotation.Nonnull HandlerEnum handler) {
        this.handler = handler;
        return this;
    }

    /**
     * Get handler
     * @return handler
     */
    @jakarta.annotation.Nonnull
    public HandlerEnum getHandler() {
        return handler;
    }

    public void setHandler(@jakarta.annotation.Nonnull HandlerEnum handler) {
        this.handler = handler;
    }

    public ViewDropConfiguration accept(@jakarta.annotation.Nullable List<AcceptEnum> accept) {
        this.accept = accept;
        return this;
    }

    public ViewDropConfiguration addAcceptItem(AcceptEnum acceptItem) {
        if (this.accept == null) {
            this.accept = new ArrayList<>();
        }
        this.accept.add(acceptItem);
        return this;
    }

    /**
     * Get accept
     * @return accept
     */
    @jakarta.annotation.Nullable public List<AcceptEnum> getAccept() {
        return accept;
    }

    public void setAccept(@jakarta.annotation.Nullable List<AcceptEnum> accept) {
        this.accept = accept;
    }

    public ViewDropConfiguration params(
            @jakarta.annotation.Nullable ViewUploadDropParameters params) {
        this.params = params;
        return this;
    }

    /**
     * Get params
     * @return params
     */
    @jakarta.annotation.Nullable public ViewUploadDropParameters getParams() {
        return params;
    }

    public void setParams(@jakarta.annotation.Nullable ViewUploadDropParameters params) {
        this.params = params;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ViewDropConfiguration viewDropConfiguration = (ViewDropConfiguration) o;
        return Objects.equals(this.handler, viewDropConfiguration.handler)
                && Objects.equals(this.accept, viewDropConfiguration.accept)
                && Objects.equals(this.params, viewDropConfiguration.params);
    }

    @Override
    public int hashCode() {
        return Objects.hash(handler, accept, params);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class ViewDropConfiguration {\n");
        sb.append("    handler: ").append(toIndentedString(handler)).append("\n");
        sb.append("    accept: ").append(toIndentedString(accept)).append("\n");
        sb.append("    params: ").append(toIndentedString(params)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>(Arrays.asList("handler", "accept", "params"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(Arrays.asList("handler"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to ViewDropConfiguration
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!ViewDropConfiguration.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in ViewDropConfiguration is not found in the empty JSON string",
                                ViewDropConfiguration.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : ViewDropConfiguration.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (!jsonObj.get("handler").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `handler` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("handler").toString()));
        }
        // validate the required field `handler`
        HandlerEnum.validateJsonElement(jsonObj.get("handler"));
        // ensure the optional json data is an array if present
        if (jsonObj.get("accept") != null
                && !jsonObj.get("accept").isJsonNull()
                && !jsonObj.get("accept").isJsonArray()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `accept` to be an array in the JSON string but got `%s`",
                            jsonObj.get("accept").toString()));
        }
        // validate the optional field `params`
        if (jsonObj.get("params") != null && !jsonObj.get("params").isJsonNull()) {
            ViewUploadDropParameters.validateJsonElement(jsonObj.get("params"));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ViewDropConfiguration.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'ViewDropConfiguration' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ViewDropConfiguration> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(ViewDropConfiguration.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<ViewDropConfiguration>() {
                        @Override
                        public void write(JsonWriter out, ViewDropConfiguration value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public ViewDropConfiguration read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of ViewDropConfiguration given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of ViewDropConfiguration
     * @throws IOException if the JSON string is invalid with respect to ViewDropConfiguration
     */
    public static ViewDropConfiguration fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, ViewDropConfiguration.class);
    }

    /**
     * Convert an instance of ViewDropConfiguration to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
