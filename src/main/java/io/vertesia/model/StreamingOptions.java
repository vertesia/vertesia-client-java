/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.4.1
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.Arrays;
import java.util.HashSet;
import java.util.Objects;

/**
 * Streaming-specific options (only needed when stream&#x3D;true)
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class StreamingOptions {
    public static final String SERIALIZED_NAME_REDIS_CHANNEL = "redis_channel";

    @SerializedName(SERIALIZED_NAME_REDIS_CHANNEL)
    @jakarta.annotation.Nonnull
    private String redisChannel;

    public static final String SERIALIZED_NAME_WORKSTREAM_ID = "workstream_id";

    @SerializedName(SERIALIZED_NAME_WORKSTREAM_ID)
    @jakarta.annotation.Nullable private String workstreamId;

    public StreamingOptions() {}

    public StreamingOptions redisChannel(@jakarta.annotation.Nonnull String redisChannel) {
        this.redisChannel = redisChannel;
        return this;
    }

    /**
     * Redis channel to publish streaming chunks to
     * @return redisChannel
     */
    @jakarta.annotation.Nonnull
    public String getRedisChannel() {
        return redisChannel;
    }

    public void setRedisChannel(@jakarta.annotation.Nonnull String redisChannel) {
        this.redisChannel = redisChannel;
    }

    public StreamingOptions workstreamId(@jakarta.annotation.Nullable String workstreamId) {
        this.workstreamId = workstreamId;
        return this;
    }

    /**
     * Optional workstream ID for multi-workstream agents
     * @return workstreamId
     */
    @jakarta.annotation.Nullable public String getWorkstreamId() {
        return workstreamId;
    }

    public void setWorkstreamId(@jakarta.annotation.Nullable String workstreamId) {
        this.workstreamId = workstreamId;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        StreamingOptions streamingOptions = (StreamingOptions) o;
        return Objects.equals(this.redisChannel, streamingOptions.redisChannel)
                && Objects.equals(this.workstreamId, streamingOptions.workstreamId);
    }

    @Override
    public int hashCode() {
        return Objects.hash(redisChannel, workstreamId);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class StreamingOptions {\n");
        sb.append("    redisChannel: ").append(toIndentedString(redisChannel)).append("\n");
        sb.append("    workstreamId: ").append(toIndentedString(workstreamId)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>(Arrays.asList("redis_channel", "workstream_id"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(Arrays.asList("redis_channel"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to StreamingOptions
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!StreamingOptions.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in StreamingOptions is not found in the empty JSON string",
                                StreamingOptions.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : StreamingOptions.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (!jsonObj.get("redis_channel").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `redis_channel` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("redis_channel").toString()));
        }
        if ((jsonObj.get("workstream_id") != null && !jsonObj.get("workstream_id").isJsonNull())
                && !jsonObj.get("workstream_id").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `workstream_id` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("workstream_id").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!StreamingOptions.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'StreamingOptions' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<StreamingOptions> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(StreamingOptions.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<StreamingOptions>() {
                        @Override
                        public void write(JsonWriter out, StreamingOptions value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public StreamingOptions read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of StreamingOptions given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of StreamingOptions
     * @throws IOException if the JSON string is invalid with respect to StreamingOptions
     */
    public static StreamingOptions fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, StreamingOptions.class);
    }

    /**
     * Convert an instance of StreamingOptions to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
