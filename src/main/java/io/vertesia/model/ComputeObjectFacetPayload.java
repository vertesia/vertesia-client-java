/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260826.064249Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashSet;
import java.util.List;
import java.util.Objects;

/**
 * ComputeObjectFacetPayload
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class ComputeObjectFacetPayload {
    public static final String SERIALIZED_NAME_FACETS = "facets";

    @SerializedName(SERIALIZED_NAME_FACETS)
    @jakarta.annotation.Nonnull
    private List<FacetSpec> facets = new ArrayList<>();

    public static final String SERIALIZED_NAME_QUERY = "query";

    @SerializedName(SERIALIZED_NAME_QUERY)
    @jakarta.annotation.Nullable private ComplexSearchQuery query;

    public ComputeObjectFacetPayload() {}

    public ComputeObjectFacetPayload facets(@jakarta.annotation.Nonnull List<FacetSpec> facets) {
        this.facets = facets;
        return this;
    }

    public ComputeObjectFacetPayload addFacetsItem(FacetSpec facetsItem) {
        if (this.facets == null) {
            this.facets = new ArrayList<>();
        }
        this.facets.add(facetsItem);
        return this;
    }

    /**
     * Get facets
     * @return facets
     */
    @jakarta.annotation.Nonnull
    public List<FacetSpec> getFacets() {
        return facets;
    }

    public void setFacets(@jakarta.annotation.Nonnull List<FacetSpec> facets) {
        this.facets = facets;
    }

    public ComputeObjectFacetPayload query(@jakarta.annotation.Nullable ComplexSearchQuery query) {
        this.query = query;
        return this;
    }

    /**
     * Get query
     * @return query
     */
    @jakarta.annotation.Nullable public ComplexSearchQuery getQuery() {
        return query;
    }

    public void setQuery(@jakarta.annotation.Nullable ComplexSearchQuery query) {
        this.query = query;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ComputeObjectFacetPayload computeObjectFacetPayload = (ComputeObjectFacetPayload) o;
        return Objects.equals(this.facets, computeObjectFacetPayload.facets)
                && Objects.equals(this.query, computeObjectFacetPayload.query);
    }

    @Override
    public int hashCode() {
        return Objects.hash(facets, query);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class ComputeObjectFacetPayload {\n");
        sb.append("    facets: ").append(toIndentedString(facets)).append("\n");
        sb.append("    query: ").append(toIndentedString(query)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>(Arrays.asList("facets", "query"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(Arrays.asList("facets"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to ComputeObjectFacetPayload
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!ComputeObjectFacetPayload.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in ComputeObjectFacetPayload is not found in the empty JSON string",
                                ComputeObjectFacetPayload.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : ComputeObjectFacetPayload.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (jsonObj.get("facets") != null) {
            if (!jsonObj.get("facets").isJsonArray()) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "Expected the field `facets` to be an array in the JSON string but got `%s`",
                                jsonObj.get("facets").toString()));
            }
            JsonArray jsonArrayfacets = jsonObj.getAsJsonArray("facets");
            // validate the required field `facets` (array)
            for (int i = 0; i < jsonArrayfacets.size(); i++) {
                FacetSpec.validateJsonElement(jsonArrayfacets.get(i));
            }
        }
        // validate the optional field `query`
        if (jsonObj.get("query") != null && !jsonObj.get("query").isJsonNull()) {
            ComplexSearchQuery.validateJsonElement(jsonObj.get("query"));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ComputeObjectFacetPayload.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'ComputeObjectFacetPayload' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ComputeObjectFacetPayload> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(ComputeObjectFacetPayload.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<ComputeObjectFacetPayload>() {
                        @Override
                        public void write(JsonWriter out, ComputeObjectFacetPayload value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public ComputeObjectFacetPayload read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of ComputeObjectFacetPayload given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of ComputeObjectFacetPayload
     * @throws IOException if the JSON string is invalid with respect to ComputeObjectFacetPayload
     */
    public static ComputeObjectFacetPayload fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, ComputeObjectFacetPayload.class);
    }

    /**
     * Convert an instance of ComputeObjectFacetPayload to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
