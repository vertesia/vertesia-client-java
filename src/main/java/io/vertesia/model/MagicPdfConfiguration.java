/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` SDK for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.2.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashSet;
import java.util.List;
import java.util.Objects;

/**
 * MagicPdfConfiguration
 */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class MagicPdfConfiguration {
    public static final String SERIALIZED_NAME_INTEGRATION = "integration";

    @SerializedName(SERIALIZED_NAME_INTEGRATION)
    @javax.annotation.Nonnull
    private SupportedIntegrationsMagicPdf integration;

    public static final String SERIALIZED_NAME_ENABLED = "enabled";

    @SerializedName(SERIALIZED_NAME_ENABLED)
    @javax.annotation.Nonnull
    private Boolean enabled;

    public static final String SERIALIZED_NAME_DEFAULT_FEATURES = "default_features";

    @SerializedName(SERIALIZED_NAME_DEFAULT_FEATURES)
    @javax.annotation.Nullable private List<String> defaultFeatures = new ArrayList<>();

    public static final String SERIALIZED_NAME_DEFAULT_ZONES = "default_zones";

    @SerializedName(SERIALIZED_NAME_DEFAULT_ZONES)
    @javax.annotation.Nullable private List<String> defaultZones = new ArrayList<>();

    public MagicPdfConfiguration() {}

    public MagicPdfConfiguration integration(
            @javax.annotation.Nonnull SupportedIntegrationsMagicPdf integration) {
        this.integration = integration;
        return this;
    }

    /**
     * Get integration
     * @return integration
     */
    @javax.annotation.Nonnull
    public SupportedIntegrationsMagicPdf getIntegration() {
        return integration;
    }

    public void setIntegration(
            @javax.annotation.Nonnull SupportedIntegrationsMagicPdf integration) {
        this.integration = integration;
    }

    public MagicPdfConfiguration enabled(@javax.annotation.Nonnull Boolean enabled) {
        this.enabled = enabled;
        return this;
    }

    /**
     * Get enabled
     * @return enabled
     */
    @javax.annotation.Nonnull
    public Boolean getEnabled() {
        return enabled;
    }

    public void setEnabled(@javax.annotation.Nonnull Boolean enabled) {
        this.enabled = enabled;
    }

    public MagicPdfConfiguration defaultFeatures(
            @javax.annotation.Nullable List<String> defaultFeatures) {
        this.defaultFeatures = defaultFeatures;
        return this;
    }

    public MagicPdfConfiguration addDefaultFeaturesItem(String defaultFeaturesItem) {
        if (this.defaultFeatures == null) {
            this.defaultFeatures = new ArrayList<>();
        }
        this.defaultFeatures.add(defaultFeaturesItem);
        return this;
    }

    /**
     * Get defaultFeatures
     * @return defaultFeatures
     */
    @javax.annotation.Nullable public List<String> getDefaultFeatures() {
        return defaultFeatures;
    }

    public void setDefaultFeatures(@javax.annotation.Nullable List<String> defaultFeatures) {
        this.defaultFeatures = defaultFeatures;
    }

    public MagicPdfConfiguration defaultZones(
            @javax.annotation.Nullable List<String> defaultZones) {
        this.defaultZones = defaultZones;
        return this;
    }

    public MagicPdfConfiguration addDefaultZonesItem(String defaultZonesItem) {
        if (this.defaultZones == null) {
            this.defaultZones = new ArrayList<>();
        }
        this.defaultZones.add(defaultZonesItem);
        return this;
    }

    /**
     * Get defaultZones
     * @return defaultZones
     */
    @javax.annotation.Nullable public List<String> getDefaultZones() {
        return defaultZones;
    }

    public void setDefaultZones(@javax.annotation.Nullable List<String> defaultZones) {
        this.defaultZones = defaultZones;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        MagicPdfConfiguration magicPdfConfiguration = (MagicPdfConfiguration) o;
        return Objects.equals(this.integration, magicPdfConfiguration.integration)
                && Objects.equals(this.enabled, magicPdfConfiguration.enabled)
                && Objects.equals(this.defaultFeatures, magicPdfConfiguration.defaultFeatures)
                && Objects.equals(this.defaultZones, magicPdfConfiguration.defaultZones);
    }

    @Override
    public int hashCode() {
        return Objects.hash(integration, enabled, defaultFeatures, defaultZones);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class MagicPdfConfiguration {\n");
        sb.append("    integration: ").append(toIndentedString(integration)).append("\n");
        sb.append("    enabled: ").append(toIndentedString(enabled)).append("\n");
        sb.append("    defaultFeatures: ").append(toIndentedString(defaultFeatures)).append("\n");
        sb.append("    defaultZones: ").append(toIndentedString(defaultZones)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields =
                new HashSet<String>(
                        Arrays.asList(
                                "integration", "enabled", "default_features", "default_zones"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(Arrays.asList("integration", "enabled"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to MagicPdfConfiguration
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!MagicPdfConfiguration.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in MagicPdfConfiguration is not found in the empty JSON string",
                                MagicPdfConfiguration.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : MagicPdfConfiguration.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // validate the required field `integration`
        SupportedIntegrationsMagicPdf.validateJsonElement(jsonObj.get("integration"));
        // ensure the optional json data is an array if present
        if (jsonObj.get("default_features") != null
                && !jsonObj.get("default_features").isJsonNull()
                && !jsonObj.get("default_features").isJsonArray()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `default_features` to be an array in the JSON string but got `%s`",
                            jsonObj.get("default_features").toString()));
        }
        // ensure the optional json data is an array if present
        if (jsonObj.get("default_zones") != null
                && !jsonObj.get("default_zones").isJsonNull()
                && !jsonObj.get("default_zones").isJsonArray()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `default_zones` to be an array in the JSON string but got `%s`",
                            jsonObj.get("default_zones").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!MagicPdfConfiguration.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'MagicPdfConfiguration' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<MagicPdfConfiguration> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(MagicPdfConfiguration.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<MagicPdfConfiguration>() {
                        @Override
                        public void write(JsonWriter out, MagicPdfConfiguration value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public MagicPdfConfiguration read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of MagicPdfConfiguration given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of MagicPdfConfiguration
     * @throws IOException if the JSON string is invalid with respect to MagicPdfConfiguration
     */
    public static MagicPdfConfiguration fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, MagicPdfConfiguration.class);
    }

    /**
     * Convert an instance of MagicPdfConfiguration to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
