/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.3.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashSet;
import java.util.List;
import java.util.Objects;

/**
 * Access control settings for a composite app nav item.  If any of &#x60;groupsAllowed&#x60;, &#x60;usersAllowed&#x60;, or &#x60;rolesAllowed&#x60; are set, access is granted when the user matches ANY list (OR logic). All empty/absent means visible to everyone. Admin users bypass all checks.
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class CompositeAppNavItemPermissions {
    public static final String SERIALIZED_NAME_GROUPS_ALLOWED = "groupsAllowed";

    @SerializedName(SERIALIZED_NAME_GROUPS_ALLOWED)
    @jakarta.annotation.Nullable private List<String> groupsAllowed = new ArrayList<>();

    public static final String SERIALIZED_NAME_USERS_ALLOWED = "usersAllowed";

    @SerializedName(SERIALIZED_NAME_USERS_ALLOWED)
    @jakarta.annotation.Nullable private List<String> usersAllowed = new ArrayList<>();

    public static final String SERIALIZED_NAME_ROLES_ALLOWED = "rolesAllowed";

    @SerializedName(SERIALIZED_NAME_ROLES_ALLOWED)
    @jakarta.annotation.Nullable private List<String> rolesAllowed = new ArrayList<>();

    public CompositeAppNavItemPermissions() {}

    public CompositeAppNavItemPermissions groupsAllowed(
            @jakarta.annotation.Nullable List<String> groupsAllowed) {
        this.groupsAllowed = groupsAllowed;
        return this;
    }

    public CompositeAppNavItemPermissions addGroupsAllowedItem(String groupsAllowedItem) {
        if (this.groupsAllowed == null) {
            this.groupsAllowed = new ArrayList<>();
        }
        this.groupsAllowed.add(groupsAllowedItem);
        return this;
    }

    /**
     * Group IDs whose members can see this item.
     * @return groupsAllowed
     */
    @jakarta.annotation.Nullable public List<String> getGroupsAllowed() {
        return groupsAllowed;
    }

    public void setGroupsAllowed(@jakarta.annotation.Nullable List<String> groupsAllowed) {
        this.groupsAllowed = groupsAllowed;
    }

    public CompositeAppNavItemPermissions usersAllowed(
            @jakarta.annotation.Nullable List<String> usersAllowed) {
        this.usersAllowed = usersAllowed;
        return this;
    }

    public CompositeAppNavItemPermissions addUsersAllowedItem(String usersAllowedItem) {
        if (this.usersAllowed == null) {
            this.usersAllowed = new ArrayList<>();
        }
        this.usersAllowed.add(usersAllowedItem);
        return this;
    }

    /**
     * User IDs who can see this item.
     * @return usersAllowed
     */
    @jakarta.annotation.Nullable public List<String> getUsersAllowed() {
        return usersAllowed;
    }

    public void setUsersAllowed(@jakarta.annotation.Nullable List<String> usersAllowed) {
        this.usersAllowed = usersAllowed;
    }

    public CompositeAppNavItemPermissions rolesAllowed(
            @jakarta.annotation.Nullable List<String> rolesAllowed) {
        this.rolesAllowed = rolesAllowed;
        return this;
    }

    public CompositeAppNavItemPermissions addRolesAllowedItem(String rolesAllowedItem) {
        if (this.rolesAllowed == null) {
            this.rolesAllowed = new ArrayList<>();
        }
        this.rolesAllowed.add(rolesAllowedItem);
        return this;
    }

    /**
     * ProjectRoles values (e.g. \&quot;developer\&quot;, \&quot;manager\&quot;) whose holders can see this item.
     * @return rolesAllowed
     */
    @jakarta.annotation.Nullable public List<String> getRolesAllowed() {
        return rolesAllowed;
    }

    public void setRolesAllowed(@jakarta.annotation.Nullable List<String> rolesAllowed) {
        this.rolesAllowed = rolesAllowed;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        CompositeAppNavItemPermissions compositeAppNavItemPermissions =
                (CompositeAppNavItemPermissions) o;
        return Objects.equals(this.groupsAllowed, compositeAppNavItemPermissions.groupsAllowed)
                && Objects.equals(this.usersAllowed, compositeAppNavItemPermissions.usersAllowed)
                && Objects.equals(this.rolesAllowed, compositeAppNavItemPermissions.rolesAllowed);
    }

    @Override
    public int hashCode() {
        return Objects.hash(groupsAllowed, usersAllowed, rolesAllowed);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class CompositeAppNavItemPermissions {\n");
        sb.append("    groupsAllowed: ").append(toIndentedString(groupsAllowed)).append("\n");
        sb.append("    usersAllowed: ").append(toIndentedString(usersAllowed)).append("\n");
        sb.append("    rolesAllowed: ").append(toIndentedString(rolesAllowed)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields =
                new HashSet<String>(Arrays.asList("groupsAllowed", "usersAllowed", "rolesAllowed"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(0);
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to CompositeAppNavItemPermissions
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!CompositeAppNavItemPermissions.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in CompositeAppNavItemPermissions is not found in the empty JSON string",
                                CompositeAppNavItemPermissions.openapiRequiredFields.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // ensure the optional json data is an array if present
        if (jsonObj.get("groupsAllowed") != null
                && !jsonObj.get("groupsAllowed").isJsonNull()
                && !jsonObj.get("groupsAllowed").isJsonArray()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `groupsAllowed` to be an array in the JSON string but got `%s`",
                            jsonObj.get("groupsAllowed").toString()));
        }
        // ensure the optional json data is an array if present
        if (jsonObj.get("usersAllowed") != null
                && !jsonObj.get("usersAllowed").isJsonNull()
                && !jsonObj.get("usersAllowed").isJsonArray()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `usersAllowed` to be an array in the JSON string but got `%s`",
                            jsonObj.get("usersAllowed").toString()));
        }
        // ensure the optional json data is an array if present
        if (jsonObj.get("rolesAllowed") != null
                && !jsonObj.get("rolesAllowed").isJsonNull()
                && !jsonObj.get("rolesAllowed").isJsonArray()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `rolesAllowed` to be an array in the JSON string but got `%s`",
                            jsonObj.get("rolesAllowed").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!CompositeAppNavItemPermissions.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'CompositeAppNavItemPermissions' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<CompositeAppNavItemPermissions> thisAdapter =
                    gson.getDelegateAdapter(
                            this, TypeToken.get(CompositeAppNavItemPermissions.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<CompositeAppNavItemPermissions>() {
                        @Override
                        public void write(JsonWriter out, CompositeAppNavItemPermissions value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public CompositeAppNavItemPermissions read(JsonReader in)
                                throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of CompositeAppNavItemPermissions given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of CompositeAppNavItemPermissions
     * @throws IOException if the JSON string is invalid with respect to CompositeAppNavItemPermissions
     */
    public static CompositeAppNavItemPermissions fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, CompositeAppNavItemPermissions.class);
    }

    /**
     * Convert an instance of CompositeAppNavItemPermissions to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
