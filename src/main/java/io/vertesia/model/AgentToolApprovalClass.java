/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.6.0-dev
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.JsonElement;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

/**
 * Approval behavior class for a tool exposed to agents.  - &#x60;read_only&#x60;: reads or inspects state without changing Vertesia, external systems, or user-visible artifacts. - &#x60;side_effecting&#x60;: can create, update, delete, send, execute, schedule, or otherwise change state. - &#x60;control&#x60;: affects agent control flow or tool availability, not user data or external systems. - &#x60;requires_confirmation&#x60;: high-impact action that must ask the user even in interactive full-control mode.
 */
@JsonAdapter(AgentToolApprovalClass.Adapter.class)
public enum AgentToolApprovalClass {
    READ_ONLY("read_only"),

    SIDE_EFFECTING("side_effecting"),

    CONTROL("control"),

    REQUIRES_CONFIRMATION("requires_confirmation"),

    UNKNOWN_DEFAULT_OPEN_API("unknown_default_open_api");

    private String value;

    AgentToolApprovalClass(String value) {
        this.value = value;
    }

    public String getValue() {
        return value;
    }

    @Override
    public String toString() {
        return String.valueOf(value);
    }

    public static AgentToolApprovalClass fromValue(String value) {
        for (AgentToolApprovalClass b : AgentToolApprovalClass.values()) {
            if (b.value.equals(value)) {
                return b;
            }
        }
        return UNKNOWN_DEFAULT_OPEN_API;
    }

    public static class Adapter extends TypeAdapter<AgentToolApprovalClass> {
        @Override
        public void write(final JsonWriter jsonWriter, final AgentToolApprovalClass enumeration)
                throws IOException {
            jsonWriter.value(enumeration.getValue());
        }

        @Override
        public AgentToolApprovalClass read(final JsonReader jsonReader) throws IOException {
            String value = jsonReader.nextString();
            return AgentToolApprovalClass.fromValue(value);
        }
    }

    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        String value = jsonElement.getAsString();
        AgentToolApprovalClass.fromValue(value);
    }
}
