/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashSet;
import java.util.List;
import java.util.Objects;

/**
 * AppUIConfig
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class AppUIConfig {
    public static final String SERIALIZED_NAME_SRC = "src";

    @SerializedName(SERIALIZED_NAME_SRC)
    @jakarta.annotation.Nonnull
    private String src;

    /**
     * The isolation strategy. If not specified it defaults to shadow - shadow - use Shadow DOM to fully isolate the plugin from the host. - css - use CSS processing (like prefixing or other isolation techniques). Ligther but plugins may conflict with the host
     */
    @JsonAdapter(IsolationEnum.Adapter.class)
    public enum IsolationEnum {
        SHADOW("shadow"),

        CSS("css"),

        UNKNOWN_DEFAULT_OPEN_API("unknown_default_open_api");

        private String value;

        IsolationEnum(String value) {
            this.value = value;
        }

        public String getValue() {
            return value;
        }

        @Override
        public String toString() {
            return String.valueOf(value);
        }

        public static IsolationEnum fromValue(String value) {
            for (IsolationEnum b : IsolationEnum.values()) {
                if (b.value.equals(value)) {
                    return b;
                }
            }
            return UNKNOWN_DEFAULT_OPEN_API;
        }

        public static class Adapter extends TypeAdapter<IsolationEnum> {
            @Override
            public void write(final JsonWriter jsonWriter, final IsolationEnum enumeration)
                    throws IOException {
                jsonWriter.value(enumeration.getValue());
            }

            @Override
            public IsolationEnum read(final JsonReader jsonReader) throws IOException {
                String value = jsonReader.nextString();
                return IsolationEnum.fromValue(value);
            }
        }

        public static void validateJsonElement(JsonElement jsonElement) throws IOException {
            String value = jsonElement.getAsString();
            IsolationEnum.fromValue(value);
        }
    }

    public static final String SERIALIZED_NAME_ISOLATION = "isolation";

    @SerializedName(SERIALIZED_NAME_ISOLATION)
    @jakarta.annotation.Nullable private IsolationEnum isolation;

    public static final String SERIALIZED_NAME_NAVIGATION = "navigation";

    @SerializedName(SERIALIZED_NAME_NAVIGATION)
    @jakarta.annotation.Nullable private List<AppUINavItem> navigation = new ArrayList<>();

    public static final String SERIALIZED_NAME_AVAILABLE_IN = "available_in";

    @SerializedName(SERIALIZED_NAME_AVAILABLE_IN)
    @jakarta.annotation.Nullable private List<AppAvailableIn> availableIn = new ArrayList<>();

    public AppUIConfig() {}

    public AppUIConfig src(@jakarta.annotation.Nonnull String src) {
        this.src = src;
        return this;
    }

    /**
     * The source URL of the app. The src can be a template which contain a variable named &#x60;buildId&#x60; which will be replaced with the current build id. For example: &#x60;/plugins/vertesia-review-center-${buildId}&#x60;
     * @return src
     */
    @jakarta.annotation.Nonnull
    public String getSrc() {
        return src;
    }

    public void setSrc(@jakarta.annotation.Nonnull String src) {
        this.src = src;
    }

    public AppUIConfig isolation(@jakarta.annotation.Nullable IsolationEnum isolation) {
        this.isolation = isolation;
        return this;
    }

    /**
     * The isolation strategy. If not specified it defaults to shadow - shadow - use Shadow DOM to fully isolate the plugin from the host. - css - use CSS processing (like prefixing or other isolation techniques). Ligther but plugins may conflict with the host
     * @return isolation
     */
    @jakarta.annotation.Nullable public IsolationEnum getIsolation() {
        return isolation;
    }

    public void setIsolation(@jakarta.annotation.Nullable IsolationEnum isolation) {
        this.isolation = isolation;
    }

    public AppUIConfig navigation(@jakarta.annotation.Nullable List<AppUINavItem> navigation) {
        this.navigation = navigation;
        return this;
    }

    public AppUIConfig addNavigationItem(AppUINavItem navigationItem) {
        if (this.navigation == null) {
            this.navigation = new ArrayList<>();
        }
        this.navigation.add(navigationItem);
        return this;
    }

    /**
     * Navigation items for the app&#39;s sidebar UI. Only applicable for apps with UI capability in shell contexts (ie. CompositeApp shell).
     * @return navigation
     */
    @jakarta.annotation.Nullable public List<AppUINavItem> getNavigation() {
        return navigation;
    }

    public void setNavigation(@jakarta.annotation.Nullable List<AppUINavItem> navigation) {
        this.navigation = navigation;
    }

    public AppUIConfig availableIn(@jakarta.annotation.Nullable List<AppAvailableIn> availableIn) {
        this.availableIn = availableIn;
        return this;
    }

    public AppUIConfig addAvailableInItem(AppAvailableIn availableInItem) {
        if (this.availableIn == null) {
            this.availableIn = new ArrayList<>();
        }
        this.availableIn.add(availableInItem);
        return this;
    }

    /**
     * Where this app&#39;s UI can be displayed. - &#39;app_portal&#39;: Available in the main app portal (standalone) - &#39;composite_app&#39;: Available within a CompositeApp shell Defaults to [&#39;app_portal&#39;, &#39;composite_app&#39;] for new apps.
     * @return availableIn
     */
    @jakarta.annotation.Nullable public List<AppAvailableIn> getAvailableIn() {
        return availableIn;
    }

    public void setAvailableIn(@jakarta.annotation.Nullable List<AppAvailableIn> availableIn) {
        this.availableIn = availableIn;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        AppUIConfig appUIConfig = (AppUIConfig) o;
        return Objects.equals(this.src, appUIConfig.src)
                && Objects.equals(this.isolation, appUIConfig.isolation)
                && Objects.equals(this.navigation, appUIConfig.navigation)
                && Objects.equals(this.availableIn, appUIConfig.availableIn);
    }

    @Override
    public int hashCode() {
        return Objects.hash(src, isolation, navigation, availableIn);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class AppUIConfig {\n");
        sb.append("    src: ").append(toIndentedString(src)).append("\n");
        sb.append("    isolation: ").append(toIndentedString(isolation)).append("\n");
        sb.append("    navigation: ").append(toIndentedString(navigation)).append("\n");
        sb.append("    availableIn: ").append(toIndentedString(availableIn)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields =
                new HashSet<String>(
                        Arrays.asList("src", "isolation", "navigation", "available_in"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(Arrays.asList("src"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to AppUIConfig
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!AppUIConfig.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in AppUIConfig is not found in the empty JSON string",
                                AppUIConfig.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : AppUIConfig.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (!jsonObj.get("src").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `src` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("src").toString()));
        }
        if ((jsonObj.get("isolation") != null && !jsonObj.get("isolation").isJsonNull())
                && !jsonObj.get("isolation").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `isolation` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("isolation").toString()));
        }
        // validate the optional field `isolation`
        if (jsonObj.get("isolation") != null && !jsonObj.get("isolation").isJsonNull()) {
            IsolationEnum.validateJsonElement(jsonObj.get("isolation"));
        }
        if (jsonObj.get("navigation") != null && !jsonObj.get("navigation").isJsonNull()) {
            JsonArray jsonArraynavigation = jsonObj.getAsJsonArray("navigation");
            if (jsonArraynavigation != null) {
                // ensure the json data is an array
                if (!jsonObj.get("navigation").isJsonArray()) {
                    throw new IllegalArgumentException(
                            String.format(
                                    java.util.Locale.ROOT,
                                    "Expected the field `navigation` to be an array in the JSON string but got `%s`",
                                    jsonObj.get("navigation").toString()));
                }

                // validate the optional field `navigation` (array)
                for (int i = 0; i < jsonArraynavigation.size(); i++) {
                    AppUINavItem.validateJsonElement(jsonArraynavigation.get(i));
                }
                ;
            }
        }
        // ensure the optional json data is an array if present
        if (jsonObj.get("available_in") != null
                && !jsonObj.get("available_in").isJsonNull()
                && !jsonObj.get("available_in").isJsonArray()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `available_in` to be an array in the JSON string but got `%s`",
                            jsonObj.get("available_in").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!AppUIConfig.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'AppUIConfig' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<AppUIConfig> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(AppUIConfig.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<AppUIConfig>() {
                        @Override
                        public void write(JsonWriter out, AppUIConfig value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public AppUIConfig read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of AppUIConfig given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of AppUIConfig
     * @throws IOException if the JSON string is invalid with respect to AppUIConfig
     */
    public static AppUIConfig fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, AppUIConfig.class);
    }

    /**
     * Convert an instance of AppUIConfig to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
