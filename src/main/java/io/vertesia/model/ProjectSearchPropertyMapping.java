/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260725.083715Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.math.BigDecimal;
import java.util.Arrays;
import java.util.HashSet;
import java.util.Objects;

/**
 * Explicit search mapping for one content-object property.  Changing a mapping requires a full reindex. Existing Elasticsearch fields cannot change type in place.
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class ProjectSearchPropertyMapping {
    public static final String SERIALIZED_NAME_TYPE = "type";

    @SerializedName(SERIALIZED_NAME_TYPE)
    @jakarta.annotation.Nonnull
    private ProjectSearchPropertyType type;

    public static final String SERIALIZED_NAME_FORMAT = "format";

    @SerializedName(SERIALIZED_NAME_FORMAT)
    @jakarta.annotation.Nullable private String format;

    public static final String SERIALIZED_NAME_IGNORE_ABOVE = "ignore_above";

    @SerializedName(SERIALIZED_NAME_IGNORE_ABOVE)
    @jakarta.annotation.Nullable private BigDecimal ignoreAbove;

    public static final String SERIALIZED_NAME_IGNORE_MALFORMED = "ignore_malformed";

    @SerializedName(SERIALIZED_NAME_IGNORE_MALFORMED)
    @jakarta.annotation.Nullable private Boolean ignoreMalformed;

    public ProjectSearchPropertyMapping() {}

    public ProjectSearchPropertyMapping type(
            @jakarta.annotation.Nonnull ProjectSearchPropertyType type) {
        this.type = type;
        return this;
    }

    /**
     * Get type
     * @return type
     */
    @jakarta.annotation.Nonnull
    public ProjectSearchPropertyType getType() {
        return type;
    }

    public void setType(@jakarta.annotation.Nonnull ProjectSearchPropertyType type) {
        this.type = type;
    }

    public ProjectSearchPropertyMapping format(@jakarta.annotation.Nullable String format) {
        this.format = format;
        return this;
    }

    /**
     * Elasticsearch date format. Valid only when type is &#x60;date&#x60;.
     * @return format
     */
    @jakarta.annotation.Nullable public String getFormat() {
        return format;
    }

    public void setFormat(@jakarta.annotation.Nullable String format) {
        this.format = format;
    }

    public ProjectSearchPropertyMapping ignoreAbove(
            @jakarta.annotation.Nullable BigDecimal ignoreAbove) {
        this.ignoreAbove = ignoreAbove;
        return this;
    }

    /**
     * Maximum indexed string length. Valid only when type is &#x60;keyword&#x60;.
     * @return ignoreAbove
     */
    @jakarta.annotation.Nullable public BigDecimal getIgnoreAbove() {
        return ignoreAbove;
    }

    public void setIgnoreAbove(@jakarta.annotation.Nullable BigDecimal ignoreAbove) {
        this.ignoreAbove = ignoreAbove;
    }

    public ProjectSearchPropertyMapping ignoreMalformed(
            @jakarta.annotation.Nullable Boolean ignoreMalformed) {
        this.ignoreMalformed = ignoreMalformed;
        return this;
    }

    /**
     * Skip malformed values instead of rejecting the whole document. Valid only for long, double, and date mappings.
     * @return ignoreMalformed
     */
    @jakarta.annotation.Nullable public Boolean getIgnoreMalformed() {
        return ignoreMalformed;
    }

    public void setIgnoreMalformed(@jakarta.annotation.Nullable Boolean ignoreMalformed) {
        this.ignoreMalformed = ignoreMalformed;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ProjectSearchPropertyMapping projectSearchPropertyMapping =
                (ProjectSearchPropertyMapping) o;
        return Objects.equals(this.type, projectSearchPropertyMapping.type)
                && Objects.equals(this.format, projectSearchPropertyMapping.format)
                && Objects.equals(this.ignoreAbove, projectSearchPropertyMapping.ignoreAbove)
                && Objects.equals(
                        this.ignoreMalformed, projectSearchPropertyMapping.ignoreMalformed);
    }

    @Override
    public int hashCode() {
        return Objects.hash(type, format, ignoreAbove, ignoreMalformed);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class ProjectSearchPropertyMapping {\n");
        sb.append("    type: ").append(toIndentedString(type)).append("\n");
        sb.append("    format: ").append(toIndentedString(format)).append("\n");
        sb.append("    ignoreAbove: ").append(toIndentedString(ignoreAbove)).append("\n");
        sb.append("    ignoreMalformed: ").append(toIndentedString(ignoreMalformed)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields =
                new HashSet<String>(
                        Arrays.asList("type", "format", "ignore_above", "ignore_malformed"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(Arrays.asList("type"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to ProjectSearchPropertyMapping
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!ProjectSearchPropertyMapping.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in ProjectSearchPropertyMapping is not found in the empty JSON string",
                                ProjectSearchPropertyMapping.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : ProjectSearchPropertyMapping.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // validate the required field `type`
        ProjectSearchPropertyType.validateJsonElement(jsonObj.get("type"));
        if ((jsonObj.get("format") != null && !jsonObj.get("format").isJsonNull())
                && !jsonObj.get("format").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `format` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("format").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ProjectSearchPropertyMapping.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'ProjectSearchPropertyMapping' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ProjectSearchPropertyMapping> thisAdapter =
                    gson.getDelegateAdapter(
                            this, TypeToken.get(ProjectSearchPropertyMapping.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<ProjectSearchPropertyMapping>() {
                        @Override
                        public void write(JsonWriter out, ProjectSearchPropertyMapping value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public ProjectSearchPropertyMapping read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of ProjectSearchPropertyMapping given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of ProjectSearchPropertyMapping
     * @throws IOException if the JSON string is invalid with respect to ProjectSearchPropertyMapping
     */
    public static ProjectSearchPropertyMapping fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, ProjectSearchPropertyMapping.class);
    }

    /**
     * Convert an instance of ProjectSearchPropertyMapping to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
