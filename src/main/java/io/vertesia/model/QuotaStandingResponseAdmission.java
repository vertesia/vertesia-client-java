/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260807.073259Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashSet;
import java.util.List;
import java.util.Objects;

/**
 * Workflow admission: per-tenant active slots per probed class. The budget itself is global and discovered (AIMD), not a per-tenant number — this is occupancy, not a limit.
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class QuotaStandingResponseAdmission {
    public static final String SERIALIZED_NAME_CLASSES = "classes";

    @SerializedName(SERIALIZED_NAME_CLASSES)
    @jakarta.annotation.Nonnull
    private List<QuotaStandingAdmissionClass> classes = new ArrayList<>();

    public static final String SERIALIZED_NAME_NOTE = "note";

    @SerializedName(SERIALIZED_NAME_NOTE)
    @jakarta.annotation.Nonnull
    private String note;

    public QuotaStandingResponseAdmission() {}

    public QuotaStandingResponseAdmission classes(
            @jakarta.annotation.Nonnull List<QuotaStandingAdmissionClass> classes) {
        this.classes = classes;
        return this;
    }

    public QuotaStandingResponseAdmission addClassesItem(QuotaStandingAdmissionClass classesItem) {
        if (this.classes == null) {
            this.classes = new ArrayList<>();
        }
        this.classes.add(classesItem);
        return this;
    }

    /**
     * Get classes
     * @return classes
     */
    @jakarta.annotation.Nonnull
    public List<QuotaStandingAdmissionClass> getClasses() {
        return classes;
    }

    public void setClasses(@jakarta.annotation.Nonnull List<QuotaStandingAdmissionClass> classes) {
        this.classes = classes;
    }

    public QuotaStandingResponseAdmission note(@jakarta.annotation.Nonnull String note) {
        this.note = note;
        return this;
    }

    /**
     * Get note
     * @return note
     */
    @jakarta.annotation.Nonnull
    public String getNote() {
        return note;
    }

    public void setNote(@jakarta.annotation.Nonnull String note) {
        this.note = note;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        QuotaStandingResponseAdmission quotaStandingResponseAdmission =
                (QuotaStandingResponseAdmission) o;
        return Objects.equals(this.classes, quotaStandingResponseAdmission.classes)
                && Objects.equals(this.note, quotaStandingResponseAdmission.note);
    }

    @Override
    public int hashCode() {
        return Objects.hash(classes, note);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class QuotaStandingResponseAdmission {\n");
        sb.append("    classes: ").append(toIndentedString(classes)).append("\n");
        sb.append("    note: ").append(toIndentedString(note)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>(Arrays.asList("classes", "note"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(Arrays.asList("classes", "note"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to QuotaStandingResponseAdmission
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!QuotaStandingResponseAdmission.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in QuotaStandingResponseAdmission is not found in the empty JSON string",
                                QuotaStandingResponseAdmission.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : QuotaStandingResponseAdmission.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (jsonObj.get("classes") != null) {
            if (!jsonObj.get("classes").isJsonArray()) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "Expected the field `classes` to be an array in the JSON string but got `%s`",
                                jsonObj.get("classes").toString()));
            }
            JsonArray jsonArrayclasses = jsonObj.getAsJsonArray("classes");
            // validate the required field `classes` (array)
            for (int i = 0; i < jsonArrayclasses.size(); i++) {
                QuotaStandingAdmissionClass.validateJsonElement(jsonArrayclasses.get(i));
            }
        }
        if (!jsonObj.get("note").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `note` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("note").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!QuotaStandingResponseAdmission.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'QuotaStandingResponseAdmission' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<QuotaStandingResponseAdmission> thisAdapter =
                    gson.getDelegateAdapter(
                            this, TypeToken.get(QuotaStandingResponseAdmission.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<QuotaStandingResponseAdmission>() {
                        @Override
                        public void write(JsonWriter out, QuotaStandingResponseAdmission value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public QuotaStandingResponseAdmission read(JsonReader in)
                                throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of QuotaStandingResponseAdmission given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of QuotaStandingResponseAdmission
     * @throws IOException if the JSON string is invalid with respect to QuotaStandingResponseAdmission
     */
    public static QuotaStandingResponseAdmission fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, QuotaStandingResponseAdmission.class);
    }

    /**
     * Convert an instance of QuotaStandingResponseAdmission to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
