/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` SDK for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.2.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;

/**
 * DSLActivityStep
 */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class DSLActivityStep {
    /**
     * The type fo the step. If not set defaults to \&quot;activity\&quot;
     */
    @JsonAdapter(TypeEnum.Adapter.class)
    public enum TypeEnum {
        ACTIVITY("activity"),

        UNKNOWN_DEFAULT_OPEN_API("unknown_default_open_api");

        private String value;

        TypeEnum(String value) {
            this.value = value;
        }

        public String getValue() {
            return value;
        }

        @Override
        public String toString() {
            return String.valueOf(value);
        }

        public static TypeEnum fromValue(String value) {
            for (TypeEnum b : TypeEnum.values()) {
                if (b.value.equals(value)) {
                    return b;
                }
            }
            return UNKNOWN_DEFAULT_OPEN_API;
        }

        public static class Adapter extends TypeAdapter<TypeEnum> {
            @Override
            public void write(final JsonWriter jsonWriter, final TypeEnum enumeration)
                    throws IOException {
                jsonWriter.value(enumeration.getValue());
            }

            @Override
            public TypeEnum read(final JsonReader jsonReader) throws IOException {
                String value = jsonReader.nextString();
                return TypeEnum.fromValue(value);
            }
        }

        public static void validateJsonElement(JsonElement jsonElement) throws IOException {
            String value = jsonElement.getAsString();
            TypeEnum.fromValue(value);
        }
    }

    public static final String SERIALIZED_NAME_TYPE = "type";

    @SerializedName(SERIALIZED_NAME_TYPE)
    @javax.annotation.Nonnull
    private TypeEnum type;

    public static final String SERIALIZED_NAME_NAME = "name";

    @SerializedName(SERIALIZED_NAME_NAME)
    @javax.annotation.Nonnull
    private String name;

    public static final String SERIALIZED_NAME_TITLE = "title";

    @SerializedName(SERIALIZED_NAME_TITLE)
    @javax.annotation.Nullable private String title;

    public static final String SERIALIZED_NAME_DESCRIPTION = "description";

    @SerializedName(SERIALIZED_NAME_DESCRIPTION)
    @javax.annotation.Nullable private String description;

    public static final String SERIALIZED_NAME_PARAMS = "params";

    @SerializedName(SERIALIZED_NAME_PARAMS)
    @javax.annotation.Nullable private Map<String, Object> params = new HashMap<>();

    public static final String SERIALIZED_NAME_OUTPUT = "output";

    @SerializedName(SERIALIZED_NAME_OUTPUT)
    @javax.annotation.Nullable private String output;

    public static final String SERIALIZED_NAME_CONDITION = "condition";

    @SerializedName(SERIALIZED_NAME_CONDITION)
    @javax.annotation.Nullable private Map<String, Object> condition = new HashMap<>();

    public static final String SERIALIZED_NAME_IMPORT = "import";

    @SerializedName(SERIALIZED_NAME_IMPORT)
    @javax.annotation.Nullable private List<Object> _import;

    public static final String SERIALIZED_NAME_FETCH = "fetch";

    @SerializedName(SERIALIZED_NAME_FETCH)
    @javax.annotation.Nullable private Map<String, ActivityFetchSpec> fetch;

    public static final String SERIALIZED_NAME_PROJECTION = "projection";

    @SerializedName(SERIALIZED_NAME_PROJECTION)
    @javax.annotation.Nullable private Map<String, Object> projection = new HashMap<>();

    public static final String SERIALIZED_NAME_PARALLEL = "parallel";

    @SerializedName(SERIALIZED_NAME_PARALLEL)
    @javax.annotation.Nullable private Boolean parallel;

    public static final String SERIALIZED_NAME_AWAIT = "await";

    @SerializedName(SERIALIZED_NAME_AWAIT)
    @javax.annotation.Nullable private String await;

    public static final String SERIALIZED_NAME_OPTIONS = "options";

    @SerializedName(SERIALIZED_NAME_OPTIONS)
    @javax.annotation.Nullable private DSLActivityOptions options;

    public DSLActivityStep() {}

    public DSLActivityStep type(@javax.annotation.Nonnull TypeEnum type) {
        this.type = type;
        return this;
    }

    /**
     * The type fo the step. If not set defaults to \&quot;activity\&quot;
     * @return type
     */
    @javax.annotation.Nonnull
    public TypeEnum getType() {
        return type;
    }

    public void setType(@javax.annotation.Nonnull TypeEnum type) {
        this.type = type;
    }

    public DSLActivityStep name(@javax.annotation.Nonnull String name) {
        this.name = name;
        return this;
    }

    /**
     * The name of the activity function
     * @return name
     */
    @javax.annotation.Nonnull
    public String getName() {
        return name;
    }

    public void setName(@javax.annotation.Nonnull String name) {
        this.name = name;
    }

    public DSLActivityStep title(@javax.annotation.Nullable String title) {
        this.title = title;
        return this;
    }

    /**
     * Title of the activity to be displayed in the UI workflow builder
     * @return title
     */
    @javax.annotation.Nullable public String getTitle() {
        return title;
    }

    public void setTitle(@javax.annotation.Nullable String title) {
        this.title = title;
    }

    public DSLActivityStep description(@javax.annotation.Nullable String description) {
        this.description = description;
        return this;
    }

    /**
     * The description of the activity to e displayed in the UI workflow builder
     * @return description
     */
    @javax.annotation.Nullable public String getDescription() {
        return description;
    }

    public void setDescription(@javax.annotation.Nullable String description) {
        this.description = description;
    }

    public DSLActivityStep params(@javax.annotation.Nullable Map<String, Object> params) {
        this.params = params;
        return this;
    }

    public DSLActivityStep putParamsItem(String key, Object paramsItem) {
        if (this.params == null) {
            this.params = new HashMap<>();
        }
        this.params.put(key, paramsItem);
        return this;
    }

    /**
     * Activities parameters. These parameters can be either literals (hardcoded strings, numbers, booleans, objects, arrays etc.), either references to the workflow variables. The workflow variables are built from the workflow params (e.g. the workflow configuration) and from the result of the previous activities.
     * @return params
     */
    @javax.annotation.Nullable public Map<String, Object> getParams() {
        return params;
    }

    public void setParams(@javax.annotation.Nullable Map<String, Object> params) {
        this.params = params;
    }

    public DSLActivityStep output(@javax.annotation.Nullable String output) {
        this.output = output;
        return this;
    }

    /**
     * The name of the workflow variable that will store the result of the activity If not specified the result will not be stored The parameters describe how the actual parameters will be obtained from the workflow execution vars. since it may contain references to workflow execution vars.
     * @return output
     */
    @javax.annotation.Nullable public String getOutput() {
        return output;
    }

    public void setOutput(@javax.annotation.Nullable String output) {
        this.output = output;
    }

    public DSLActivityStep condition(@javax.annotation.Nullable Map<String, Object> condition) {
        this.condition = condition;
        return this;
    }

    public DSLActivityStep putConditionItem(String key, Object conditionItem) {
        if (this.condition == null) {
            this.condition = new HashMap<>();
        }
        this.condition.put(key, conditionItem);
        return this;
    }

    /**
     * A JSON expression which evaluate to true or false similar to mongo matches. We support for now basic expression like: $true, $false, $eq, $ne, $gt, $gte, $lt, $lte, $in, $nin, $regexp {$eq: {name: value}}, Ex: {$eq: {wfVarName: value}}
     * @return condition
     */
    @javax.annotation.Nullable public Map<String, Object> getCondition() {
        return condition;
    }

    public void setCondition(@javax.annotation.Nullable Map<String, Object> condition) {
        this.condition = condition;
    }

    public DSLActivityStep _import(@javax.annotation.Nullable List<Object> _import) {
        this._import = _import;
        return this;
    }

    public DSLActivityStep addImportItem(Object _importItem) {
        if (this._import == null) {
            this._import = new ArrayList<>();
        }
        this._import.add(_importItem);
        return this;
    }

    /**
     * The import spec is used to import data from workflow variables. The import spec is a list of variable names to import from the workflow context. You can also use objects to rename the imported variables, or to reference an expression. Example: [\&quot;runId\&quot;, {\&quot;typeId\&quot;: \&quot;docType.id\&quot;}]
     * @return _import
     */
    @javax.annotation.Nullable public List<Object> getImport() {
        return _import;
    }

    public void setImport(@javax.annotation.Nullable List<Object> _import) {
        this._import = _import;
    }

    public DSLActivityStep fetch(@javax.annotation.Nullable Map<String, ActivityFetchSpec> fetch) {
        this.fetch = fetch;
        return this;
    }

    public DSLActivityStep putFetchItem(String key, ActivityFetchSpec fetchItem) {
        if (this.fetch == null) {
            this.fetch = new HashMap<>();
        }
        this.fetch.put(key, fetchItem);
        return this;
    }

    /**
     * The fetch phase is used to fetch data from external sources.
     * @return fetch
     */
    @javax.annotation.Nullable public Map<String, ActivityFetchSpec> getFetch() {
        return fetch;
    }

    public void setFetch(@javax.annotation.Nullable Map<String, ActivityFetchSpec> fetch) {
        this.fetch = fetch;
    }

    public DSLActivityStep projection(@javax.annotation.Nullable Map<String, Object> projection) {
        this.projection = projection;
        return this;
    }

    public DSLActivityStep putProjectionItem(String key, Object projectionItem) {
        if (this.projection == null) {
            this.projection = new HashMap<>();
        }
        this.projection.put(key, projectionItem);
        return this;
    }

    /**
     * Projection to apply to the result. Not all activities support this.
     * @return projection
     */
    @javax.annotation.Nullable public Map<String, Object> getProjection() {
        return projection;
    }

    public void setProjection(@javax.annotation.Nullable Map<String, Object> projection) {
        this.projection = projection;
    }

    public DSLActivityStep parallel(@javax.annotation.Nullable Boolean parallel) {
        this.parallel = parallel;
        return this;
    }

    /**
     * If true the activity will be executed in parallel with the other activities. (i.e. the workflow will not wait for the activity to finish before starting the next one)
     * @return parallel
     */
    @javax.annotation.Nullable public Boolean getParallel() {
        return parallel;
    }

    public void setParallel(@javax.annotation.Nullable Boolean parallel) {
        this.parallel = parallel;
    }

    public DSLActivityStep await(@javax.annotation.Nullable String await) {
        this.await = await;
        return this;
    }

    /**
     * Await for a parallel activity execution to return.
     * @return await
     */
    @javax.annotation.Nullable public String getAwait() {
        return await;
    }

    public void setAwait(@javax.annotation.Nullable String await) {
        this.await = await;
    }

    public DSLActivityStep options(@javax.annotation.Nullable DSLActivityOptions options) {
        this.options = options;
        return this;
    }

    /**
     * Activity options for configuring the activity execution, which overrides the activity options defined at workflow level.
     * @return options
     */
    @javax.annotation.Nullable public DSLActivityOptions getOptions() {
        return options;
    }

    public void setOptions(@javax.annotation.Nullable DSLActivityOptions options) {
        this.options = options;
    }

    /**
     * A container for additional, undeclared properties.
     * This is a holder for any undeclared properties as specified with
     * the 'additionalProperties' keyword in the OAS document.
     */
    private Map<String, Object> additionalProperties;

    /**
     * Set the additional (undeclared) property with the specified name and value.
     * If the property does not already exist, create it otherwise replace it.
     *
     * @param key name of the property
     * @param value value of the property
     * @return the DSLActivityStep instance itself
     */
    public DSLActivityStep putAdditionalProperty(String key, Object value) {
        if (this.additionalProperties == null) {
            this.additionalProperties = new HashMap<String, Object>();
        }
        this.additionalProperties.put(key, value);
        return this;
    }

    /**
     * Return the additional (undeclared) property.
     *
     * @return a map of objects
     */
    public Map<String, Object> getAdditionalProperties() {
        return additionalProperties;
    }

    /**
     * Return the additional (undeclared) property with the specified name.
     *
     * @param key name of the property
     * @return an object
     */
    public Object getAdditionalProperty(String key) {
        if (this.additionalProperties == null) {
            return null;
        }
        return this.additionalProperties.get(key);
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        DSLActivityStep dsLActivityStep = (DSLActivityStep) o;
        return Objects.equals(this.type, dsLActivityStep.type)
                && Objects.equals(this.name, dsLActivityStep.name)
                && Objects.equals(this.title, dsLActivityStep.title)
                && Objects.equals(this.description, dsLActivityStep.description)
                && Objects.equals(this.params, dsLActivityStep.params)
                && Objects.equals(this.output, dsLActivityStep.output)
                && Objects.equals(this.condition, dsLActivityStep.condition)
                && Objects.equals(this._import, dsLActivityStep._import)
                && Objects.equals(this.fetch, dsLActivityStep.fetch)
                && Objects.equals(this.projection, dsLActivityStep.projection)
                && Objects.equals(this.parallel, dsLActivityStep.parallel)
                && Objects.equals(this.await, dsLActivityStep.await)
                && Objects.equals(this.options, dsLActivityStep.options)
                && Objects.equals(this.additionalProperties, dsLActivityStep.additionalProperties);
    }

    @Override
    public int hashCode() {
        return Objects.hash(
                type,
                name,
                title,
                description,
                params,
                output,
                condition,
                _import,
                fetch,
                projection,
                parallel,
                await,
                options,
                additionalProperties);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class DSLActivityStep {\n");
        sb.append("    type: ").append(toIndentedString(type)).append("\n");
        sb.append("    name: ").append(toIndentedString(name)).append("\n");
        sb.append("    title: ").append(toIndentedString(title)).append("\n");
        sb.append("    description: ").append(toIndentedString(description)).append("\n");
        sb.append("    params: ").append(toIndentedString(params)).append("\n");
        sb.append("    output: ").append(toIndentedString(output)).append("\n");
        sb.append("    condition: ").append(toIndentedString(condition)).append("\n");
        sb.append("    _import: ").append(toIndentedString(_import)).append("\n");
        sb.append("    fetch: ").append(toIndentedString(fetch)).append("\n");
        sb.append("    projection: ").append(toIndentedString(projection)).append("\n");
        sb.append("    parallel: ").append(toIndentedString(parallel)).append("\n");
        sb.append("    await: ").append(toIndentedString(await)).append("\n");
        sb.append("    options: ").append(toIndentedString(options)).append("\n");
        sb.append("    additionalProperties: ")
                .append(toIndentedString(additionalProperties))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields =
                new HashSet<String>(
                        Arrays.asList(
                                "type",
                                "name",
                                "title",
                                "description",
                                "params",
                                "output",
                                "condition",
                                "import",
                                "fetch",
                                "projection",
                                "parallel",
                                "await",
                                "options"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(Arrays.asList("type", "name"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to DSLActivityStep
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!DSLActivityStep.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in DSLActivityStep is not found in the empty JSON string",
                                DSLActivityStep.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : DSLActivityStep.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (!jsonObj.get("type").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `type` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("type").toString()));
        }
        // validate the required field `type`
        TypeEnum.validateJsonElement(jsonObj.get("type"));
        if (!jsonObj.get("name").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `name` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("name").toString()));
        }
        if ((jsonObj.get("title") != null && !jsonObj.get("title").isJsonNull())
                && !jsonObj.get("title").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `title` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("title").toString()));
        }
        if ((jsonObj.get("description") != null && !jsonObj.get("description").isJsonNull())
                && !jsonObj.get("description").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `description` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("description").toString()));
        }
        if ((jsonObj.get("output") != null && !jsonObj.get("output").isJsonNull())
                && !jsonObj.get("output").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `output` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("output").toString()));
        }
        // ensure the optional json data is an array if present
        if (jsonObj.get("import") != null
                && !jsonObj.get("import").isJsonNull()
                && !jsonObj.get("import").isJsonArray()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `import` to be an array in the JSON string but got `%s`",
                            jsonObj.get("import").toString()));
        }
        if ((jsonObj.get("await") != null && !jsonObj.get("await").isJsonNull())
                && !jsonObj.get("await").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `await` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("await").toString()));
        }
        // validate the optional field `options`
        if (jsonObj.get("options") != null && !jsonObj.get("options").isJsonNull()) {
            DSLActivityOptions.validateJsonElement(jsonObj.get("options"));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!DSLActivityStep.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'DSLActivityStep' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<DSLActivityStep> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(DSLActivityStep.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<DSLActivityStep>() {
                        @Override
                        public void write(JsonWriter out, DSLActivityStep value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            obj.remove("additionalProperties");
                            // serialize additional properties
                            if (value.getAdditionalProperties() != null) {
                                for (Map.Entry<String, Object> entry :
                                        value.getAdditionalProperties().entrySet()) {
                                    if (entry.getValue() instanceof String)
                                        obj.addProperty(entry.getKey(), (String) entry.getValue());
                                    else if (entry.getValue() instanceof Number)
                                        obj.addProperty(entry.getKey(), (Number) entry.getValue());
                                    else if (entry.getValue() instanceof Boolean)
                                        obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                                    else if (entry.getValue() instanceof Character)
                                        obj.addProperty(
                                                entry.getKey(), (Character) entry.getValue());
                                    else {
                                        JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                                        if (jsonElement.isJsonArray()) {
                                            obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                                        } else {
                                            obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                                        }
                                    }
                                }
                            }
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public DSLActivityStep read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            JsonObject jsonObj = jsonElement.getAsJsonObject();
                            // store additional fields in the deserialized instance
                            DSLActivityStep instance = thisAdapter.fromJsonTree(jsonObj);
                            for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
                                if (!openapiFields.contains(entry.getKey())) {
                                    if (entry.getValue().isJsonPrimitive()) { // primitive type
                                        if (entry.getValue().getAsJsonPrimitive().isString())
                                            instance.putAdditionalProperty(
                                                    entry.getKey(), entry.getValue().getAsString());
                                        else if (entry.getValue().getAsJsonPrimitive().isNumber())
                                            instance.putAdditionalProperty(
                                                    entry.getKey(), entry.getValue().getAsNumber());
                                        else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                                            instance.putAdditionalProperty(
                                                    entry.getKey(),
                                                    entry.getValue().getAsBoolean());
                                        else
                                            throw new IllegalArgumentException(
                                                    String.format(
                                                            java.util.Locale.ROOT,
                                                            "The field `%s` has unknown primitive type. Value: %s",
                                                            entry.getKey(),
                                                            entry.getValue().toString()));
                                    } else if (entry.getValue().isJsonArray()) {
                                        instance.putAdditionalProperty(
                                                entry.getKey(),
                                                gson.fromJson(entry.getValue(), List.class));
                                    } else { // JSON object
                                        instance.putAdditionalProperty(
                                                entry.getKey(),
                                                gson.fromJson(entry.getValue(), HashMap.class));
                                    }
                                }
                            }
                            return instance;
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of DSLActivityStep given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of DSLActivityStep
     * @throws IOException if the JSON string is invalid with respect to DSLActivityStep
     */
    public static DSLActivityStep fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, DSLActivityStep.class);
    }

    /**
     * Convert an instance of DSLActivityStep to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
