/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.math.BigDecimal;
import java.util.Arrays;
import java.util.HashSet;
import java.util.Objects;

/**
 * AgenticViewRerankConfiguration
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class AgenticViewRerankConfiguration {
    public static final String SERIALIZED_NAME_INTERACTION = "interaction";

    @SerializedName(SERIALIZED_NAME_INTERACTION)
    @jakarta.annotation.Nullable private String interaction;

    public static final String SERIALIZED_NAME_MAX_CANDIDATES = "max_candidates";

    @SerializedName(SERIALIZED_NAME_MAX_CANDIDATES)
    @jakarta.annotation.Nullable private BigDecimal maxCandidates;

    public static final String SERIALIZED_NAME_INCLUDE_WHY_MATCH = "include_why_match";

    @SerializedName(SERIALIZED_NAME_INCLUDE_WHY_MATCH)
    @jakarta.annotation.Nullable private Boolean includeWhyMatch;

    public static final String SERIALIZED_NAME_TIMEOUT_MS = "timeout_ms";

    @SerializedName(SERIALIZED_NAME_TIMEOUT_MS)
    @jakarta.annotation.Nullable private BigDecimal timeoutMs;

    public AgenticViewRerankConfiguration() {}

    public AgenticViewRerankConfiguration interaction(
            @jakarta.annotation.Nullable String interaction) {
        this.interaction = interaction;
        return this;
    }

    /**
     * Interaction used only for candidate reranking. Defaults to sys:ContentSearchReranker.
     * @return interaction
     */
    @jakarta.annotation.Nullable public String getInteraction() {
        return interaction;
    }

    public void setInteraction(@jakarta.annotation.Nullable String interaction) {
        this.interaction = interaction;
    }

    public AgenticViewRerankConfiguration maxCandidates(
            @jakarta.annotation.Nullable BigDecimal maxCandidates) {
        this.maxCandidates = maxCandidates;
        return this;
    }

    /**
     * Maximum candidates from the current result page sent to the model.
     * @return maxCandidates
     */
    @jakarta.annotation.Nullable public BigDecimal getMaxCandidates() {
        return maxCandidates;
    }

    public void setMaxCandidates(@jakarta.annotation.Nullable BigDecimal maxCandidates) {
        this.maxCandidates = maxCandidates;
    }

    public AgenticViewRerankConfiguration includeWhyMatch(
            @jakarta.annotation.Nullable Boolean includeWhyMatch) {
        this.includeWhyMatch = includeWhyMatch;
        return this;
    }

    /**
     * Ask the model for a short per-result explanation. Defaults to true.
     * @return includeWhyMatch
     */
    @jakarta.annotation.Nullable public Boolean getIncludeWhyMatch() {
        return includeWhyMatch;
    }

    public void setIncludeWhyMatch(@jakarta.annotation.Nullable Boolean includeWhyMatch) {
        this.includeWhyMatch = includeWhyMatch;
    }

    public AgenticViewRerankConfiguration timeoutMs(
            @jakarta.annotation.Nullable BigDecimal timeoutMs) {
        this.timeoutMs = timeoutMs;
        return this;
    }

    /**
     * Reranking-stage timeout.
     * @return timeoutMs
     */
    @jakarta.annotation.Nullable public BigDecimal getTimeoutMs() {
        return timeoutMs;
    }

    public void setTimeoutMs(@jakarta.annotation.Nullable BigDecimal timeoutMs) {
        this.timeoutMs = timeoutMs;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        AgenticViewRerankConfiguration agenticViewRerankConfiguration =
                (AgenticViewRerankConfiguration) o;
        return Objects.equals(this.interaction, agenticViewRerankConfiguration.interaction)
                && Objects.equals(this.maxCandidates, agenticViewRerankConfiguration.maxCandidates)
                && Objects.equals(
                        this.includeWhyMatch, agenticViewRerankConfiguration.includeWhyMatch)
                && Objects.equals(this.timeoutMs, agenticViewRerankConfiguration.timeoutMs);
    }

    @Override
    public int hashCode() {
        return Objects.hash(interaction, maxCandidates, includeWhyMatch, timeoutMs);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class AgenticViewRerankConfiguration {\n");
        sb.append("    interaction: ").append(toIndentedString(interaction)).append("\n");
        sb.append("    maxCandidates: ").append(toIndentedString(maxCandidates)).append("\n");
        sb.append("    includeWhyMatch: ").append(toIndentedString(includeWhyMatch)).append("\n");
        sb.append("    timeoutMs: ").append(toIndentedString(timeoutMs)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields =
                new HashSet<String>(
                        Arrays.asList(
                                "interaction",
                                "max_candidates",
                                "include_why_match",
                                "timeout_ms"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(0);
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to AgenticViewRerankConfiguration
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!AgenticViewRerankConfiguration.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in AgenticViewRerankConfiguration is not found in the empty JSON string",
                                AgenticViewRerankConfiguration.openapiRequiredFields.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if ((jsonObj.get("interaction") != null && !jsonObj.get("interaction").isJsonNull())
                && !jsonObj.get("interaction").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `interaction` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("interaction").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!AgenticViewRerankConfiguration.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'AgenticViewRerankConfiguration' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<AgenticViewRerankConfiguration> thisAdapter =
                    gson.getDelegateAdapter(
                            this, TypeToken.get(AgenticViewRerankConfiguration.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<AgenticViewRerankConfiguration>() {
                        @Override
                        public void write(JsonWriter out, AgenticViewRerankConfiguration value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public AgenticViewRerankConfiguration read(JsonReader in)
                                throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of AgenticViewRerankConfiguration given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of AgenticViewRerankConfiguration
     * @throws IOException if the JSON string is invalid with respect to AgenticViewRerankConfiguration
     */
    public static AgenticViewRerankConfiguration fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, AgenticViewRerankConfiguration.class);
    }

    /**
     * Convert an instance of AgenticViewRerankConfiguration to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
