/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.5.0-dev.20260901.052906Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashSet;
import java.util.List;
import java.util.Objects;

/**
 * ViewActionsConfiguration
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class ViewActionsConfiguration {
    public static final String SERIALIZED_NAME_INCLUDE_DEFAULTS = "include_defaults";

    @SerializedName(SERIALIZED_NAME_INCLUDE_DEFAULTS)
    @jakarta.annotation.Nullable private Boolean includeDefaults;

    /**
     * Gets or Sets excludeDefaults
     */
    @JsonAdapter(ExcludeDefaultsEnum.Adapter.class)
    public enum ExcludeDefaultsEnum {
        EXPORT("export"),

        DELETE("delete"),

        UNKNOWN_DEFAULT_OPEN_API("unknown_default_open_api");

        private String value;

        ExcludeDefaultsEnum(String value) {
            this.value = value;
        }

        public String getValue() {
            return value;
        }

        @Override
        public String toString() {
            return String.valueOf(value);
        }

        public static ExcludeDefaultsEnum fromValue(String value) {
            for (ExcludeDefaultsEnum b : ExcludeDefaultsEnum.values()) {
                if (b.value.equals(value)) {
                    return b;
                }
            }
            return UNKNOWN_DEFAULT_OPEN_API;
        }

        public static class Adapter extends TypeAdapter<ExcludeDefaultsEnum> {
            @Override
            public void write(final JsonWriter jsonWriter, final ExcludeDefaultsEnum enumeration)
                    throws IOException {
                jsonWriter.value(enumeration.getValue());
            }

            @Override
            public ExcludeDefaultsEnum read(final JsonReader jsonReader) throws IOException {
                String value = jsonReader.nextString();
                return ExcludeDefaultsEnum.fromValue(value);
            }
        }

        public static void validateJsonElement(JsonElement jsonElement) throws IOException {
            String value = jsonElement.getAsString();
            ExcludeDefaultsEnum.fromValue(value);
        }
    }

    public static final String SERIALIZED_NAME_EXCLUDE_DEFAULTS = "exclude_defaults";

    @SerializedName(SERIALIZED_NAME_EXCLUDE_DEFAULTS)
    @jakarta.annotation.Nullable private List<ExcludeDefaultsEnum> excludeDefaults = new ArrayList<>();

    public static final String SERIALIZED_NAME_ITEMS = "items";

    @SerializedName(SERIALIZED_NAME_ITEMS)
    @jakarta.annotation.Nullable private List<ViewActionConfiguration> items = new ArrayList<>();

    public ViewActionsConfiguration() {}

    public ViewActionsConfiguration includeDefaults(
            @jakarta.annotation.Nullable Boolean includeDefaults) {
        this.includeDefaults = includeDefaults;
        return this;
    }

    /**
     * Include the standard content export and delete actions. Defaults to true when selection is enabled.
     * @return includeDefaults
     */
    @jakarta.annotation.Nullable public Boolean getIncludeDefaults() {
        return includeDefaults;
    }

    public void setIncludeDefaults(@jakarta.annotation.Nullable Boolean includeDefaults) {
        this.includeDefaults = includeDefaults;
    }

    public ViewActionsConfiguration excludeDefaults(
            @jakarta.annotation.Nullable List<ExcludeDefaultsEnum> excludeDefaults) {
        this.excludeDefaults = excludeDefaults;
        return this;
    }

    public ViewActionsConfiguration addExcludeDefaultsItem(
            ExcludeDefaultsEnum excludeDefaultsItem) {
        if (this.excludeDefaults == null) {
            this.excludeDefaults = new ArrayList<>();
        }
        this.excludeDefaults.add(excludeDefaultsItem);
        return this;
    }

    /**
     * Hide individual standard actions while keeping the remaining defaults.
     * @return excludeDefaults
     */
    @jakarta.annotation.Nullable public List<ExcludeDefaultsEnum> getExcludeDefaults() {
        return excludeDefaults;
    }

    public void setExcludeDefaults(
            @jakarta.annotation.Nullable List<ExcludeDefaultsEnum> excludeDefaults) {
        this.excludeDefaults = excludeDefaults;
    }

    public ViewActionsConfiguration items(
            @jakarta.annotation.Nullable List<ViewActionConfiguration> items) {
        this.items = items;
        return this;
    }

    public ViewActionsConfiguration addItemsItem(ViewActionConfiguration itemsItem) {
        if (this.items == null) {
            this.items = new ArrayList<>();
        }
        this.items.add(itemsItem);
        return this;
    }

    /**
     * Get items
     * @return items
     */
    @jakarta.annotation.Nullable public List<ViewActionConfiguration> getItems() {
        return items;
    }

    public void setItems(@jakarta.annotation.Nullable List<ViewActionConfiguration> items) {
        this.items = items;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ViewActionsConfiguration viewActionsConfiguration = (ViewActionsConfiguration) o;
        return Objects.equals(this.includeDefaults, viewActionsConfiguration.includeDefaults)
                && Objects.equals(this.excludeDefaults, viewActionsConfiguration.excludeDefaults)
                && Objects.equals(this.items, viewActionsConfiguration.items);
    }

    @Override
    public int hashCode() {
        return Objects.hash(includeDefaults, excludeDefaults, items);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class ViewActionsConfiguration {\n");
        sb.append("    includeDefaults: ").append(toIndentedString(includeDefaults)).append("\n");
        sb.append("    excludeDefaults: ").append(toIndentedString(excludeDefaults)).append("\n");
        sb.append("    items: ").append(toIndentedString(items)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields =
                new HashSet<String>(Arrays.asList("include_defaults", "exclude_defaults", "items"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(0);
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to ViewActionsConfiguration
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!ViewActionsConfiguration.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in ViewActionsConfiguration is not found in the empty JSON string",
                                ViewActionsConfiguration.openapiRequiredFields.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // ensure the optional json data is an array if present
        if (jsonObj.get("exclude_defaults") != null
                && !jsonObj.get("exclude_defaults").isJsonNull()
                && !jsonObj.get("exclude_defaults").isJsonArray()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `exclude_defaults` to be an array in the JSON string but got `%s`",
                            jsonObj.get("exclude_defaults").toString()));
        }
        if (jsonObj.get("items") != null && !jsonObj.get("items").isJsonNull()) {
            JsonArray jsonArrayitems = jsonObj.getAsJsonArray("items");
            if (jsonArrayitems != null) {
                // ensure the json data is an array
                if (!jsonObj.get("items").isJsonArray()) {
                    throw new IllegalArgumentException(
                            String.format(
                                    java.util.Locale.ROOT,
                                    "Expected the field `items` to be an array in the JSON string but got `%s`",
                                    jsonObj.get("items").toString()));
                }

                // validate the optional field `items` (array)
                for (int i = 0; i < jsonArrayitems.size(); i++) {
                    ViewActionConfiguration.validateJsonElement(jsonArrayitems.get(i));
                }
                ;
            }
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ViewActionsConfiguration.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'ViewActionsConfiguration' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ViewActionsConfiguration> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(ViewActionsConfiguration.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<ViewActionsConfiguration>() {
                        @Override
                        public void write(JsonWriter out, ViewActionsConfiguration value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public ViewActionsConfiguration read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of ViewActionsConfiguration given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of ViewActionsConfiguration
     * @throws IOException if the JSON string is invalid with respect to ViewActionsConfiguration
     */
    public static ViewActionsConfiguration fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, ViewActionsConfiguration.class);
    }

    /**
     * Convert an instance of ViewActionsConfiguration to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
