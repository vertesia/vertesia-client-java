/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` SDK for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.2.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;

/**
 * AppInstallationListEntry
 */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class AppInstallationListEntry {
    public static final String SERIALIZED_NAME_ID = "id";

    @SerializedName(SERIALIZED_NAME_ID)
    @javax.annotation.Nonnull
    private String id;

    public static final String SERIALIZED_NAME_PROJECT = "project";

    @SerializedName(SERIALIZED_NAME_PROJECT)
    @javax.annotation.Nonnull
    private String project;

    public static final String SERIALIZED_NAME_SETTINGS = "settings";

    @SerializedName(SERIALIZED_NAME_SETTINGS)
    @javax.annotation.Nullable private Map<String, Object> settings = new HashMap<>();

    public static final String SERIALIZED_NAME_TOOL_ALLOWLIST = "tool_allowlist";

    @SerializedName(SERIALIZED_NAME_TOOL_ALLOWLIST)
    @javax.annotation.Nullable private List<String> toolAllowlist = new ArrayList<>();

    public static final String SERIALIZED_NAME_OAUTH_BINDINGS = "oauth_bindings";

    @SerializedName(SERIALIZED_NAME_OAUTH_BINDINGS)
    @javax.annotation.Nullable private List<AppInstallationOAuthBinding> oauthBindings = new ArrayList<>();

    public static final String SERIALIZED_NAME_PROVIDER_BINDINGS = "provider_bindings";

    @SerializedName(SERIALIZED_NAME_PROVIDER_BINDINGS)
    @javax.annotation.Nullable private List<AppInstallationProviderBinding> providerBindings = new ArrayList<>();

    public static final String SERIALIZED_NAME_CREATED_AT = "created_at";

    @SerializedName(SERIALIZED_NAME_CREATED_AT)
    @javax.annotation.Nonnull
    private String createdAt;

    public static final String SERIALIZED_NAME_UPDATED_AT = "updated_at";

    @SerializedName(SERIALIZED_NAME_UPDATED_AT)
    @javax.annotation.Nonnull
    private String updatedAt;

    public static final String SERIALIZED_NAME_MANIFEST = "manifest";

    @SerializedName(SERIALIZED_NAME_MANIFEST)
    @javax.annotation.Nullable private AppManifest manifest;

    public static final String SERIALIZED_NAME_OAUTH_COLLECTION_IDS = "oauth_collection_ids";

    @SerializedName(SERIALIZED_NAME_OAUTH_COLLECTION_IDS)
    @javax.annotation.Nullable private List<String> oauthCollectionIds = new ArrayList<>();

    public AppInstallationListEntry() {}

    public AppInstallationListEntry id(@javax.annotation.Nonnull String id) {
        this.id = id;
        return this;
    }

    /**
     * Get id
     * @return id
     */
    @javax.annotation.Nonnull
    public String getId() {
        return id;
    }

    public void setId(@javax.annotation.Nonnull String id) {
        this.id = id;
    }

    public AppInstallationListEntry project(@javax.annotation.Nonnull String project) {
        this.project = project;
        return this;
    }

    /**
     * Get project
     * @return project
     */
    @javax.annotation.Nonnull
    public String getProject() {
        return project;
    }

    public void setProject(@javax.annotation.Nonnull String project) {
        this.project = project;
    }

    public AppInstallationListEntry settings(
            @javax.annotation.Nullable Map<String, Object> settings) {
        this.settings = settings;
        return this;
    }

    public AppInstallationListEntry putSettingsItem(String key, Object settingsItem) {
        if (this.settings == null) {
            this.settings = new HashMap<>();
        }
        this.settings.put(key, settingsItem);
        return this;
    }

    /**
     * Get settings
     * @return settings
     */
    @javax.annotation.Nullable public Map<String, Object> getSettings() {
        return settings;
    }

    public void setSettings(@javax.annotation.Nullable Map<String, Object> settings) {
        this.settings = settings;
    }

    public AppInstallationListEntry toolAllowlist(
            @javax.annotation.Nullable List<String> toolAllowlist) {
        this.toolAllowlist = toolAllowlist;
        return this;
    }

    public AppInstallationListEntry addToolAllowlistItem(String toolAllowlistItem) {
        if (this.toolAllowlist == null) {
            this.toolAllowlist = new ArrayList<>();
        }
        this.toolAllowlist.add(toolAllowlistItem);
        return this;
    }

    /**
     * Admin-managed allowlist of tool names permitted for this installation. When undefined, all tools from the app are permitted. When set, only listed tool names are available for agent configuration and execution.
     * @return toolAllowlist
     */
    @javax.annotation.Nullable public List<String> getToolAllowlist() {
        return toolAllowlist;
    }

    public void setToolAllowlist(@javax.annotation.Nullable List<String> toolAllowlist) {
        this.toolAllowlist = toolAllowlist;
    }

    public AppInstallationListEntry oauthBindings(
            @javax.annotation.Nullable List<AppInstallationOAuthBinding> oauthBindings) {
        this.oauthBindings = oauthBindings;
        return this;
    }

    public AppInstallationListEntry addOauthBindingsItem(
            AppInstallationOAuthBinding oauthBindingsItem) {
        if (this.oauthBindings == null) {
            this.oauthBindings = new ArrayList<>();
        }
        this.oauthBindings.add(oauthBindingsItem);
        return this;
    }

    /**
     * OAuth bindings created at install time via oauth_config provisioning. Maps collection identity (id or name) → OAuth provider ObjectId. Used by the runtime to resolve the correct OAuth provider without relying on manifest names.
     * @return oauthBindings
     */
    @javax.annotation.Nullable public List<AppInstallationOAuthBinding> getOauthBindings() {
        return oauthBindings;
    }

    public void setOauthBindings(
            @javax.annotation.Nullable List<AppInstallationOAuthBinding> oauthBindings) {
        this.oauthBindings = oauthBindings;
    }

    public AppInstallationListEntry providerBindings(
            @javax.annotation.Nullable List<AppInstallationProviderBinding> providerBindings) {
        this.providerBindings = providerBindings;
        return this;
    }

    public AppInstallationListEntry addProviderBindingsItem(
            AppInstallationProviderBinding providerBindingsItem) {
        if (this.providerBindings == null) {
            this.providerBindings = new ArrayList<>();
        }
        this.providerBindings.add(providerBindingsItem);
        return this;
    }

    /**
     * OAuth bindings created at install time via oauth_providers provisioning. Maps provider key → OAuth provider ObjectId. Multiple collections sharing the same provider all resolve to the same OAuth provider.
     * @return providerBindings
     */
    @javax.annotation.Nullable public List<AppInstallationProviderBinding> getProviderBindings() {
        return providerBindings;
    }

    public void setProviderBindings(
            @javax.annotation.Nullable List<AppInstallationProviderBinding> providerBindings) {
        this.providerBindings = providerBindings;
    }

    public AppInstallationListEntry createdAt(@javax.annotation.Nonnull String createdAt) {
        this.createdAt = createdAt;
        return this;
    }

    /**
     * Get createdAt
     * @return createdAt
     */
    @javax.annotation.Nonnull
    public String getCreatedAt() {
        return createdAt;
    }

    public void setCreatedAt(@javax.annotation.Nonnull String createdAt) {
        this.createdAt = createdAt;
    }

    public AppInstallationListEntry updatedAt(@javax.annotation.Nonnull String updatedAt) {
        this.updatedAt = updatedAt;
        return this;
    }

    /**
     * Get updatedAt
     * @return updatedAt
     */
    @javax.annotation.Nonnull
    public String getUpdatedAt() {
        return updatedAt;
    }

    public void setUpdatedAt(@javax.annotation.Nonnull String updatedAt) {
        this.updatedAt = updatedAt;
    }

    public AppInstallationListEntry manifest(@javax.annotation.Nullable AppManifest manifest) {
        this.manifest = manifest;
        return this;
    }

    /**
     * Get manifest
     * @return manifest
     */
    @javax.annotation.Nullable public AppManifest getManifest() {
        return manifest;
    }

    public void setManifest(@javax.annotation.Nullable AppManifest manifest) {
        this.manifest = manifest;
    }

    public AppInstallationListEntry oauthCollectionIds(
            @javax.annotation.Nullable List<String> oauthCollectionIds) {
        this.oauthCollectionIds = oauthCollectionIds;
        return this;
    }

    public AppInstallationListEntry addOauthCollectionIdsItem(String oauthCollectionIdsItem) {
        if (this.oauthCollectionIds == null) {
            this.oauthCollectionIds = new ArrayList<>();
        }
        this.oauthCollectionIds.add(oauthCollectionIdsItem);
        return this;
    }

    /**
     * Get oauthCollectionIds
     * @return oauthCollectionIds
     */
    @javax.annotation.Nullable public List<String> getOauthCollectionIds() {
        return oauthCollectionIds;
    }

    public void setOauthCollectionIds(@javax.annotation.Nullable List<String> oauthCollectionIds) {
        this.oauthCollectionIds = oauthCollectionIds;
    }

    /**
     * A container for additional, undeclared properties.
     * This is a holder for any undeclared properties as specified with
     * the 'additionalProperties' keyword in the OAS document.
     */
    private Map<String, Object> additionalProperties;

    /**
     * Set the additional (undeclared) property with the specified name and value.
     * If the property does not already exist, create it otherwise replace it.
     *
     * @param key name of the property
     * @param value value of the property
     * @return the AppInstallationListEntry instance itself
     */
    public AppInstallationListEntry putAdditionalProperty(String key, Object value) {
        if (this.additionalProperties == null) {
            this.additionalProperties = new HashMap<String, Object>();
        }
        this.additionalProperties.put(key, value);
        return this;
    }

    /**
     * Return the additional (undeclared) property.
     *
     * @return a map of objects
     */
    public Map<String, Object> getAdditionalProperties() {
        return additionalProperties;
    }

    /**
     * Return the additional (undeclared) property with the specified name.
     *
     * @param key name of the property
     * @return an object
     */
    public Object getAdditionalProperty(String key) {
        if (this.additionalProperties == null) {
            return null;
        }
        return this.additionalProperties.get(key);
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        AppInstallationListEntry appInstallationListEntry = (AppInstallationListEntry) o;
        return Objects.equals(this.id, appInstallationListEntry.id)
                && Objects.equals(this.project, appInstallationListEntry.project)
                && Objects.equals(this.settings, appInstallationListEntry.settings)
                && Objects.equals(this.toolAllowlist, appInstallationListEntry.toolAllowlist)
                && Objects.equals(this.oauthBindings, appInstallationListEntry.oauthBindings)
                && Objects.equals(this.providerBindings, appInstallationListEntry.providerBindings)
                && Objects.equals(this.createdAt, appInstallationListEntry.createdAt)
                && Objects.equals(this.updatedAt, appInstallationListEntry.updatedAt)
                && Objects.equals(this.manifest, appInstallationListEntry.manifest)
                && Objects.equals(
                        this.oauthCollectionIds, appInstallationListEntry.oauthCollectionIds)
                && Objects.equals(
                        this.additionalProperties, appInstallationListEntry.additionalProperties);
    }

    @Override
    public int hashCode() {
        return Objects.hash(
                id,
                project,
                settings,
                toolAllowlist,
                oauthBindings,
                providerBindings,
                createdAt,
                updatedAt,
                manifest,
                oauthCollectionIds,
                additionalProperties);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class AppInstallationListEntry {\n");
        sb.append("    id: ").append(toIndentedString(id)).append("\n");
        sb.append("    project: ").append(toIndentedString(project)).append("\n");
        sb.append("    settings: ").append(toIndentedString(settings)).append("\n");
        sb.append("    toolAllowlist: ").append(toIndentedString(toolAllowlist)).append("\n");
        sb.append("    oauthBindings: ").append(toIndentedString(oauthBindings)).append("\n");
        sb.append("    providerBindings: ").append(toIndentedString(providerBindings)).append("\n");
        sb.append("    createdAt: ").append(toIndentedString(createdAt)).append("\n");
        sb.append("    updatedAt: ").append(toIndentedString(updatedAt)).append("\n");
        sb.append("    manifest: ").append(toIndentedString(manifest)).append("\n");
        sb.append("    oauthCollectionIds: ")
                .append(toIndentedString(oauthCollectionIds))
                .append("\n");
        sb.append("    additionalProperties: ")
                .append(toIndentedString(additionalProperties))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields =
                new HashSet<String>(
                        Arrays.asList(
                                "id",
                                "project",
                                "settings",
                                "tool_allowlist",
                                "oauth_bindings",
                                "provider_bindings",
                                "created_at",
                                "updated_at",
                                "manifest",
                                "oauth_collection_ids"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields =
                new HashSet<String>(
                        Arrays.asList("id", "project", "created_at", "updated_at", "manifest"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to AppInstallationListEntry
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!AppInstallationListEntry.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in AppInstallationListEntry is not found in the empty JSON string",
                                AppInstallationListEntry.openapiRequiredFields.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : AppInstallationListEntry.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (!jsonObj.get("id").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `id` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("id").toString()));
        }
        if (!jsonObj.get("project").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `project` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("project").toString()));
        }
        // ensure the optional json data is an array if present
        if (jsonObj.get("tool_allowlist") != null
                && !jsonObj.get("tool_allowlist").isJsonNull()
                && !jsonObj.get("tool_allowlist").isJsonArray()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `tool_allowlist` to be an array in the JSON string but got `%s`",
                            jsonObj.get("tool_allowlist").toString()));
        }
        if (jsonObj.get("oauth_bindings") != null && !jsonObj.get("oauth_bindings").isJsonNull()) {
            JsonArray jsonArrayoauthBindings = jsonObj.getAsJsonArray("oauth_bindings");
            if (jsonArrayoauthBindings != null) {
                // ensure the json data is an array
                if (!jsonObj.get("oauth_bindings").isJsonArray()) {
                    throw new IllegalArgumentException(
                            String.format(
                                    java.util.Locale.ROOT,
                                    "Expected the field `oauth_bindings` to be an array in the JSON string but got `%s`",
                                    jsonObj.get("oauth_bindings").toString()));
                }

                // validate the optional field `oauth_bindings` (array)
                for (int i = 0; i < jsonArrayoauthBindings.size(); i++) {
                    AppInstallationOAuthBinding.validateJsonElement(jsonArrayoauthBindings.get(i));
                }
                ;
            }
        }
        if (jsonObj.get("provider_bindings") != null
                && !jsonObj.get("provider_bindings").isJsonNull()) {
            JsonArray jsonArrayproviderBindings = jsonObj.getAsJsonArray("provider_bindings");
            if (jsonArrayproviderBindings != null) {
                // ensure the json data is an array
                if (!jsonObj.get("provider_bindings").isJsonArray()) {
                    throw new IllegalArgumentException(
                            String.format(
                                    java.util.Locale.ROOT,
                                    "Expected the field `provider_bindings` to be an array in the JSON string but got `%s`",
                                    jsonObj.get("provider_bindings").toString()));
                }

                // validate the optional field `provider_bindings` (array)
                for (int i = 0; i < jsonArrayproviderBindings.size(); i++) {
                    AppInstallationProviderBinding.validateJsonElement(
                            jsonArrayproviderBindings.get(i));
                }
                ;
            }
        }
        if (!jsonObj.get("created_at").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `created_at` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("created_at").toString()));
        }
        if (!jsonObj.get("updated_at").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `updated_at` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("updated_at").toString()));
        }
        if (jsonObj.get("manifest") != null && !jsonObj.get("manifest").isJsonNull()) {
            // validate the required field `manifest`
            AppManifest.validateJsonElement(jsonObj.get("manifest"));
        }
        // ensure the optional json data is an array if present
        if (jsonObj.get("oauth_collection_ids") != null
                && !jsonObj.get("oauth_collection_ids").isJsonNull()
                && !jsonObj.get("oauth_collection_ids").isJsonArray()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `oauth_collection_ids` to be an array in the JSON string but got `%s`",
                            jsonObj.get("oauth_collection_ids").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!AppInstallationListEntry.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'AppInstallationListEntry' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<AppInstallationListEntry> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(AppInstallationListEntry.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<AppInstallationListEntry>() {
                        @Override
                        public void write(JsonWriter out, AppInstallationListEntry value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            obj.remove("additionalProperties");
                            // serialize additional properties
                            if (value.getAdditionalProperties() != null) {
                                for (Map.Entry<String, Object> entry :
                                        value.getAdditionalProperties().entrySet()) {
                                    if (entry.getValue() instanceof String)
                                        obj.addProperty(entry.getKey(), (String) entry.getValue());
                                    else if (entry.getValue() instanceof Number)
                                        obj.addProperty(entry.getKey(), (Number) entry.getValue());
                                    else if (entry.getValue() instanceof Boolean)
                                        obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                                    else if (entry.getValue() instanceof Character)
                                        obj.addProperty(
                                                entry.getKey(), (Character) entry.getValue());
                                    else {
                                        JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                                        if (jsonElement.isJsonArray()) {
                                            obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                                        } else {
                                            obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                                        }
                                    }
                                }
                            }
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public AppInstallationListEntry read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            JsonObject jsonObj = jsonElement.getAsJsonObject();
                            // store additional fields in the deserialized instance
                            AppInstallationListEntry instance = thisAdapter.fromJsonTree(jsonObj);
                            for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
                                if (!openapiFields.contains(entry.getKey())) {
                                    if (entry.getValue().isJsonPrimitive()) { // primitive type
                                        if (entry.getValue().getAsJsonPrimitive().isString())
                                            instance.putAdditionalProperty(
                                                    entry.getKey(), entry.getValue().getAsString());
                                        else if (entry.getValue().getAsJsonPrimitive().isNumber())
                                            instance.putAdditionalProperty(
                                                    entry.getKey(), entry.getValue().getAsNumber());
                                        else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                                            instance.putAdditionalProperty(
                                                    entry.getKey(),
                                                    entry.getValue().getAsBoolean());
                                        else
                                            throw new IllegalArgumentException(
                                                    String.format(
                                                            java.util.Locale.ROOT,
                                                            "The field `%s` has unknown primitive type. Value: %s",
                                                            entry.getKey(),
                                                            entry.getValue().toString()));
                                    } else if (entry.getValue().isJsonArray()) {
                                        instance.putAdditionalProperty(
                                                entry.getKey(),
                                                gson.fromJson(entry.getValue(), List.class));
                                    } else { // JSON object
                                        instance.putAdditionalProperty(
                                                entry.getKey(),
                                                gson.fromJson(entry.getValue(), HashMap.class));
                                    }
                                }
                            }
                            return instance;
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of AppInstallationListEntry given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of AppInstallationListEntry
     * @throws IOException if the JSON string is invalid with respect to AppInstallationListEntry
     */
    public static AppInstallationListEntry fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, AppInstallationListEntry.class);
    }

    /**
     * Convert an instance of AppInstallationListEntry to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
