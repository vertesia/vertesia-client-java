/*
 * Vertesia API
 * The Vertesia API provides project-scoped access to interaction execution and content workflows. Use it to run interactions, inspect execution history, stream run progress, replay previous executions, and search content objects and collections. Authenticate requests with a bearer token, or use the official `@vertesia/client` package for typed access from Node.js applications.
 *
 * The version of the OpenAPI document: 1.6.0-dev.20260923.085325Z
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package io.vertesia.model;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.vertesia.JSON;
import java.io.IOException;
import java.util.Arrays;
import java.util.HashSet;
import java.util.Objects;

/**
 * UpdateInteractionConfigurationPayload
 */
@jakarta.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.22.0")
public class UpdateInteractionConfigurationPayload {
    public static final String SERIALIZED_NAME_INFERENCE_PROFILE = "inference_profile";

    @SerializedName(SERIALIZED_NAME_INFERENCE_PROFILE)
    @jakarta.annotation.Nullable private String inferenceProfile;

    public UpdateInteractionConfigurationPayload() {}

    public UpdateInteractionConfigurationPayload inferenceProfile(
            @jakarta.annotation.Nullable String inferenceProfile) {
        this.inferenceProfile = inferenceProfile;
        return this;
    }

    /**
     * MongoDB ObjectId of the inference profile.
     * @return inferenceProfile
     */
    @jakarta.annotation.Nullable public String getInferenceProfile() {
        return inferenceProfile;
    }

    public void setInferenceProfile(@jakarta.annotation.Nullable String inferenceProfile) {
        this.inferenceProfile = inferenceProfile;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        UpdateInteractionConfigurationPayload updateInteractionConfigurationPayload =
                (UpdateInteractionConfigurationPayload) o;
        return Objects.equals(
                this.inferenceProfile, updateInteractionConfigurationPayload.inferenceProfile);
    }

    @Override
    public int hashCode() {
        return Objects.hash(inferenceProfile);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class UpdateInteractionConfigurationPayload {\n");
        sb.append("    inferenceProfile: ").append(toIndentedString(inferenceProfile)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /**
     * Convert the given object to string with each line indented by 4 spaces
     * (except the first line).
     */
    private String toIndentedString(Object o) {
        return o == null ? "null" : o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>(Arrays.asList("inference_profile"));

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>(Arrays.asList("inference_profile"));
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to UpdateInteractionConfigurationPayload
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!UpdateInteractionConfigurationPayload.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field(s) %s in UpdateInteractionConfigurationPayload is not found in the empty JSON string",
                                UpdateInteractionConfigurationPayload.openapiRequiredFields
                                        .toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : UpdateInteractionConfigurationPayload.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(
                        String.format(
                                java.util.Locale.ROOT,
                                "The required field `%s` is not found in the JSON string: %s",
                                requiredField,
                                jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if ((jsonObj.get("inference_profile") != null
                        && !jsonObj.get("inference_profile").isJsonNull())
                && !jsonObj.get("inference_profile").isJsonPrimitive()) {
            throw new IllegalArgumentException(
                    String.format(
                            java.util.Locale.ROOT,
                            "Expected the field `inference_profile` to be a primitive type in the JSON string but got `%s`",
                            jsonObj.get("inference_profile").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!UpdateInteractionConfigurationPayload.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'UpdateInteractionConfigurationPayload'
                // and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<UpdateInteractionConfigurationPayload> thisAdapter =
                    gson.getDelegateAdapter(
                            this, TypeToken.get(UpdateInteractionConfigurationPayload.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<UpdateInteractionConfigurationPayload>() {
                        @Override
                        public void write(
                                JsonWriter out, UpdateInteractionConfigurationPayload value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public UpdateInteractionConfigurationPayload read(JsonReader in)
                                throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of UpdateInteractionConfigurationPayload given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of UpdateInteractionConfigurationPayload
     * @throws IOException if the JSON string is invalid with respect to UpdateInteractionConfigurationPayload
     */
    public static UpdateInteractionConfigurationPayload fromJson(String jsonString)
            throws IOException {
        return JSON.getGson().fromJson(jsonString, UpdateInteractionConfigurationPayload.class);
    }

    /**
     * Convert an instance of UpdateInteractionConfigurationPayload to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
